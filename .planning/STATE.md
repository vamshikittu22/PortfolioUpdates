# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-07-13)

**Core value:** The user opens the app (or gets a Telegram message) and immediately knows what's happening with *their* stocks — real holdings, real prices, real news — without digging through noise.
**Current focus:** Phase 5 — Alerts + Telegram

## Current Position

Phase: 6 of 7 (News Pipeline + Summarization) — executing CONCURRENTLY with Phase 7 (Daily Digest) per user direction; disjoint-file rounds, orchestrator-batched bookkeeping.
Plan: Phase 6 at 7/10 and Phase 7 at 2/5 after Round B (six concurrent executors, zero corrective commits: 06-04 feed parsers TDD, 06-05 Gemini summarization pure+wrapper TDD, 06-06 getNewsFeed + /news wiring, 06-07 news alert sweep, 07-01 digest_preferences + deploy-gated cron migrations + RLS check 11, 07-02 digest composition TDD with test:digest-compose). Round A earlier: (06-01 news schema migration authored; 06-02 dedup primitives TDD green + @google/genai 2.12.0 + fast-xml-parser 5.10.1 installed, all `test:news-*` scripts registered; 06-03 word-boundary instrument matcher TDD green — all SUMMARY-complete, Round A, zero git races under the no-bookkeeping rule). Phase 7 at 0/5, planned+checked. Both phases fully planned by parallel Fable swarm agents (06: 10 plans/5 waves, commit 3f96963 + external research 02fef3d — Finnhub/Google News RSS/Indian RSS/@google/genai all live-verified; 07: 5 plans/4 waves, commits 0b47f8c+4e4c592), both plan-checkers PASSED first iteration including the cross-phase news seam contract (07-03's getDailyDigestNews column set matches 06-01's schema; kind='daily_digest' pre-enumerated in the Phase 5 outbox CHECK). Phase 5 closed 2026-07-17: 9/9 plans (05-01 through 05-08 SUMMARY-complete; 05-09 SUMMARY-complete-as-DEFERRED), verifier passed 60/60 static checks (05-VERIFICATION.md, committed 712f29a) — only gaps are the token/migration deferrals, documented, not code gaps. Research complete (3 parallel researchers, committed 36d85de: telegram-api, schema-outbox, codebase slices); planning complete (9 plans in 5 waves, committed b36e307). Phase 4 closed 2026-07-16: 7/7 plans (04-01..04-06 SUMMARY-complete, 04-07 DEFERRED), verifier scored 9/10 must-haves (04-VERIFICATION.md, committed 03746d2) — the only gap is the consent-gated live E2E, documented, not a code gap.
Status: Phase 5 CODE-COMPLETE and STATIC-VERIFIED end to end; LIVE VERIFICATION OUTSTANDING. 05-09 (the phase-closing live-verify checkpoint) was reached on 2026-07-16; per standing user direction for this session, user-gated live verification is deferred and recorded honestly — no Telegram bot has been created (no `TELEGRAM_BOT_TOKEN`) and no consent was given to push either pending migration (`20260715230011_csv_import.sql` or `20260716221450_alerts_telegram.sql`) to the live DB. Recorded honestly as DEFERRED in 05-09-SUMMARY.md, matching the 03-06/04-07 precedent. Pre-checkpoint honest-state checks re-run: `npx tsc --noEmit` clean; `npm run build` clean (`/alerts` + both API routes listed); `npm run test:telegram` PASS; `npm run test:alerts` PASS; `npm run test:import-parse` PASS (regression); `npm run test:rls` honest FAIL (`public.import_batches` not found — both Phase 4's and Phase 5's migrations genuinely still unapplied live). 05-08 (/alerts UI rewrite, wave 4) is CODE-COMPLETE and STATIC-VERIFIED — `src/lib/types.ts`'s mock-era `AlertItem` (categorical kinds, string threshold, Email/Push/In-App delivery, no instrument identity) is retired, re-exporting `PriceAlertView` (05-07) as the single alert display type. `src/components/dashboard/AlertsTable.tsx` rewritten to render `PriceAlertView[]`: direction icon + numeric threshold, honest em-dash current price when unpriced, a single Telegram delivery indicator (Push/Email/In-App icons removed, not relabeled), Active/Paused status, and edit/toggle/delete row actions each in their own `useTransition` with per-row inline errors. `src/components/dashboard/AlertFormDialog.tsx` (new) — create/edit dialog modeled on `HoldingFormDialog.tsx`: debounced real-instrument-master search in add mode, fixed read-only instrument in edit mode, direction/threshold(>0)/cooldown(default 1440, min 60) fields, calling `createPriceAlert`/`updatePriceAlert` (05-07) with inline `{success:false,error}` surfacing. `src/components/dashboard/TelegramLinkCard.tsx` (new) — the ALRT-01 handshake UI replacing the dead "Delivery Settings" button: unlinked → "Link Telegram" renders the `t.me` deep link → "I've sent /start" → `checkTelegramLink` polls+binds → linked view (badge + pinned `en-IN`/`Asia/Kolkata` linked-since timestamp) → Unlink, every branch including the honest "no /start received yet" case surfaced inline. `src/app/(dashboard)/alerts/page.tsx` rewritten from a static mock page into an async auth-guarded Server Component (same `createClient`→`auth.getUser`→`if (!user) return null`→`getAccountId` pattern as `holdings/page.tsx`), reading `getPriceAlerts` + `getTelegramLink` in parallel; the three Phase-6 marketing cards (Price/Sentiment/Volume) are gone, replaced by one honest "Price Alerts" note. One Rule-3 blocking fix: `src/server-actions/alerts.ts`'s bare `export { searchInstrumentsAction } from './portfolio'` silently zeroed out this `'use server'` module's entire client-bundle export surface (a bundler-only failure, invisible to `tsc` alone — only surfaced via `npm run build` once `AlertsTable`/`AlertFormDialog` actually imported from the file) and was replaced with a directly-declared async wrapper function. `npx tsc --noEmit` and `npm run build` both clean, `/alerts` listed in the build's route table, every plan-specified grep check passes. Committed as three atomic task commits (6f56e2b Task 1 + the Rule-3 fix, 9a7abd4 Task 2, 3d7665b Task 3) plus one self-caught corrective commit (e5df2e0): `git commit -m "..." -- <pathspec>` was discovered to silently re-stage a file from the WORKING TREE rather than honoring an already-precise index (built via `git hash-object -w` + `git update-index --cacheinfo` to isolate this session's unrelated pre-existing dirty `HoldingLot`/`Holding.lots` hunk in `src/lib/types.ts` from this plan's `AlertItem`→`PriceAlertView` hunk) — the unrelated hunk briefly landed in Task 1's commit and was reverted from history in the very next commit before Task 2 began; full account in 05-08-SUMMARY.md. Live click-through (real Telegram bot, real handshake, real price-crossing trigger) remains explicitly DEFERRED to 05-09, matching every prior Phase 5 plan. 05-06 (Telegram linking handshake, wave 3) is CODE-COMPLETE and STATIC-VERIFIED — `src/lib/telegram/redeem.ts` implements `redeemStartToken(admin, token, chatId)`, the single admin-client bind path shared by both handshake entry points: it looks up the token, treats an already-`linked` token with the SAME chat_id as idempotent success (a replayed /start update must never error or double-bind), rejects an already-used/revoked/expired token honestly, then binds via a compare-and-set `UPDATE ... WHERE status='pending'` (a concurrent redeem of the same token affects zero rows the second time), mapping a `telegram_links_chat_unique` violation to a friendly "already linked to another account" reason. `src/lib/telegram/read.ts` implements `getTelegramLink(supabase, userId)` — a cookie-bound RLS-scoped status read for the `/alerts` RSC, never the admin client. `src/server-actions/telegram.ts` implements `generateTelegramLink` (cookie-bound DELETE+INSERT of a fresh pending row, renders the token-free `https://t.me/<username>?start=<token>` deep link from `TELEGRAM_BOT_USERNAME`), `checkTelegramLink` (the no-public-URL dev handshake: `getUser()` gate on the cookie client, then the admin client polls `getTelegramUpdates()`, parses via `parseStartPayload`, redeems via `redeemStartToken`, acknowledges by re-polling with `offset = maxUpdateId + 1`, never swallows a non-ok poll result such as a 409 webhook-active), and `unlinkTelegram` (cookie-bound delete of the caller's own row). `POST /api/telegram/webhook` verifies `X-Telegram-Bot-Api-Secret-Token` against `TELEGRAM_WEBHOOK_SECRET` strictly BEFORE `createAdminClient()` is ever called (an unset secret always rejects), then redeems via the same `redeemStartToken`; live `setWebhook` activation is explicitly deferred to 05-09 (deploy-gated, same precedent as `price_refresh_cron.sql`) — never enabled locally, since that would 409 `checkTelegramLink`'s dev poll. `TELEGRAM_BOT_USERNAME` and `TELEGRAM_WEBHOOK_SECRET` added as labeled `.env.local` placeholders (gitignored). `npx tsc --noEmit` and `npm run build` both clean, all plan-specified greps pass. Committed as three atomic task commits; Tasks 2 and 3 were each committed with an explicit trailing pathspec because the concurrently-running 05-05 executor had its own files staged in the shared git index at those moments — `git show HEAD --stat` confirmed exactly one intended file landed each time. A transient `tsc` error (`DispatchSummary` not exported from `notifications/outbox.ts`) was observed between Task 1 and Task 2, traced to the concurrent 05-05 executor's in-flight files (`src/app/api/prices/refresh/route.ts`, `src/server-actions/prices.ts` — explicitly out of scope for this plan) and logged to `.planning/phases/05-alerts-telegram/deferred-items.md` rather than fixed; it resolved on its own once 05-05 completed. Live handshake (real BotFather bot + token, 05-01's migration applied) and live webhook activation remain explicitly DEFERRED to 05-09. 05-05 (evaluate-and-enqueue sweep + refresh-tail composition, wave 3) is CODE-COMPLETE and STATIC-VERIFIED — `src/lib/alerts/sweep.ts` implements `evaluateAndEnqueueAlerts(admin)`: loads all active `price_alerts` joined to instrument display + `investment_accounts.user_id` with the admin client (cross-user reach, same rationale as `discoverInstrumentIds`), loads matching `price_cache` rows, runs the pure `evaluateAlerts`, and for each trigger pre-renders `buildPriceAlertMessage` then calls `enqueueNotifications` BEFORE stamping `last_triggered_at` (enqueue-first, dedupe-key-backed crash-recovery ordering, documented as two non-transactional statements not a true transaction). Both `src/app/api/prices/refresh/route.ts` (cron entry point) and `src/server-actions/prices.ts` (`refreshPricesNow`) now run `evaluateAndEnqueueAlerts` + `dispatchOutbox` at the tail, AFTER `refreshAllPrices` returns, wrapped in an inner try/catch so a Telegram problem can never fail the price refresh itself (reported via an `alerts` result field instead); `refreshPricesNow` additionally revalidates `/alerts`; `refreshAllPrices` itself is confirmed byte-for-byte unchanged via `git diff`. One blocking-fix deviation: re-exported `DispatchSummary` from `src/lib/notifications/outbox.ts` (previously only in `notifications/types.ts`) to satisfy the plan's exact import shape. `npx tsc --noEmit` and `npm run build` both clean, every plan-specified grep check passes. Committed as two atomic task commits, the second restricted to an explicit pathspec (`git commit ... -- <3 files>`) because a concurrently running 05-06 executor had its own `src/server-actions/telegram.ts` staged in the shared git index at commit time — `git show HEAD --stat` confirmed exactly the intended files landed both times. Live triggering (real threshold crossing, real Telegram delivery, cooldown suppression) remains explicitly DEFERRED to 05-09. 05-04 (Telegram delivery subsystem, wave 2) is CODE-COMPLETE and STATIC-VERIFIED — `src/lib/telegram/api.ts` implements `sendTelegramMessage`/`getTelegramUpdates`/`getTelegramMe` as raw-fetch wrappers (HTML `parse_mode`, JSON-`ok`-envelope checked on every response, server-only `TELEGRAM_BOT_TOKEN`, never throws for a single send); `src/lib/notifications/{types,outbox}.ts` implement `enqueueNotifications` (dedupe-safe upsert on the `dedupe_key` partial unique index) and `dispatchOutbox` (claims via `claim_due_notifications` RPC, resolves linked chats, sends sequentially — never `Promise.all` — and records sent/retried(429 honors real `retry_after`)/failed(+revokes `telegram_links` on 403/blocked/chat-not-found)); `src/app/api/notifications/dispatch/route.ts` is secret-guarded (`NOTIFY_DISPATCH_SECRET`, separate from `PRICE_REFRESH_SECRET`) before any Supabase client is constructed, cloning `/api/prices/refresh`'s structure — confirmed against this repo's bundled Next 16.2.9 route-handler docs. `npx tsc --noEmit` and `npm run build` both clean, all plan-specified greps pass, three atomic task commits verified via `git show HEAD --stat` with a concurrent 05-07 executor active on disjoint files (no cross-contamination). `TELEGRAM_BOT_TOKEN` added as a labeled placeholder and `NOTIFY_DISPATCH_SECRET` self-generated in `.env.local` (gitignored); live sends and live claim-concurrency behavior remain DEFERRED to 05-09. 05-07 (price-alert CRUD Server Actions + getPriceAlerts read, wave 2) is CODE-COMPLETE and STATIC-VERIFIED — `src/server-actions/alerts.ts` implements create/update/toggle/delete over `price_alerts` (cookie-bound, RLS-authorized, never the admin client; 23505 mapped to a friendly duplicate-direction error; re-exports `searchInstrumentsAction` for real-master ticker selection; every mutation revalidates `/alerts`) and `src/lib/alerts/read.ts` implements `getPriceAlerts` (single joined query + batched price_cache lookup, honest-null `currentPrice`). `npx tsc --noEmit` clean, all plan-specified greps pass, both commits verified atomic via `git show HEAD --stat` with a concurrent 05-04 executor active on disjoint files (no cross-contamination this time, unlike the 05-01/02/03 races). 05-01 (schema: price_alerts + telegram_links + notifications_outbox + claim_due_notifications RPC + RLS test extension) is CODE-COMPLETE and STATIC-VERIFIED — one new migration authored (`supabase/migrations/20260716221450_alerts_telegram.sql`), `scripts/rls-isolation-test.ts` extended for all three new tables, `npx tsc --noEmit` clean. Live apply and live `npm run test:rls` run explicitly DEFERRED to 05-09 (consent-gated, matches 03-01→03-06/04-01→04-07 precedent) — `npm run test:rls` re-run and honest-FAILs at `import_batches not found` (Phase 4's migration is also still unapplied live), confirming the deferred state is real, not fabricated. 05-03 (pure alert evaluation) is also CODE-COMPLETE and STATIC-VERIFIED — `src/lib/alerts/{types,evaluate}.ts` implement `evaluateAlerts`/`computeAlertDedupeKey`/`isCooldownElapsed`, zero I/O, proven by `scripts/alerts-eval-test.ts` (15/15 case groups, node:assert/strict), `npx tsc --noEmit` clean on this plan's own files. 05-02 (pure Telegram logic) is also CODE-COMPLETE and STATIC-VERIFIED — `src/lib/telegram/{types,parse-start-payload,link-token,build-message,classify-send-error}.ts` implement the entire token-free tier (handshake text parsing, deep-link token generation/validation, HTML parse_mode message building with 4096-char truncation, and the 429/403/400/5xx send-error taxonomy), zero I/O, proven by `scripts/telegram-logic-test.ts` (18 assertions, node:assert/strict), `npx tsc --noEmit` clean project-wide. All three wave-1 plans ran concurrently in this same session on disjoint files. Phase 4 remains CODE-COMPLETE/STATIC-VERIFIED end to end with LIVE VERIFICATION OUTSTANDING (waiting on user's real broker files + migration-push consent) — 04-07 was reached 2026-07-16 and the user responded "defer"; recorded honestly in 04-07-SUMMARY.md, matching the 03-06 precedent.
Last activity: 2026-07-16 — 05-09 checkpoint reached and DEFERRED (no Telegram bot token yet, no migration-push consent yet); 05-09-SUMMARY.md written recording what was honestly verified now and the full deferred list. Earlier same day: 05-08 (/alerts UI rewrite) completed: Task 1 (6f56e2b) src/lib/types.ts (AlertItem retired) + src/components/dashboard/AlertsTable.tsx rewrite + src/server-actions/alerts.ts Rule-3 fix; corrective (e5df2e0) reverted an accidentally-included unrelated hunk from types.ts's git history (working tree untouched throughout); Task 2 (9a7abd4) src/components/dashboard/AlertFormDialog.tsx (new); Task 3 (3d7665b) src/components/dashboard/TelegramLinkCard.tsx (new) + src/app/(dashboard)/alerts/page.tsx rewrite. Ran solo (no concurrent executor this session). Earlier same day: 05-06 (Telegram linking handshake) completed: Task 1 (3ea0329) src/lib/telegram/{redeem,read}.ts; Task 2 (42a1bb8) src/server-actions/telegram.ts, committed with an explicit pathspec; Task 3 (ad1d433) src/app/api/telegram/webhook/route.ts, also committed with an explicit pathspec. Ran concurrently with 05-05 (evaluate-and-enqueue sweep, on src/lib/alerts/sweep.ts + src/app/api/prices/refresh/route.ts + src/server-actions/prices.ts + src/lib/notifications/outbox.ts) on disjoint files — a shared git-index race caused one `Unable to create index.lock` retry and both plans' Task-2-or-later commits used explicit trailing pathspecs as a result; `git show HEAD --stat` confirmed no cross-contamination on any of this plan's three commits. Earlier same day: 05-05 (evaluate-and-enqueue sweep + refresh-tail composition) completed: Task 1 (22de67b) src/lib/alerts/sweep.ts; Task 2 (963ff45) src/app/api/prices/refresh/route.ts + src/server-actions/prices.ts + src/lib/notifications/outbox.ts (DispatchSummary re-export). Earlier same day: 05-04 (Telegram delivery subsystem: raw-fetch API wrapper + retryable outbox engine + secret-guarded dispatch route) completed: Task 1 (71fe071) src/lib/telegram/api.ts + src/lib/notifications/types.ts; Task 2 (3d87880) src/lib/notifications/outbox.ts; Task 3 (2867b75) src/app/api/notifications/dispatch/route.ts. Ran concurrently with 05-07 (alert CRUD Server Actions, on src/server-actions/alerts.ts + src/lib/alerts/read.ts) on disjoint files — all three of this plan's commits confirmed atomic post-commit via `git show HEAD --stat`, no shared-doc race. Earlier same day: 05-07 (price-alert CRUD Server Actions + getPriceAlerts read) completed: Task 1 (bf0e11b) src/server-actions/alerts.ts create/update/toggle/delete; Task 2 (3318b43) src/lib/alerts/read.ts getPriceAlerts. Ran concurrently with 05-04 (outbox engine, on src/lib/telegram/api.ts + src/lib/notifications/* + dispatch route) on disjoint files — both this plan's commits confirmed atomic post-commit, no shared-doc race this time. Earlier same day: 05-02 (pure Telegram logic layer) completed: Task 1 RED (67d1009) + GREEN (dddd916) parseStartPayload/generateLinkToken/isValidLinkTokenShape/escapeHtml/buildPriceAlertMessage; Task 2 RED (92cacf0) + GREEN (2566bfc) classifySendError delivery taxonomy. Ran concurrently with 05-01 and 05-03 on disjoint files (src/lib/telegram/*, scripts/telegram-logic-test.ts, package.json — sole wave-1 package.json owner, registered both test:telegram and test:alerts); a parallel-executor git-index race briefly swept this plan's staged package.json/test-script into 05-03's own RED commit, caught via `git show HEAD --stat` (05-03's executor self-corrected with `git reset`), content verified intact before re-staging and re-committing with explicit pathspec-restricted commits (see 05-02-SUMMARY.md "Issues Encountered"). Earlier same day: 05-03 (pure alert evaluation core) completed: Task 1 RED (4d5ce8d) + GREEN (38eac34) evaluateAlerts/isCooldownElapsed with null/failed-price exclusion and strict direction boundaries; Task 2 RED (6f13857) + GREEN (d1d7080) computeAlertDedupeKey cooldown-window bucket key. Ran concurrently with 05-01 and 05-02 on disjoint files (src/lib/alerts/*, scripts/alerts-eval-test.ts); a parallel-executor git-index race swept a concurrent executor's files into this plan's first commit, caught via `git show HEAD --stat` and corrected with `git reset --soft HEAD~1` + selective re-staging before re-committing (see 05-03-SUMMARY.md "Issues Encountered"). Earlier same day: 05-01 (Phase 5 schema plan) completed: Task 1 (fe08893) price_alerts + legacy alerts drop, Task 2 (ffc7c9b) telegram_links + notifications_outbox + claim_due_notifications, Task 3 (a8dd620) RLS isolation test extension. Earlier: 04-07 checkpoint reached and DEFERRED at user direction (no broker files yet, no migration-push consent); 04-07-SUMMARY.md written recording what was honestly verified now and the full deferred list. Earlier: 04-06 (/import progressive page shell + container state machine + Holdings entry point) completed, committed as three atomic task commits (31f9f92 feat /import shell, f025ce1 feat ImportPage container, d21a675 feat Holdings header Import link). Earlier: 04-05 (import preview UI leaf components) completed, committed as three atomic task commits (4d65fac feat ImportDropzone+ImportSummary, df113ae feat PreviewTable, 9fd4e80 feat SymbolMappingSection); 04-04 (previewImport/commitImport Server Actions + next.config.ts body-size limit) completed, committed as three atomic task commits (79e3ae4 chore, 37e65ed feat, c39c74b feat); 04-03 (broker parsers + instrument matching + duplicate detection) completed, committed as three atomic TDD commits (chore fixtures/RED/GREEN — no REFACTOR needed); 04-02 (import parsing primitives) completed as four atomic TDD commits; 04-01 (CSV import storage + controlled-write foundation) completed as three atomic commits.

Progress: [███████] 100% of Phase 5 plans closed (9/9: 05-01 schema, 05-02 pure Telegram logic, 05-03 pure alert evaluation, 05-04 Telegram delivery subsystem, 05-05 alert sweep + refresh-tail composition, 05-06 Telegram linking handshake, 05-07 alert CRUD + read, 05-08 /alerts UI rewrite — all code-complete/static-verified; 05-09 live-verify checkpoint DEFERRED, live verification outstanding). Phase 4: [███████] 100% closed (7/7, 04-07 live-verify checkpoint DEFERRED). See each plan's own SUMMARY for detail.

### Verification status (rewritten 2026-07-14 after live DB + blocker clearing)

**A live hosted Supabase EXISTS** — project `ozkorwkhtamyaavuphhm`, real credentials in `.env.local` (gitignored). No Docker; Docker is permanently out of scope per user direction.

**CLEARED (genuinely verified against the live DB):**
1. ✅ The 5 Phase 1 + Phase 2 migrations are APPLIED to the live DB.
2. ✅ Real anon + service-role keys are in `.env.local` (no placeholders).
3. ✅ `npm run test:rls` → PASS (cross-user read/write blocked; price_cache/news_items write holes rejected). NOTE: this test was STALE — it referenced the `holdings` table Phase 2 dropped. Rewritten against `transactions`; only found because a live DB finally existed.
4. ✅ Phase 2 UAT 4/4 PASS (dashboard empty state, add holding, **persistence survives F5** — the headline criterion, watchlist add/remove). Seed instruments + `(isin, exchange)` identity confirmed live: INFY resolves distinctly on NSE and NYSE.
5. ✅ `npm run test:derive-holdings` 7/7 and `npm run test:price-pnl` 12/12 (pure logic, no DB needed).
6. ✅ Fabricated-value defects found and fixed: hardcoded Alerts `badge: 3` (3e6d0e5); `MOCK_HOLDINGS` in the YouTube analyze route, which also had NO auth gate (ecf939a).
7. ✅ FX provider fixed — `exchangerate.host` went key-only; swapped to Frankfurter/ECB (no key). Verified live: USD→INR 96.2, INR→USD 0.01039 (self-consistent), bad currency → honest HTTP 404, never a fabricated rate (bac8107).
8. ✅ Branch drift resolved — `master` fast-forwarded to contain all work (rollback point: cbf6b19). 66 commits unpushed to origin.

9. ✅ **Phase 3 schema migration APPLIED** (`price_fx_schema.sql`, 2026-07-14, explicit user consent). `fx_cache` exists; `price_cache` re-keyed to `instrument_id` with nullable price/source + `fetch_error` + `corporate_action_flag`.
10. ✅ **PRICE PIPELINE PROVEN LIVE.** `refreshAllPrices` against the hosted DB returned `{instrumentsConsidered:2, instrumentsFetched:2, succeeded:2, failed:0, fxUpdated:true}`. Real rows landed: `INFY.NSE = 1082.40`, `AAPL.NASDAQ = 317.31` (both `fetch_error: null`, `corporate_action_flag: false`), and `fx_cache USD_INR = 96.2`. Confirms end-to-end: Yahoo fetch for both Indian + US tickers, honest upsert, Frankfurter FX inside the real pipeline, and correct scoping (only held/watchlisted instruments fetched, not all 16 seeds).

**STILL OPEN:**
1. ⛔ **`price_refresh_cron.sql` deliberately NOT applied.** Held back on purpose (not an oversight): it is authored and sitting in `supabase/migrations/` as the only pending migration. Applying it while running locally would schedule a pg_cron job that silently fails every 3 hours, because Supabase's cloud cannot reach `localhost:3000`. Apply it ONLY when deploying — see item 2.
2. ⛔ **PRICE-02 (scheduled refresh) is DEPLOY-GATED, not just migration-gated.** `price_refresh_cron.sql` has Supabase's cloud pg_cron POST via pg_net to the refresh endpoint. Supabase's cloud CANNOT reach `localhost:3000`. The 3-hourly schedule can NEVER be verified locally — it requires deploying to a publicly reachable URL (e.g. Vercel) AND setting the DB config for the endpoint URL + `PRICE_REFRESH_SECRET`. Applying the cron migration while only running locally will schedule a job that silently fails every 3 hours. The on-demand "Refresh now" path IS locally verifiable once migrations land.
3. ⛔ Phase 1 browser auth UAT paused at user direction — 1/7 passed (sign-up→dashboard), 6 outstanding: real email shown, refresh persistence, logout, log back in, forged-cookie rejection, settings 401. NOTE: `/api/settings/keys` 401 and RLS isolation are proven at the API level; only the browser-observable checks are outstanding.
4. ⛔ `npx supabase db lint --level warning` (Security Advisor) never run against the live DB.
5. ⛔ Phase 2 UI round-trips never exercised: partial-sell, split/bonus, edit/delete through the dialogs (the underlying math IS proven by unit tests), research deep-link click-through, YouTube channel persistence.
6. ⛔ **Phase 4 live verification DEFERRED (04-07, 2026-07-16, user direction).** The whole 8-step live E2E is outstanding: (a) real Groww holdings-statement `.xlsx` + real Robinhood activity-report `.csv` do not exist yet — user will upload later; only synthetic fixtures in `scripts/fixtures/`; (b) Supabase CLI never authenticated/linked (`supabase login` + `supabase link --project-ref ozkorwkhtamyaavuphhm` not run — auth gate); (c) `20260715230011_csv_import.sql` NOT pushed (consent-gated; until then `import_batches`/`symbol_mappings`/provenance columns/partial unique index/`find_or_create_instrument` don't exist live and `npm run test:rls` honest-fails on `import_batches` not found); (d) the browser E2E — preview gates commit, symbol mapping/creation with a real ISIN, commit + live P&L via the Holdings link, idempotent re-import adding ZERO transactions, loud malformed-file failure. Resume: re-run /gsd:execute-phase 4 (or 04-07-PLAN.md directly) when files + consent exist. CAUTION: a blanket `supabase db push` would also apply the deliberately-held-back `price_refresh_cron.sql` (item 1) — push selectively.
7. ⛔ **Phase 5 live verification DEFERRED (05-09, 2026-07-16, standing user direction this session).** The whole 9-step live E2E is outstanding: (a) no Telegram bot exists yet — `TELEGRAM_BOT_TOKEN`/`TELEGRAM_BOT_USERNAME`/`TELEGRAM_WEBHOOK_SECRET` remain labeled `.env.local` placeholders, user has not messaged `@BotFather`; (b) `20260716221450_alerts_telegram.sql` NOT pushed (consent-gated, same as item 6's `csv_import.sql` — both are still pending together; until pushed, `price_alerts`/`telegram_links`/`notifications_outbox`/`claim_due_notifications` don't exist live and `npm run test:rls` honest-fails before even reaching the new Phase 5 assertions, at `import_batches` not found); (c) the live handshake — real `/start` click-through capturing a real `chat_id` (ALRT-01); (d) live alert creation + display (ALRT-02); (e) live trigger + cooldown — one message, no repeat inside the window (ALRT-03); (f) live outbox retry/dead-letter — permanent 403 revokes the link, retryable failure re-delivers via `POST /api/notifications/dispatch` (ALRT-05); (g) the webhook (`setWebhook`/`getWebhookInfo`) is additionally deploy-gated, same treatment as `price_refresh_cron.sql` — cannot be verified from `localhost` under any circumstances. Resume: re-run /gsd:execute-phase 5 (or 05-09-PLAN.md directly) once a bot token exists and migration-push consent is given. CAUTION carried forward unchanged: a `supabase db push` at that point must be restricted to `20260715230011_csv_import.sql` + `20260716221450_alerts_telegram.sql` ONLY — never the still-deliberately-held-back `price_refresh_cron.sql` (item 1) until a public deploy exists.

Resume: all 7 plans in Phase 2 (02-01 through 02-07) are now SUMMARY-complete. A live Supabase now exists and the DEFERRED items above involving the 5 Phase 1/2 migrations (test:rls, test:derive-holdings) are confirmed passing against it. Phase 3 is now underway: plan 03-01 (schema) is code-complete; plan 03-02 also has a SUMMARY from a concurrent run. Next: continue Phase 3 plans 03-03 onward, then push the accumulated new migrations (03-01's two files plus any from later plans) to the live DB with explicit user consent before Phase 3's live-verification checkpoint (03-06).

## Performance Metrics

**Velocity:**
- Total plans completed: 1
- Average duration: 17 min
- Total execution time: ~0.3 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01 | 1 | 4 | 17 min |

**Recent Trend:**
- Last 5 plans: 01-01 (17 min, 3 tasks, 6 files)
- Trend: —

*Updated after each plan completion*

**Plan detail:**

| Plan | Duration | Tasks | Files |
|------|----------|-------|-------|
| 01-01 | 17 min | 3 | 6 |
| Phase 02 P01 | 12min | 3 tasks | 3 files |
| Phase 02 P03 | 12min | 2 tasks | 2 files |
| Phase 02 P02 | 15min | 1 tasks | 5 files |
| Phase 02 P04 | 15min | 2 tasks | 2 files |
| Phase 02 P06 | 25min | 3 tasks | 6 files |
| Phase 02 P07 | 7min | 2 tasks | 3 files |
| Phase 03 P01 | 2min | 3 tasks | 2 files |
| Phase 03 P02 | 10min | 2 tasks | 4 files |
| Phase 03 P03 | 15min | 2 tasks | 2 files |
| Phase 03 P04 | 13min | 3 tasks | 4 files |
| Phase 03 P05 | 20min | 3 tasks | 6 files |
| Phase 04 P01 | 12min | 3 tasks | 2 files |
| Phase 04 P02 | 10min | 1 TDD task (4 sub-commits) | 6 files |
| Phase 04 P03 | 20min | 1 tasks | 7 files |
| Phase 04 P04 | 15min | 3 tasks | 2 files |
| Phase 04 P05 | 15min | 3 tasks | 4 files |
| Phase 04 P06 | 25min | 3 tasks | 3 files |
| Phase 05 P01 | 15min | 3 tasks | 2 files |
| Phase 05 P02 | 10min | 2 TDD tasks (4 sub-commits) | 7 files |
| Phase 05 P03 | 6min | 2 TDD tasks (4 sub-commits) | 3 files |
| Phase 05 P04 | 15min | 3 tasks | 4 files |
| Phase 05 P05 | 12min | 2 tasks | 4 files |
| Phase 05 P06 | 12min | 3 tasks | 4 files |
| Phase 05 P08 | 35min | 3 tasks (+1 blocking fix, +1 corrective commit) | 6 files |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- [Roadmap]: 7-phase strict-downward dependency order (auth → schema → prices → import/alerts → news → digest), converged on by all four research files.
- [Roadmap]: Two hardest-to-reverse choices front-loaded — instrument identity is ISIN+exchange (Phase 2) and holdings modeled as a transactions ledger (Phase 2), before import/prices build on them.
- [Roadmap]: ALRT-04 (significant-news Telegram alert) mapped to Phase 6, not Phase 5 — it needs news matching and reuses the Phase 5 outbox.
- [Roadmap]: AI (Gemini) is always a second pass — price/news pipelines must work on raw data before summarization is layered on.
- [Phase 01]: Phase 1 runs in CODE-ONLY / DEFER-VERIFICATION mode: no Docker and no live Supabase, so stack start, live migration apply, and real-key capture are deferred; .env.local holds labeled placeholders.
- [Phase 02]: Continued CODE-ONLY / DEFER-VERIFICATION mode: instruments/transactions/watchlist migrations authored and statically verified only, live apply deferred (no Docker/live Supabase).
- [Phase 02]: Data access layer (02-04): reads via injected SupabaseClient reusing deriveHoldings; 8 Server Actions mutate only via cookie-bound client (never admin), revalidate / /holdings /news
- [Phase 02]: [Phase 02, plan 07]: Research deep-link entry point (WIRE-01) — ticker param read once on mount; useSearchParams() required a Suspense boundary in research/page.tsx, confirmed by running next build.
- [Phase 02]: [Phase 02, plan 06]: Mock portfolio store (usePortfolioStore.ts, mock-portfolio.ts) deleted outright, closing out PORT-07 — layout.tsx uses a static single-account label instead of a fetched name (smaller diff, matches the single-account-per-user scope decision); News/Alerts pages pass honest empty arrays for not-yet-built Phase 5/6 data instead of any mock fallback.
- [Phase 03]: price_cache re-keyed from symbol to instrument_id; price/source made nullable to represent honest never-fetched state
- [Phase 03]: Price ingestion + P&L pure logic isolated in src/lib/prices/* with zero I/O; never-fabricate-a-value discipline (parse failures -> null, unpriced holdings -> status:'pending' with null fields, not 0); proven by npm run test:price-pnl (node:assert/strict, no jest/vitest)
- [Phase 03]: fetchPrices/fetchFXRate (03-03) are pure network wrappers keyed only by symbol/currency-pair strings — no Supabase/instrument_id awareness; caller (03-04) owns symbol->instrument_id mapping and preserving last-known-good fx_cache/price_cache rows on failure
- [Phase 03]: Live finding (03-03): exchangerate.host's free /convert endpoint now returns missing_access_key in production — the "free, no key" FX source assumed in 03-RESEARCH.md is stale. fetchFXRate degrades correctly (honest error, no fabricated rate) but 03-04/later must register a key or swap FX providers before fx_cache can populate for real.
- [Phase 03]: refreshAllPrices (03-04) is the single write path for price_cache/fx_cache, accepts an already-constructed admin client rather than building one itself; cron route (secret-guarded, checked before any Supabase call) and refreshPricesNow Server Action (auth-gated via getUser()) both call it identically. PRICE-03 left Pending — backend done but not yet wired to a UI button (03-05) or live-verified (03-06).
- [Phase 03]: [Phase 03, plan 05] getPortfolioPnL (src/lib/prices/get-portfolio-pnl.ts) is the sole place a Supabase client meets the pure P&L math — reads only (cookie-bound client safe), reuses getHoldings/calculateHoldingPnL/calculatePortfolioTotals unchanged. When a non-base-currency holding exists but no FX rate is cached, that holding is excluded from the cross-currency portfolio total (never converted at a fabricated 1:1) while its own row still shows real native price/P&L. PRICE-03/PRICE-04 left Pending — UI wiring (RefreshPricesButton, StalenessBadge) is code-complete but live rendering against real cached rows is deferred to 03-06 (migrations not yet pushed).
- [Phase 04, plan 01]: One new migration (`supabase/migrations/20260715230011_csv_import.sql`) is the only schema/RLS-posture-touching plan in Phase 4 — import_batches + symbol_mappings (account-ownership RLS, identical EXISTS-subquery shape to transactions), transactions.import_batch_id/import_row_hash + partial UNIQUE idempotency index, and find_or_create_instrument SECURITY DEFINER RPC as the sole controlled write path into the closed instruments table (authenticated-only, no admin client, no permissive INSERT policy). file_hash on import_batches deliberately left non-unique — idempotency enforcement lives at the row level via the partial index. scripts/rls-isolation-test.ts extended to cover both new tables; typechecks clean. Live migration apply + live test:rls run explicitly DEFERRED to 04-07 (matches the Phase 2/3 CODE-COMPLETE/STATIC-VERIFIED pattern); confirmed the deferred state is real by actually running npm run test:rls against the live hosted DB and getting an honest "table not found" failure, not a placeholder-credentials skip.
- [Phase 04, plan 02]: `src/lib/import/*` is a zero-I/O pure layer (types.ts + normalize.ts + row-hash.ts + detect-broker.ts), TDD'd via `scripts/import-primitives-test.ts` (node:assert/strict, no jest/vitest — same pattern as price-pnl-test.ts). Every normalization function returns `null` on unparseable input, never a fabricated `0` (project's cardinal sin per 04-RESEARCH). `computeRowHashes` hashes normalized strings (not raw floats) with a 1-based occurrence index, proven stable across repeated calls and drift-free against float round-tripping — the IMPT-05 idempotency linchpin. `xlsx` pinned to SheetJS's CDN tarball 0.20.3 (confirmed via `node_modules/xlsx/package.json`, zero `0.18.5` references in package-lock.json — the npm-registry version carries open CVEs). Both `test:import-primitives` and `test:import-parse` npm scripts registered now so 04-03 (parsers) needs no `package.json` edit.
- [Phase 04, plan 03]: `src/lib/import/{parse-groww,parse-robinhood,match-instruments,detect-duplicates}.ts` complete the pure import pipeline (zero I/O). `parseGroww` header-scans (never positional) and models each equity holding as ONE synthetic opening BUY, preserving cost basis exactly through `deriveHoldings` (proven by test, not just claimed) — MF (`INF...`) rows reported `unsupported`, an unrecognizable sheet throws `ImportParseError` echoing the first rows seen. `parseRobinhood` is header-name-driven (proven identical across 9- and 11-column fixtures), maps Buy/Sell/SPL to ledger txn types, routes every other Trans Code to `unsupported` with a reason. `matchInstruments` implements the locked broker-disambiguation decision (Groww ISIN auto-picks NSE>BSE, Robinhood ticker filtered to US exchanges+USD auto-picks NASDAQ>NYSE>OTHER, saved mapping short-circuits both). `detectDuplicates` implements hash/field-match/Groww-already-held rules in priority order. `scripts/import-parse-test.ts` (7 case groups, node:assert/strict) proves all of it against synthetic fixtures (`scripts/fixtures/groww-holdings-sample.xlsx`, `robinhood-activity-sample.csv`) — `npm run test:import-parse` passes, re-run twice for identical output, `npx tsc --noEmit` clean. TDD'd as 3 commits (chore fixtures / RED stubs+test / GREEN real implementation) — no REFACTOR needed. IMPT-01/IMPT-02/IMPT-04/IMPT-05 re-confirmed Complete; IMPT-03 deliberately left Pending — this plan supplies the validation/dedup machinery IMPT-03 needs, but the actual preview flow (Server Action + UI with skip/override) is 04-04 through 04-06's scope, not yet built.
- [Phase 04]: [Phase 04, plan 04] previewImport/commitImport Server Actions are thin orchestrators over the pure lib/import pipeline; commitImport hashes only the final imported subset (not the full eligible set), so a forced re-import of duplicates collides with the partial unique index as intended
- [Phase 04, plan 05]: `src/components/import/{ImportDropzone,PreviewTable,SymbolMappingSection,ImportSummary}.tsx` are the four leaf presentational components — pure typed props/callbacks, local view state only. PreviewTable surfaces bulk-only category toggles (IMPT-03 locked decision, no per-row checkboxes) and confirmed via commitImport's actual filtering logic that unsupported rows need no toggle (they're unconditionally excluded regardless of choices.importUnsupported). SymbolMappingSection (IMPT-04) resolves each unique unmatched symbol once via reused searchInstrumentsAction or a create-new form with an honest ISIN policy (prefilled Groww, required Robinhood, never fabricated). ImportSummary fires refreshPricesNow() once, fire-and-forget, and never auto-redirects. All four files pre-existed as uncommitted drafts from a prior session; this plan verified each against the plan spec and real Server Action code before committing as three atomic task commits.
- [Phase 04]: [Phase 04, plan 06]: ImportPage owns all import interaction state and is the sole place previewImport/commitImport are called, re-sending the same File both times; commit gated on preview.unmatchedSymbols minus choices.mappings so no unresolved symbol can be silently dropped
- [Phase 05, plan 01]: One new migration (`supabase/migrations/20260716221450_alerts_telegram.sql`) drops the empty legacy Phase-1 `alerts` table and creates `price_alerts` (account-ownership RLS, one alert per (account, instrument, direction), cooldown_minutes default 1440/floor 60), `telegram_links` (user-keyed, deliberately NO UPDATE policy for anyone — that closure IS the ALRT-01 allowlist; chat_id/status='linked' can only ever be written by the service role; BIGINT chat_id, never INT), and `notifications_outbox` (generic kind/payload/dedupe_key, zero authenticated write policies, service-role-only — same closed posture as price_cache/fx_cache) plus `claim_due_notifications` (SECURITY DEFINER, FOR UPDATE SKIP LOCKED, dead-letters rows at 8 failed attempts, revoked from anon/authenticated — stricter than find_or_create_instrument). `scripts/rls-isolation-test.ts` extended (not rewritten) for all three tables, including a new "structural closure" assertion shape: telegram_links UPDATE must affect zero rows for BOTH a stranger AND the owner. ALRT-01/02/03/05 marked Complete in REQUIREMENTS.md in the same code-complete/static-verified sense used throughout this project (matches the plan's own frontmatter requirements list and the IMPT-04/05-at-04-01 precedent) — actual handshake/evaluation/dispatch/UI functionality lands in 05-02 through 05-08, live apply + live RLS run remain DEFERRED to 05-09. Ran concurrently with 05-02 (Telegram pure logic) and 05-03 (alert evaluation pure logic) in the same session on disjoint files (src/lib/telegram/*, src/lib/alerts/*, package.json, their own test scripts) — untouched by this plan.
- [Phase 05, plan 02]: `src/lib/telegram/{types,parse-start-payload,link-token,build-message,classify-send-error}.ts` implement the entire token-free Telegram tier — `parseStartPayload` requires a single `[A-Za-z0-9_-]{1,64}` run after `/start `, `generateLinkToken` uses `crypto.randomBytes(32).toString('base64url')` (43 chars), `buildPriceAlertMessage` uses HTML `parse_mode` exclusively (only `displaySymbol` is escaped — the sole untrusted interpolated value) and truncates to 4096 chars, `classifySendError` keys on `error_code` first with case-insensitive substring description matching (429→retryable+retry_after, 403→permanent, any 400→permanent, 5xx/null→retryable). This plan is Phase 5's sole wave-1 `package.json` owner — registered both `test:telegram` and `test:alerts` (the latter for sibling plan 05-03, which does not touch package.json itself). Zero I/O by design — `scripts/telegram-logic-test.ts` proves all 18 assertions under `node:assert/strict`, `npx tsc --noEmit` clean project-wide. TDD'd as 2 RED+GREEN task pairs (no REFACTOR needed). ALRT-01/ALRT-03 already marked Complete in REQUIREMENTS.md by 05-01's plan-level marking; this plan supplies the actual pure-logic proof those marks assumed. Ran concurrently with 05-01 (schema) and 05-03 (alert evaluation pure logic) on disjoint files — a parallel-executor git-index race briefly swept this plan's first-commit staged files into 05-03's own commit (self-corrected by 05-03 via `git reset`); all four of this plan's own commits were verified atomic and correctly scoped via `git show HEAD --stat` after every commit.
- [Phase 05, plan 03]: `src/lib/alerts/{types,evaluate}.ts` implement the pure alert-evaluation core — `evaluateAlerts` is level+cooldown (NOT edge detection: no previous-observed-price state exists in this schema, so ALRT-03's "does not repeat every refresh" is satisfied entirely by the cooldown gate on `last_triggered_at`), excludes both `price === null` (never-fetched) and `fetchError !== null` (last refresh attempt failed) snapshots BEFORE the threshold comparison ever runs — never alert on a fabricated or knowingly-stale value. Direction comparisons are strict (`>`/`<`); exactly-equal never fires (documented boundary). `computeAlertDedupeKey` produces the `price_alert:{id}:{floor(epoch_seconds/(cooldownMinutes*60))}` cooldown-window bucket key — identical within one window (backs the enqueue-first crash-recovery dedupe via `uniq_notifications_outbox_dedupe`), distinct across windows/alert ids. Both `isCooldownElapsed` and `computeAlertDedupeKey` exported as directly-testable units, matching `ingest.ts`'s export-everything-testable style. Zero I/O by design — `scripts/alerts-eval-test.ts` proves all 15 case groups under `node:assert/strict`, no DB/network/token dependency, nothing deferred from this plan. `package.json`'s `test:alerts` registration deliberately left to 05-02 (disjoint-file discipline within wave 1); verified via direct `npx tsx` invocation throughout. ALRT-02/ALRT-03 marked Complete in REQUIREMENTS.md. Ran concurrently with 05-01 (schema) and 05-02 (Telegram pure logic) on disjoint files.
- [Phase 05, plan 05]: `src/lib/alerts/sweep.ts` (`evaluateAndEnqueueAlerts`) composes 05-02/05-03/05-04's layers behind the admin client: loads active `price_alerts` joined to instrument display + `investment_accounts.user_id`, loads matching `price_cache`, runs the pure `evaluateAlerts`, and for each trigger enqueues the pre-rendered `buildPriceAlertMessage` output via `enqueueNotifications` BEFORE stamping `last_triggered_at` — enqueue-first ordering documented as a crash-recovery/dedupe-key backstop (two non-transactional statements, not a real transaction). Both `/api/prices/refresh` and `refreshPricesNow` now run `evaluateAndEnqueueAlerts` + `dispatchOutbox` at the tail after `refreshAllPrices` returns, wrapped in an inner try/catch so a Telegram outage never fails the price refresh (reported via a new `alerts` result field instead); `refreshPricesNow` also revalidates `/alerts`; `refreshAllPrices` itself confirmed unchanged via `git diff`. Re-exported `DispatchSummary` from `notifications/outbox.ts` (Rule 3 blocking fix — the type previously only lived in `notifications/types.ts`). `npx tsc --noEmit` and `npm run build` both clean. Ran concurrently with 05-06 (Telegram handshake Server Actions) on disjoint files; the second task commit used an explicit pathspec (`git commit ... -- <files>`) because 05-06's own staged file was present in the shared git index at commit time, confirmed atomic via `git show HEAD --stat`. Live triggering explicitly DEFERRED to 05-09.
- [Phase 05, plan 06]: `redeemStartToken` (src/lib/telegram/redeem.ts) is the single admin-client bind path shared by both handshake entry points (the dev poll and the deploy-gated webhook) — a compare-and-set `UPDATE ... WHERE status='pending'` makes a concurrent second redeem of the same token affect zero rows, and an already-`linked` token with the SAME chat_id is treated as idempotent success (not an error) since both entry points can independently observe the same /start update. `generateTelegramLink` uses DELETE-then-INSERT (not a PK upsert) to regenerate a pending row, matching `telegram_links`' actual RLS policy shape (separate INSERT/DELETE-own policies, deliberately no UPDATE policy — that closure IS the ALRT-01 allowlist). `checkTelegramLink` never swallows a non-ok `getUpdates` result (e.g. 409 webhook-active) — surfaced verbatim so a stale local webhook is diagnosable. The webhook route clones `/api/prices/refresh`'s guard-before-Supabase ordering exactly, with live `setWebhook` activation deferred to 05-09.
- [Phase 05, plan 04]: `src/lib/telegram/api.ts` mirrors `fetch-prices.ts`'s "network mechanics only" split — `sendTelegramMessage`/`getTelegramUpdates`/`getTelegramMe` are raw-fetch wrappers over `api.telegram.org`, parse the JSON `ok` envelope on every response (never trust HTTP status alone), and never throw for a single send (an unset `TELEGRAM_BOT_TOKEN` yields an honest `{ok:false, errorCode:null, ...}`, not a throw). `src/lib/notifications/outbox.ts` is the SOLE Telegram-sending path: `enqueueNotifications` dedupe-suppresses via the `dedupe_key` partial unique index (`upsert` + `ignoreDuplicates`), `dispatchOutbox` claims via the `claim_due_notifications` SKIP LOCKED RPC, batch-resolves `telegram_links` (only `status='linked' && chat_id != null`), and sends sequentially (never `Promise.all`) — outcomes map to sent / retried (429 overwrites `next_attempt_at` with the API's real `retry_after`, honest not fabricated) / failed (+ flips `telegram_links.status` to `'revoked'` on a 403 or a description containing "blocked"/"chat not found", closing the allowlist honestly). `POST /api/notifications/dispatch` clones `/api/prices/refresh`'s exact guard-before-Supabase structure with its own `NOTIFY_DISPATCH_SECRET` (kept separate from `PRICE_REFRESH_SECRET` for independent rotation) — confirmed against this repo's bundled Next 16.2.9 `route.md` docs (`export async function POST(request: Request)`). `TELEGRAM_BOT_TOKEN` added as a labeled placeholder and `NOTIFY_DISPATCH_SECRET` self-generated (both gitignored `.env.local`, informational). `npx tsc --noEmit` and `npm run build` both clean. ALRT-03/ALRT-05 already marked Complete in REQUIREMENTS.md by 05-01's plan-level marking; this plan supplies the actual delivery-mechanism proof those marks assumed. Ran concurrently with 05-07 (alert CRUD Server Actions) on disjoint files — all three commits verified atomic via `git show HEAD --stat`, no cross-contamination.
- [Phase 05, plan 08]: `src/lib/types.ts`'s mock-era `AlertItem` is retired in favor of re-exporting `PriceAlertView` (05-07) — the single alert display type UI code imports. `AlertsTable.tsx`, `AlertFormDialog.tsx` (new), and `TelegramLinkCard.tsx` (new) complete ALRT-01/ALRT-02's user-visible surface: Telegram-only delivery (mock Push/Email/In-App icons removed, not relabeled), real-instrument-master search reused from `HoldingFormDialog`'s pattern for alert creation, and the full link/check/unlink handshake state machine. `/alerts` is now a real auth-guarded async Server Component (mirrors `holdings/page.tsx`'s `createClient`→`auth.getUser`→`getAccountId` pattern), reading `getPriceAlerts`+`getTelegramLink`; the Phase-6 marketing cards are gone. One Rule-3 blocking fix: `src/server-actions/alerts.ts`'s bare `export { x } from './y'` re-export silently zeroed out the entire `'use server'` module's client-bundle exports (a `npm run build`-only failure, invisible to `tsc`) — fixed with a directly-declared async wrapper. **Git-mechanics lesson**: `git commit -m "..." -- <pathspec>` re-stages the given paths from the WORKING TREE, silently overriding any hand-crafted index content (e.g. from `git update-index --cacheinfo`) — when isolating hunks in a dirty shared file via git plumbing, commit with NO pathspec once the index already holds exactly the intended tree, or the isolation is defeated. Self-caught and corrected in the very next commit (see 05-08-SUMMARY.md). Live click-through remains DEFERRED to 05-09.

### Pending Todos

[From .planning/todos/pending/ — ideas captured during sessions]

None yet.

### Blockers/Concerns

[Issues that affect future work]

- REQUIREMENTS.md header stated "36 total" but the enumerated v1 IDs total **39** (AUTH 6, PORT 7, PRICE 7, IMPT 5, NEWS 5, ALRT 5, DGST 2, WIRE 2). Traceability count corrected to 39/39; header wording left for the user to confirm.
- [Phase 03, plan 03] exchangerate.host's free FX endpoint now requires a paid access key (`missing_access_key` error confirmed live) — 03-RESEARCH.md's "free, no key" assumption is stale. Must be resolved (API key or provider swap, e.g. Frankfurter/open.er-api.com) before 03-06's live FX refresh can populate `fx_cache` end-to-end. Does not block 03-03/03-04 (honest-failure contract fully met in both — refreshAllPrices records fetch_error and preserves the last-known-good rate, never blocks price refresh on FX failure).
- [Phase 03, plan 04] `refreshPricesNow` Server Action exists and is statically verified but has no UI caller yet (grep confirms zero references outside `src/server-actions/prices.ts`) — wiring a "refresh now" button is 03-05's scope. PRICE-03 left Pending in REQUIREMENTS.md accordingly.
- [Phase 03, plan 05] `RefreshPricesButton`/`StalenessBadge`/`getPortfolioPnL` are now code-complete and statically verified (tsc clean, greps confirm wiring), but zero live rendering has occurred — 03-01's two migrations remain unpushed, so `price_cache`/`fx_cache` cannot be queried against the new schema shape yet. PRICE-03 and PRICE-04 both left Pending in REQUIREMENTS.md; live verification is 03-06's scope.
- Research flags for phase-time verification: Vercel Hobby function-duration + pg_cron→pg_net ergonomics (Phase 3), Groww XLSX / Robinhood CSV real layouts (Phase 4), exact Gemini free-tier RPD + Finnhub US-only (Phase 6).
- [RESOLVED via decision] Plan 01-01 Task 3 was blocked by missing Docker. Superseded by the CODE-ONLY / DEFER-VERIFICATION decision — no Docker/live DB used. DEFERRED work carried forward: `supabase start`, applying migrations against a live DB, verifying RLS enforcement, and capturing real anon/service-role keys. Must be done before Phase 1 verification can pass.
- [Phase 04, plan 01] `supabase/migrations/20260715230011_csv_import.sql` is authored and statically verified but NOT yet pushed to the live hosted DB — `find_or_create_instrument` cannot be called live and `npm run test:rls`'s new import_batches/symbol_mappings checks cannot pass until 04-07's live-apply checkpoint (explicit user consent required, matching the 03-01→03-06 pattern). IMPT-04/IMPT-05 marked Complete in REQUIREMENTS.md in the code-complete/static-verified sense used throughout this project; live-behavior confirmation is 04-07's scope.
- Pre-existing uncommitted working-tree changes unrelated to Phase 4 were observed at the start of this session (`.planning/REQUIREMENTS.md` PORT-08/PORT-09 additions and a "Phase 3.1" reference, plus modified `src/components/dashboard/HoldingFormDialog.tsx`/`HoldingsTable.tsx`, `src/lib/supabase/portfolio.ts`, `src/lib/types.ts`, `src/server-actions/portfolio.ts`, and untracked `src/components/dashboard/LotEditDialog.tsx`). These were left untouched and uncommitted by 04-01's executor — out of scope for this plan. Whoever owns that work should commit or stash it separately. Still untouched and uncommitted as of 05-01 (2026-07-16).
- [Phase 05, plan 01] `supabase/migrations/20260716221450_alerts_telegram.sql` is authored and statically verified only — NOT yet pushed to the live hosted DB. `npm run test:rls` re-confirmed the honest DEFERRED state (fails at `import_batches not found` — Phase 4's still-unapplied migration blocks execution before it ever reaches the new Phase 5 assertions). ALRT-01/02/03/05 marked Complete in REQUIREMENTS.md in the code-complete/static-verified sense only; live handshake, evaluation, dispatch, and UI behavior are 05-02 through 05-09's scope. CAUTION carried forward: a blanket `supabase db push` at 05-09 would apply BOTH still-pending migrations (`20260715230011_csv_import.sql` and this one) plus the deliberately-held-back `price_refresh_cron.sql` — push selectively, with explicit user consent.
- [Phase 05, plan 06] `TELEGRAM_BOT_USERNAME` and `TELEGRAM_WEBHOOK_SECRET` added as labeled `.env.local` placeholders (gitignored) — no real BotFather bot exists yet, so `generateTelegramLink` will honestly error `TELEGRAM_BOT_USERNAME is not configured` and the webhook route will honestly 401 every request until 05-09 supplies real values. Live handshake (real /start click-through, real chat_id capture) and live webhook activation (`setWebhook` + `getWebhookInfo`, requires a public deploy — Telegram cannot reach `localhost`) both remain explicitly DEFERRED to 05-09, same treatment as `price_refresh_cron.sql`.

## Session Continuity

Last session: 2026-07-16
Stopped at: 05-09 (live-verification checkpoint) reached and closed as DEFERRED per standing user direction this session — no Telegram bot exists yet (`.env.local`'s `TELEGRAM_BOT_TOKEN`/`TELEGRAM_BOT_USERNAME`/`TELEGRAM_WEBHOOK_SECRET` confirmed still the labeled placeholders) and no consent was given to push `20260715230011_csv_import.sql` or `20260716221450_alerts_telegram.sql` (`npx supabase migration list` confirms the project is still not linked). Pre-checkpoint honest-state checks: `npx tsc --noEmit` clean, `npm run build` clean (`/alerts` + `/api/notifications/dispatch` + `/api/telegram/webhook` all listed), `npm run test:telegram` PASS, `npm run test:alerts` PASS, `npm run test:import-parse` PASS (regression), `npm run test:rls` honest FAIL (`public.import_batches` not in schema cache — both pending migrations genuinely unapplied). 05-09-SUMMARY.md records the deferral, the exact 9-item deferred list, and the resume path (matching the 03-06/04-07 precedent). Phase 5 is now 9/9 plans closed — all code-complete/static-verified, live verification outstanding (see STILL OPEN item 7). Next: Phase 6 (News Pipeline + Summarization) planning can proceed, OR re-run /gsd:execute-phase 5 / 05-09-PLAN.md the moment the user creates a Telegram bot + supplies migration-push consent.
Previously stopped at: 05-08 (/alerts UI rewrite) completed and SUMMARY-complete. `src/lib/types.ts` — `AlertItem` retired, re-exports `PriceAlertView` (05-07) as the single alert display type. `src/components/dashboard/AlertsTable.tsx` — rewritten for `PriceAlertView[]`: direction icon + numeric threshold, honest em-dash current price, single Telegram delivery indicator, Active/Paused status, edit/toggle/delete row actions each in their own `useTransition` with per-row inline errors. `src/components/dashboard/AlertFormDialog.tsx` (new) — create/edit dialog modeled on `HoldingFormDialog.tsx`: debounced real-instrument-master search (add mode) / fixed read-only instrument (edit mode), direction/threshold(>0)/cooldown(default 1440, min 60), calling `createPriceAlert`/`updatePriceAlert` with inline error surfacing. `src/components/dashboard/TelegramLinkCard.tsx` (new) — the ALRT-01 handshake UI (replaces the dead "Delivery Settings" button): unlinked → generate `t.me` link → "I've sent /start" → `checkTelegramLink` polls+binds → linked view (badge + pinned `en-IN`/`Asia/Kolkata` timestamp) → Unlink. `src/app/(dashboard)/alerts/page.tsx` — rewritten as an async auth-guarded Server Component reading `getPriceAlerts`+`getTelegramLink`, Phase-6 marketing cards removed. One Rule-3 blocking fix: `src/server-actions/alerts.ts`'s bare re-export silently broke the whole `'use server'` module's client-bundle exports (`npm run build`-only failure) — replaced with a directly-declared async wrapper. `npx tsc --noEmit` and `npm run build` both clean, `/alerts` listed in the build output, every plan-specified grep check passes. Committed as three atomic task commits (`6f56e2b` Task 1 + the blocking fix, `9a7abd4` Task 2, `3d7665b` Task 3) plus one self-caught corrective commit (`e5df2e0`) that reverted an accidentally-included unrelated hunk from `types.ts`'s git history — `git commit -m "..." -- <pathspec>` was discovered to silently re-stage from the working tree, overriding precise `git update-index --cacheinfo` isolation of this session's pre-existing unrelated `HoldingLot`/`Holding.lots` dirty changes; the working tree itself was never touched and local tsc/build stayed clean throughout — full account in 05-08-SUMMARY.md "Deviations". ALRT-01/ALRT-02 were already marked Complete in REQUIREMENTS.md by 05-01's plan-level marking; this plan supplies the actual UI-surface proof underpinning those marks. Live click-through (real Telegram bot, real handshake, real price-crossing trigger delivering a message) remains explicitly DEFERRED to 05-09. Next: 05-09 is the phase-closing live checkpoint (BotFather bot, migration apply with consent, real handshake + alert trigger/cooldown + outbox retry, deploy-gated webhook) — the LAST plan in Phase 5.
Previously stopped at: 05-06 (Telegram linking handshake) completed and SUMMARY-complete. `src/lib/telegram/redeem.ts` — `redeemStartToken(admin, token, chatId)`, the single admin-client bind path shared by both handshake entry points: validates a single-use, unexpired, `pending` token, treats an already-`linked` token with the SAME chat_id as idempotent success, rejects an already-used/expired token honestly, binds via a compare-and-set `UPDATE ... WHERE status='pending'`, and maps a `telegram_links_chat_unique` violation to a friendly reason. `src/lib/telegram/read.ts` — `getTelegramLink(supabase, userId)`, cookie-bound RLS-scoped status read for the `/alerts` RSC. `src/server-actions/telegram.ts` — `generateTelegramLink` (cookie-bound DELETE+INSERT pending row + token-free `t.me` deep link from `TELEGRAM_BOT_USERNAME`), `checkTelegramLink` (the no-public-URL dev handshake: `getUser()` gate then admin-client `getTelegramUpdates()` poll -> `parseStartPayload` -> `redeemStartToken`, re-polls to acknowledge, never swallows a non-ok poll result), `unlinkTelegram` (cookie-bound delete of the caller's own row). `src/app/api/telegram/webhook/route.ts` — `POST` verifies `X-Telegram-Bot-Api-Secret-Token` against `TELEGRAM_WEBHOOK_SECRET` strictly before `createAdminClient()`, redeems via the same `redeemStartToken`, always 200s a well-formed request; live `setWebhook` activation deferred to 05-09. `TELEGRAM_BOT_USERNAME`/`TELEGRAM_WEBHOOK_SECRET` added as labeled `.env.local` placeholders. `npx tsc --noEmit` and `npm run build` both clean, every plan-specified grep check passes. Committed as three atomic task commits: Task 1 `3ea0329` (redeem.ts + read.ts), Task 2 `42a1bb8` (telegram.ts Server Actions, explicit pathspec), Task 3 `ad1d433` (webhook route, explicit pathspec) — both later commits used an explicit trailing pathspec because the concurrently-running 05-05 executor had its own files staged in the shared git index at those moments; `git show HEAD --stat` confirmed no cross-contamination on any of the three. A transient `tsc` error (`DispatchSummary` not exported from `notifications/outbox.ts`) observed between Task 1 and Task 2 was traced to 05-05's in-flight files (explicitly out of scope for this plan) and logged to `deferred-items.md` rather than fixed — it resolved on its own once 05-05 completed. ALRT-01 was already marked Complete in REQUIREMENTS.md by 05-01's plan-level marking; this plan supplies the actual handshake-mechanism proof underpinning that mark. Live handshake (real BotFather bot + token, real /start click-through) and live webhook activation (`setWebhook`, requires a public deploy) remain explicitly DEFERRED to 05-09. Next: 05-08 (/alerts UI rewrite, can now import `generateTelegramLink`/`checkTelegramLink`/`unlinkTelegram`/`getTelegramLink` directly for the `TelegramLinkCard`) is the last remaining plan before 05-09's live checkpoint — check its PLAN.md's `depends_on` field before starting.
Previously stopped at: 05-05 (evaluate-and-enqueue sweep + refresh-tail composition) completed and SUMMARY-complete. `src/lib/alerts/sweep.ts` — `evaluateAndEnqueueAlerts(admin)` loads all active `price_alerts` joined to instrument display + `investment_accounts.user_id` with the admin client, loads matching `price_cache` rows, runs the pure `evaluateAlerts` (05-03), pre-renders each trigger via `buildPriceAlertMessage` (05-02), and enqueues via `enqueueNotifications` (05-04) BEFORE stamping `last_triggered_at` — enqueue-first, dedupe-key-backed. `src/app/api/prices/refresh/route.ts` and `src/server-actions/prices.ts` (`refreshPricesNow`) both now run `evaluateAndEnqueueAlerts` + `dispatchOutbox` at the tail after `refreshAllPrices` returns, wrapped in an inner try/catch so a Telegram problem never fails the price refresh; `refreshPricesNow` additionally revalidates `/alerts`; `refreshAllPrices` confirmed byte-for-byte unchanged via `git diff`. One blocking-fix deviation: re-exported `DispatchSummary` from `src/lib/notifications/outbox.ts`. `npx tsc --noEmit` and `npm run build` both clean, every plan-specified grep check passes. Committed as two atomic task commits: Task 1 `22de67b` (sweep.ts), Task 2 `963ff45` (both refresh entry points + outbox.ts re-export, committed with an explicit pathspec due to a concurrent 05-06 executor's staged file sharing the git index — confirmed atomic via `git show HEAD --stat`). ALRT-03/ALRT-05 were already marked Complete in REQUIREMENTS.md by 05-01's plan-level marking; this plan supplies the actual wiring proof underpinning those marks. Live triggering (real threshold crossing enqueueing once, real Telegram delivery, cooldown suppression) remains explicitly DEFERRED to 05-09.
Previously stopped at: 05-04 (Telegram delivery subsystem) completed and SUMMARY-complete. `src/lib/telegram/api.ts` — raw-fetch `sendTelegramMessage`/`getTelegramUpdates`/`getTelegramMe`, HTML `parse_mode`, JSON-`ok`-envelope checked on every response, server-only `TELEGRAM_BOT_TOKEN` (an unset token yields an honest error result, never a throw). `src/lib/notifications/types.ts` — `NotificationKind`/`EnqueueRow`/`OutboxRow`/`DispatchSummary`/`TelegramUpdate` declarations. `src/lib/notifications/outbox.ts` — `enqueueNotifications` (dedupe-safe upsert on the `dedupe_key` partial unique index) + `dispatchOutbox` (claims via `claim_due_notifications` SKIP LOCKED RPC, batch-resolves `telegram_links` where `status='linked' && chat_id != null`, sends sequentially — never `Promise.all` — records sent/retried(429 honors real `retry_after`)/failed(+revokes the link on 403/blocked/chat-not-found)). `src/app/api/notifications/dispatch/route.ts` — secret-guarded (`NOTIFY_DISPATCH_SECRET`, separate from `PRICE_REFRESH_SECRET`) POST route cloning `/api/prices/refresh`'s guard-before-Supabase structure, confirmed against this repo's bundled Next 16.2.9 route-handler docs. `TELEGRAM_BOT_TOKEN` added as a labeled placeholder, `NOTIFY_DISPATCH_SECRET` self-generated, both in gitignored `.env.local`. `npx tsc --noEmit` and `npm run build` both clean; every plan-specified grep check passes. Committed as three atomic task commits: Task 1 `71fe071` (api.ts + notifications/types.ts), Task 2 `3d87880` (outbox.ts), Task 3 `2867b75` (dispatch route). Ran concurrently with 05-07 (alert CRUD Server Actions) in the same session on disjoint files — all three commits verified atomic via `git show HEAD --stat`, no cross-contamination. ALRT-03/ALRT-05 were already marked Complete in REQUIREMENTS.md by 05-01's plan-level marking; this plan supplies the actual delivery-mechanism proof underpinning those marks. Live sends, live 429/403 observation, and live claim-concurrency behavior remain explicitly DEFERRED to 05-09 (no real bot token yet, 05-01's migration not yet pushed). Next: 05-05 (evaluate-and-enqueue sweep) can now import `enqueueNotifications` directly and compose it with `evaluateAlerts`/`computeAlertDedupeKey` (05-03) + `buildPriceAlertMessage` (05-02); 05-06 (handshake Server Actions) can import `getTelegramUpdates`/`sendTelegramMessage` directly — check each PLAN.md's `depends_on` field before starting.
Previously stopped at: 05-02 (pure Telegram logic layer) completed and SUMMARY-complete. `src/lib/telegram/types.ts` — declarations-only shared vocabulary (`SendErrorKind`, `SendErrorClassification`, `AlertDirection`, `PriceAlertMessageInput`). `src/lib/telegram/parse-start-payload.ts` — `parseStartPayload(text)` requiring text to start with `/start ` followed by a single non-empty `[A-Za-z0-9_-]{1,64}` run, else `null` (bare `/start`, non-start text, whitespace-containing or out-of-charset payloads all rejected). `src/lib/telegram/link-token.ts` — `generateLinkToken()` = `crypto.randomBytes(32).toString('base64url')` (43 chars, exactly Telegram's deep-link charset), `isValidLinkTokenShape(t)` = `/^[A-Za-z0-9_-]{1,64}$/`. `src/lib/telegram/build-message.ts` — `escapeHtml(s)` escapes only `&`/`<`/`>` (in that order), `buildPriceAlertMessage(input)` composes an HTML `parse_mode` string (`<b>`/`<code>` tags, only `displaySymbol` escaped) truncated to Telegram's 4096-char sendMessage limit — deliberately HTML mode only, never the legacy escape-heavy markdown mode (05-RESEARCH-telegram-api Q4/Pitfall 1). `src/lib/telegram/classify-send-error.ts` — `classifySendError(errorCode, description, retryAfterSeconds?)` implementing the full 429(retryable+retry_after)/403(permanent)/400(permanent, any cause)/5xx-or-null(retryable) taxonomy, keyed on `error_code` first with `.toLowerCase().includes(...)` description matching (never exact equality, per Telegram's own "subject to change" caveat). This plan is Phase 5's sole wave-1 `package.json` owner — registered both `test:telegram` and `test:alerts` npm scripts. `scripts/telegram-logic-test.ts` — 18 assertions under `node:assert/strict`, `npm run test:telegram` → PASS; `npx tsc --noEmit` clean project-wide. Committed as 2 TDD task pairs (RED+GREEN each, no REFACTOR needed): Task 1 `67d1009`/`dddd916` (parse-start-payload/link-token/build-message), Task 2 `92cacf0`/`2566bfc` (classify-send-error). A parallel-executor git-index race briefly swept this plan's first-commit staged `package.json`/`scripts/telegram-logic-test.ts` into 05-03's own RED commit (`6796d5c`); 05-03's executor self-corrected via `git reset` before this plan re-staged/re-verified content and re-committed with explicit pathspec-restricted commits — `git show HEAD --stat` confirmed after every one of this plan's 4 commits that exactly the intended files landed. ALRT-01/ALRT-03 were already marked Complete in REQUIREMENTS.md by 05-01's plan-level marking; this plan supplies the actual pure-logic proof underpinning those marks. Ran concurrently with 05-01 (schema) and 05-03 (alert evaluation pure logic) in the same session on disjoint files. Next: 05-04 (outbox engine) can now import `classifySendError` and `buildPriceAlertMessage` directly; 05-06 (handshake Server Actions) can import `parseStartPayload`/`generateLinkToken`/`isValidLinkTokenShape` directly — check each PLAN.md's `depends_on` field before starting.
Previously stopped at: 05-03 (pure alert evaluation core) completed and SUMMARY-complete. `src/lib/alerts/types.ts` — shared vocabulary (`AlertDirection`, `AlertEvalRow`, `PriceSnapshot`, `TriggeredAlert`), declarations-only. `src/lib/alerts/evaluate.ts` — `evaluateAlerts(alerts, pricesByInstrument, now)` implementing the level+cooldown trigger rule (skips inactive alerts, missing/null price-map entries, `fetchError`-flagged snapshots, applies strict direction comparison — exactly-equal never fires — then gates on cooldown), `isCooldownElapsed` (exported, directly unit-tested), and `computeAlertDedupeKey(alert, now)` producing `price_alert:{id}:{floor(epoch_seconds/(cooldownMinutes*60))}` — the ALRT-05 idempotency-backstop cooldown-window bucket key, identical within a window (crash-recovery dedupe) and distinct across windows/alert ids. Zero I/O, zero DB, no internal `Date.now()` — mirrors `src/lib/prices/ingest.ts`'s shape exactly. `scripts/alerts-eval-test.ts` — 15 case groups under `node:assert/strict`, `npx tsx scripts/alerts-eval-test.ts` → PASS; `npx tsc --noEmit` clean on this plan's own files (a transient error in the concurrently-running 05-02 executor's in-flight `scripts/telegram-logic-test.ts` confirmed out of scope via grep). Committed as 2 TDD task pairs (RED+GREEN each, no REFACTOR needed): Task 1 `4d5ce8d`/`38eac34` (types+evaluateAlerts), Task 2 `6f13857`/`d1d7080` (computeAlertDedupeKey). A parallel-executor git-index race (first commit silently swept in a concurrent executor's `package.json`/`scripts/telegram-logic-test.ts` changes despite explicit pathspec staging) was caught immediately via `git show HEAD --stat` and corrected with `git reset --soft HEAD~1` + selective unstage + re-commit — zero foreign content landed in this plan's final commits. `package.json` correctly left untouched (05-02 owns `test:alerts` registration). ALRT-02/ALRT-03 marked Complete in REQUIREMENTS.md in the code-complete/static-verified sense. Ran concurrently with 05-01 (schema) and 05-02 (Telegram pure logic) in the same session on disjoint files. Next: 05-05 (evaluate-and-enqueue sweep) can now import `evaluateAlerts`/`computeAlertDedupeKey` directly and compose them with 05-01's schema; 05-02 continues/completes on its own disjoint files; 05-04 (outbox engine) is next in wave order per ROADMAP.md — check each PLAN.md's `depends_on` field before starting.
Previously stopped at: 05-01 (Phase 5 schema plan) completed and SUMMARY-complete. `supabase/migrations/20260716221450_alerts_telegram.sql` — the single new migration: drops the empty legacy Phase-1 `alerts` table; creates `price_alerts` (account-ownership RLS, four EXISTS-subquery policies copied verbatim from transactions/import_batches, one-per-(account,instrument,direction) unique constraint, cooldown_minutes default 1440/floor 60); creates `telegram_links` (user-keyed handshake table, BIGINT chat_id, NO UPDATE policy for anyone — that closure structurally IS the ALRT-01 allowlist, only the service role can ever set chat_id/status='linked'); creates `notifications_outbox` (generic kind/payload/dedupe_key, all three roadmapped kinds enumerated now, zero authenticated write policies — same closed posture as price_cache/fx_cache, partial unique dedupe index + due-pending index); creates `claim_due_notifications` (SECURITY DEFINER, FOR UPDATE SKIP LOCKED claim with exponential backoff, dead-letters rows at 8 failed attempts, revoked from anon/authenticated). `scripts/rls-isolation-test.ts` extended (not rewritten) with three new coverage blocks, introducing a new "structural closure" assertion shape alongside the existing ownership-CRUD shape: telegram_links UPDATE must affect zero rows for both a stranger and the owner. `npx tsc --noEmit` clean for this plan's files (one unrelated pre-existing error in the parallel 05-03 executor's in-flight `scripts/alerts-eval-test.ts` confirmed out of scope). `npm run test:rls` re-run and honest-FAILs at `import_batches not found` (Phase 4's migration also still unapplied live) — confirms live apply/live RLS run are genuinely DEFERRED to 05-09, not fabricated as passing. Committed as three atomic task commits (fe08893 feat price_alerts, ffc7c9b feat telegram_links+notifications_outbox+claim function, a8dd620 test RLS extension). ALRT-01/02/03/05 marked Complete in REQUIREMENTS.md in the code-complete/static-verified sense. Ran concurrently with 05-02 (Telegram pure logic) and 05-03 (alert evaluation pure logic) in the same session on disjoint files. Next: 05-02 and 05-03 (both wave-1, disjoint-file parallel plans) continue or complete; later plans (05-04 outbox engine, 05-05 evaluate-and-enqueue sweep, 05-06 handshake Server Actions, 05-07 alert CRUD Server Actions, 05-08 /alerts UI rewrite) can build directly against this migration's exact table/column/function shapes — check each PLAN.md's `depends_on` field before starting. 05-09 is the phase-closing live checkpoint (migration push with consent, real BotFather bot, real handshake + trigger + cooldown + retry verification).
Previously stopped at: 04-07 (live-verification checkpoint) reached and closed as DEFERRED at user direction — user has no real Groww/Robinhood export files yet (will upload later) and did not consent to pushing `20260715230011_csv_import.sql` (no `supabase login`/`link` run). Pre-checkpoint honest-state checks: `npx tsc --noEmit` clean, `npm run test:import-parse` PASS on synthetic fixtures, `npm run test:rls` honest FAIL (`public.import_batches` not in schema cache — migration genuinely unapplied), `npx supabase migration list`/`projects list` confirm not-linked/not-authenticated. 04-07-SUMMARY.md records the deferral, the exact deferred list, and the resume path (matching 03-06's precedent). Phase 4 is now 7/7 plans closed — all code-complete/static-verified, live verification outstanding (see STILL OPEN item 6). Next: Phase 5 planning can proceed, OR re-run /gsd:execute-phase 4 / 04-07-PLAN.md the moment the user supplies real broker exports + migration-push consent.
Previously stopped at: 04-06 (/import progressive page shell + container state machine + Holdings entry point) completed and SUMMARY-complete. `src/app/(dashboard)/import/page.tsx` — an auth-guarded Server Component shell (same `createClient`→`auth.getUser`→`if (!user) return null` pattern as `holdings/page.tsx`) rendering a titled glass-card header and `<ImportPage />`. `src/components/import/ImportPage.tsx` — the `'use client'` container that owns the entire progressive state machine (`idle`→`previewing`→`preview`→`committing`→`done`) and is the ONLY place the import UI calls `previewImport`/`commitImport`: the same `File` is re-sent on both calls, plus `JSON.stringify(choices)` on commit, so the server always re-parses and the client never ships parsed row data. Commit is gated by computing `unresolvedSymbols` (`preview.unmatchedSymbols` minus symbols already present in `choices.mappings`) and disabling the button while any remain — since none of the 04-05 leaf components expose a per-symbol skip, full resolution is required by construction (IMPT-04). A `growwIsinBySymbol` map is derived via `useMemo` from unmatched Groww rows' real ISINs (never fabricated for Robinhood). An optional manual broker-override retry (Groww/Robinhood buttons) re-calls `previewImport` with an explicit `broker` field when detection fails. `phase==='done'` renders `<ImportSummary>` in place — no `router.push`, no auto-redirect. `src/app/(dashboard)/holdings/page.tsx` gained one `<Link href="/import">` "Import" button in the header actions cluster (next to Refresh now/Add Asset) — confirmed via `git diff` to be the ONLY change to that file. `npx tsc --noEmit` and `npm run build` both clean (`/import` listed as a dynamic route), every plan-specified grep check passes. Committed as three atomic task commits (31f9f92 feat /import shell, f025ce1 feat ImportPage container, d21a675 feat Holdings header Import link). Functional/E2E verification (click-through: select file→preview→resolve→commit→summary→Holdings shows imported holdings with live P&L→idempotent re-import) remains explicitly DEFERRED to 04-07's checkpoint — no live DB/migration apply has occurred. Next: 04-07 is the final plan in Phase 4 — the live-verify checkpoint (real-file fixtures + end-to-end import verification, including finally pushing 04-01's migration to the live hosted DB with explicit user consent, matching the 03-01→03-06 pattern).
Previously stopped at: 04-05 (import preview UI leaf components) completed and SUMMARY-complete. `src/components/import/{ImportDropzone,ImportSummary,PreviewTable,SymbolMappingSection}.tsx` — the four leaf presentational client components the import page composes: dropzone (drag-drop/click, .xlsx/.csv, 4MB client-side size guard mirroring next.config.ts's bodySizeLimit), PreviewTable (per-category chips, bulk-only importDuplicates toggle bound to CommitChoices — no per-row checkboxes, the locked IMPT-03 decision — and compact rows that expand on click to full rawFields/normalized/instrument/statusReason detail), SymbolMappingSection (IMPT-04: one entry per unique unmatched broker symbol, resolved via reused debounced searchInstrumentsAction search or a create-new-instrument form that prefills Groww's ISIN and requires — never fabricates — Robinhood's, gated by client-side ISIN shape validation), and ImportSummary (explicit imported/skipped/duplicate audit-trail counts, Holdings link with no auto-redirect, one-time fire-and-forget refreshPricesNow() call). All four files pre-existed as uncommitted drafts from a prior session (two staged, two untracked); this execution's value-add was verifying each against every plan `must_haves` truth/artifact/key_link AND against the real `src/server-actions/import.ts` code — specifically confirming via grep that `commitImport` unconditionally excludes `unsupported` rows regardless of `choices.importUnsupported`, validating PreviewTable's design choice to omit an unsupported toggle — before committing as three atomic task commits (4d65fac feat ImportDropzone+ImportSummary, df113ae feat PreviewTable, 9fd4e80 feat SymbolMappingSection). `npx tsc --noEmit` and `npm run build` both clean, every plan-specified grep check passes. IMPT-03/IMPT-04 marked Complete in REQUIREMENTS.md. Visual/functional verification (rows expanding, toggles changing commit behavior, live search, price-refresh firing) remains DEFERRED to 04-07's human-verify checkpoint. Next: 04-06 (import page container/state machine) can now import all four components directly and wire them to previewImport/commitImport — check 04-06-PLAN.md's depends_on field before starting.
Previously stopped at (older): 04-04 (previewImport/commitImport Server Actions + next.config.ts body-size limit) completed and SUMMARY-complete. `src/server-actions/import.ts` created with `previewImport` (re-derives file bytes/hash server-side, detects/accepts an override broker, parses via the pure lib, loads instrument universe + saved mappings + dedup inputs in parallel, returns a full classified ImportPreview, writes nothing) and `commitImport` (re-parses the same file, resolves user mapping/create choices via the find_or_create_instrument RPC only, re-runs matching + dedup, writes atomically with a compensating batch delete on insert failure, persists resolved mappings non-fatally, revalidates `/` and `/holdings`). Neither action imports the admin client; both share a `requireAuthedContext` helper copied from `src/server-actions/portfolio.ts`'s pattern. `next.config.ts` raises `experimental.serverActions.bodySizeLimit` to `'4mb'`. `npx tsc --noEmit` clean, every plan-specified grep check passes. Committed as three atomic task commits (79e3ae4 chore, 37e65ed feat previewImport, c39c74b feat commitImport). IMPT-01 through IMPT-05 marked Complete in REQUIREMENTS.md (code-complete/static-verified sense). Live write behavior (compensating delete firing, partial unique index rejecting a forced duplicate re-commit, find_or_create_instrument creating a real instrument) is explicitly DEFERRED to 04-07 — 04-01's migration is still not pushed to the live DB.
Previously stopped at (older): 04-03 (broker parsers + instrument matching + duplicate detection) completed and SUMMARY-complete. `src/lib/import/{parse-groww,parse-robinhood,match-instruments,detect-duplicates}.ts` + `scripts/import-parse-test.ts` + synthetic fixtures (`scripts/fixtures/groww-holdings-sample.xlsx`, `robinhood-activity-sample.csv`) — full TDD cycle (chore fixtures -> RED stubs+failing test -> GREEN real implementation; no REFACTOR needed), all 7 case groups passing, `npx tsc --noEmit` clean, re-run twice for identical output (purity proven). Committed as three atomic commits (f7cb477 chore, 8286d06 test, b20f619 feat). IMPT-01/IMPT-02/IMPT-04/IMPT-05 re-confirmed Complete in REQUIREMENTS.md; IMPT-03 deliberately left Pending (this plan supplies the validation/dedup machinery but the actual preview flow is 04-04 through 04-06's scope). Next: 04-04 (previewImport/commitImport Server Actions) can now build directly on this pure pipeline plus 04-01's schema — check 04-04-PLAN.md's depends_on field before starting. Earlier this session: 04-02 (import parsing primitives) completed and SUMMARY-complete, executed concurrently with 04-01 in this same session on disjoint files. `src/lib/import/{types,normalize,row-hash,detect-broker}.ts` + `scripts/import-primitives-test.ts` — full TDD cycle (Setup install -> RED -> GREEN -> REFACTOR), all 6 case groups passing, `npx tsc --noEmit` clean, re-run twice for identical output (purity proven). `xlsx` pinned to SheetJS CDN tarball 0.20.3, `papaparse`/`@types/papaparse` installed, both import npm scripts registered. Committed as four atomic commits (10734e7 chore, 06a2749 test, 909e8f3 feat, 1f8c55a refactor). IMPT-01/IMPT-02/IMPT-05 marked Complete in REQUIREMENTS.md in the code-complete/static-verified sense (this plan has zero DB/network dependency by design). Also this session: 04-01 (CSV import storage + controlled-write foundation) completed and SUMMARY-complete. One new migration (`supabase/migrations/20260715230011_csv_import.sql`): import_batches + symbol_mappings tables (account-ownership RLS), transactions.import_batch_id/import_row_hash + partial idempotency index, find_or_create_instrument SECURITY DEFINER RPC. scripts/rls-isolation-test.ts extended for the two new tables. All statically verified (tsc clean, migration greps pass, git diff --stat confirms one new file / zero existing migrations touched); live apply + live test:rls deferred to 04-07. IMPT-04/IMPT-05 marked Complete in REQUIREMENTS.md. Committed as three atomic task commits (2d2321a, 5d27e40, e192909). Next: continue Phase 4 with the next unblocked plan per ROADMAP.md's wave ordering (check 04-*-PLAN.md depends_on fields — 04-03's parser plan can now build on both 04-01's schema and 04-02's primitives). Earlier this session: Phase 4 context gathered via /gsd:discuss-phase (04-CONTEXT.md), then /gsd:plan-phase 4 produced all 7 plans across 6 waves (0163a17). Previous session (2026-07-14): Completed 03-05 (Dashboard/Holdings pricing UI). `getPortfolioPnL` (src/lib/prices/get-portfolio-pnl.ts) combines Phase 2's getHoldings with price_cache/fx_cache reads and 03-02's pure calculateHoldingPnL/calculatePortfolioTotals — the sole place a Supabase client meets the P&L math, read-only, cookie-bound client. Dashboard and Holdings pages render real price/day-change/total-change gated on status==='priced' (Phase 2's em-dash pending path preserved, now correctly conditional), a shared StalenessBadge (fresh/stale/very-stale/error/pending) per row, a corporate-action warning pill that never hides the row, and an FX-breakdown-visible portfolio total that excludes non-base holdings from the aggregate (rather than fabricating a 1:1 conversion) when no FX rate is cached. RefreshPricesButton client island wired into holdings/page.tsx calls refreshPricesNow (03-04); relies on that action's existing revalidatePath, confirmed against this Next.js version's docs and Phase 2's own server-actions/portfolio.ts precedent — no manual router.refresh() needed. `npx tsc --noEmit` clean, `npm run test:price-pnl` (12/12) and `npm run test:derive-holdings` (7/7) both still pass unchanged. No live Supabase call was made — 03-01's two migrations remain unpushed, so none of this UI has rendered against real cached data yet. PRICE-03 and PRICE-04 intentionally left Pending in REQUIREMENTS.md (code-complete, live-verification deferred to 03-06). Previously: 03-04 (refreshAllPrices orchestration + secret-guarded POST /api/prices/refresh route + refreshPricesNow Server Action), 03-03 (fetchPrices + fetchFXRate network wrappers, surfaced the exchangerate.host missing_access_key issue — see Blockers/Concerns), 03-01 (fx_cache + price_cache re-key + pg_cron scheduling migrations, authored/statically verified, live push still deferred), 03-02 (price ingestion + P&L pure logic layer, fully TDD'd and passing).
Resume file: .planning/phases/04-csv-import/04-CONTEXT.md
