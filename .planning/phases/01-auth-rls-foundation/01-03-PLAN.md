---
phase: 01-auth-rls-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/proxy.ts
  - src/middleware.ts
  - src/utils/supabase/middleware.ts
  - src/app/login/page.tsx
  - src/app/(dashboard)/layout.tsx
  - src/app/auth/signout/route.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "User can sign up and log in with email/password via Supabase Auth"
    - "The demo abc@g.com credentials, sign-up-disabled block, and foliointel-session cookie no longer exist anywhere"
    - "A refreshed browser stays logged in only when getUser() validates the session server-side"
    - "User can log out from any page, clearing the Supabase session"
  artifacts:
    - path: "src/proxy.ts"
      provides: "Next.js 16 proxy that refreshes session and revalidates via getUser()"
      contains: "getUser"
    - path: "src/app/login/page.tsx"
      provides: "Real Supabase email/password sign-in + sign-up"
      contains: "signInWithPassword"
    - path: "src/app/auth/signout/route.ts"
      provides: "Server-side signOut endpoint"
      contains: "signOut"
  key_links:
    - from: "src/proxy.ts"
      to: "supabase.auth.getUser()"
      via: "server-side revalidation immediately after createServerClient"
      pattern: "auth\\.getUser\\(\\)"
    - from: "src/app/login/page.tsx"
      to: "supabase.auth.signInWithPassword / signUp"
      via: "browser client from @/utils/supabase/client"
      pattern: "signInWithPassword|signUp"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "supabase.auth.signOut / getUser"
      via: "real email source + real logout, no cookie parsing"
      pattern: "signOut"
---

<objective>
Replace the entire mock auth path with real Supabase Auth: migrate the deprecated `middleware` convention to Next.js 16 `proxy` with server-side `getUser()` validation (AUTH-02), rewrite the login page to real email/password sign-in + sign-up and delete the demo credentials/cookie (AUTH-01), and source the dashboard user email from Supabase plus wire real logout (AUTH-03).

Purpose: Success criteria 1 and 2 — real sign up / log in / log out with the demo cookie deleted, and a session that survives refresh only when validated server-side (a forged cookie is rejected). Almost all of this is DELETING hand-rolled auth and ACTIVATING the existing `@supabase/ssr` scaffolding.
Output: `src/proxy.ts` (replacing `src/middleware.ts` + `src/utils/supabase/middleware.ts`), rewritten `login/page.tsx`, updated dashboard `layout.tsx`, new `auth/signout/route.ts`.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-auth-rls-foundation/01-RESEARCH.md
@AGENTS.md
@src/middleware.ts
@src/utils/supabase/middleware.ts
@src/utils/supabase/server.ts
@src/utils/supabase/client.ts
@src/app/login/page.tsx
@src/app/(dashboard)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate middleware to proxy with real getUser() validation (AUTH-02)</name>
  <files>src/proxy.ts, src/middleware.ts, src/utils/supabase/middleware.ts</files>
  <action>
    FIRST read `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md` (AGENTS.md requires reading the installed Next.js 16 docs before writing Next code — the `middleware` file convention is deprecated and renamed to `proxy` in v16).
    Create `src/proxy.ts` per research Code Examples ("Proxy session refresh"):
      - Export an async function `proxy(request: NextRequest)`.
      - Build a `createServerClient` (from `@supabase/ssr`) with anon key and the `getAll`/`setAll` cookie wiring against the request/response.
      - Immediately call `const { data: { user } } = await supabase.auth.getUser()` — do NOT run other code between client creation and `getUser()`. This is the network-revalidated check that replaces the current cookie-presence trust (AUTH-02).
      - Redirect logic: treat `/login` and `/auth` as public; if `!user` and not public → redirect to `/login`; if `user` and on `/login` → redirect to `/`.
      - Keep the existing matcher config (copy from `src/middleware.ts`).
    DELETE the mock cookie logic: there must be NO read of `foliointel-session` anywhere. Then delete `src/middleware.ts` and delete `src/utils/supabase/middleware.ts` (its `updateSession` mock-cookie reader is fully replaced by `proxy.ts`). You may run `npx @next/codemod@canary middleware-to-proxy .` to assist the rename, but the session logic must be rewritten to call `getUser()` as above, not preserved.
    Confirm no remaining imports point at the deleted `@/utils/supabase/middleware`.
  </action>
  <verify>`test -f src/proxy.ts && ! test -f src/middleware.ts && ! test -f src/utils/supabase/middleware.ts`; `grep -rn "foliointel-session" src/` returns nothing; `grep -n "auth.getUser" src/proxy.ts` matches; `npx next build` (or `npx tsc --noEmit`) emits no middleware deprecation warning and no unresolved import of the deleted module.</verify>
  <done>`src/proxy.ts` validates the session via `getUser()`; the old middleware files and every `foliointel-session` reference are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the login page with real Supabase email/password auth (AUTH-01)</name>
  <files>src/app/login/page.tsx</files>
  <action>
    Rewrite `src/app/login/page.tsx` to use real Supabase Auth via the existing browser client (`createClient` from `@/utils/supabase/client`). Keep the existing visual shell (glass card, inputs, show/hide password, sign-in/sign-up toggle) but replace the fake logic:
      - DELETE the hardcoded initial state `useState('abc@g.com')` / `useState('asdfg')` → default both to empty strings.
      - DELETE the demo `placeholder="abc@g.com"` (line ~106) and `placeholder="asdfg"` (line ~125) attributes on the email/password inputs → replace with neutral placeholders (e.g. `placeholder="you@example.com"` for email and `placeholder="Your password"` for password). No `abc@g.com` or `asdfg` string may remain in the file.
      - DELETE the "Local Demo Account Details" banner block entirely.
      - DELETE the `if (email === 'abc@g.com' && password === 'asdfg')` branch and the `document.cookie = 'foliointel-session=...'` write.
      - DELETE the artificial `setTimeout(600)` lag and the "Sign up is disabled" block.
      - On submit, when signing in: `await supabase.auth.signInWithPassword({ email, password })`; on error show `error.message`; on success `router.replace('/')` then `router.refresh()` (the proxy will now see a real validated session).
      - When signing up: `await supabase.auth.signUp({ email, password })`; since email confirmation is disabled locally (plan 01), on success either the session is active (redirect to `/`) or show a success message to sign in. Enable the sign-up path (it is currently disabled).
      - Keep the `?error=` query-param display for callback errors.
    No `foliointel-session` cookie may be written anywhere.
  </action>
  <verify>`grep -n "abc@g.com\|asdfg\|foliointel-session\|Demo Account" src/app/login/page.tsx` returns nothing; `grep -n "signInWithPassword" src/app/login/page.tsx` and `grep -n "signUp" src/app/login/page.tsx` match; `npx tsc --noEmit` passes for this file.</verify>
  <done>Login page performs real Supabase `signInWithPassword` and `signUp`; all demo credentials, placeholders, banner, and cookie writes are deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Source dashboard email from Supabase and wire real logout (AUTH-03)</name>
  <files>src/app/(dashboard)/layout.tsx, src/app/auth/signout/route.ts</files>
  <action>
    In `src/app/(dashboard)/layout.tsx`:
      - DELETE the `useEffect` cookie-parsing block (lines ~86-101) that regex-matches `foliointel-session` and falls back to `abc@g.com`. Replace it with reading the real user: call `supabase.auth.getUser()` (using the existing `createClient` browser client already imported) in a `useEffect`, and set `userEmail` from `data.user?.email ?? null`. No `abc@g.com` fallback — if there is no user, the proxy will already have redirected; showing null/blank is acceptable.
      - Rewrite `handleLogout` to call `await supabase.auth.signOut()` (browser client) instead of deleting the `foliointel-session` cookie, then `router.replace('/login')` and `router.refresh()`. This is invoked from every logout affordance (sidebar, mobile drawer, profile dropdown) which already call `handleLogout`, so logout works from any page (AUTH-03).
    Create `src/app/auth/signout/route.ts` as a server-side POST handler that calls the server client's `supabase.auth.signOut()` and redirects to `/login` — this gives a robust server-side signout path (research file layout lists `auth/signout/route.ts` as the AUTH-03 endpoint). Use `createClient` from `@/utils/supabase/server`.
    No `foliointel-session` or `abc@g.com` reference may remain in the layout.
  </action>
  <verify>`grep -n "foliointel-session\|abc@g.com" src/app/(dashboard)/layout.tsx` returns nothing; `grep -n "signOut" src/app/(dashboard)/layout.tsx` matches; `test -f src/app/auth/signout/route.ts` and `grep -n "signOut" src/app/auth/signout/route.ts` matches; `npx tsc --noEmit` passes.</verify>
  <done>Dashboard email comes from `supabase.auth.getUser()`, logout calls `signOut()`, a server signout route exists, and no mock cookie/email fallback remains.</done>
</task>

</tasks>

<verification>
- `grep -rn "foliointel-session" src/` and `grep -rn "abc@g.com" src/` both return nothing (mock auth fully deleted).
- `src/middleware.ts` and `src/utils/supabase/middleware.ts` no longer exist; `src/proxy.ts` calls `getUser()`.
- `npx next build` completes without a middleware→proxy deprecation warning.
- Login uses `signInWithPassword`/`signUp`; logout uses `signOut()`.
</verification>

<success_criteria>
Real email/password auth end-to-end: sign up, log in, and log out all use Supabase Auth; the demo credential/cookie login is deleted; the session is validated server-side in `proxy.ts` via `getUser()` so a forged cookie is rejected on refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-rls-foundation/01-03-SUMMARY.md`.
</output>
