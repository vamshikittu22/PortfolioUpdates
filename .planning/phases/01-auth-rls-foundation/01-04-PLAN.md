---
phase: 01-auth-rls-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/app/api/settings/keys/route.ts
autonomous: false
requirements: [AUTH-06]

must_haves:
  truths:
    - "/api/settings/keys returns 401 for an unauthenticated request on both GET and POST"
    - "An authenticated user still gets the existing provider-flags response and can save keys"
    - "The full auth flow works in a browser: sign up, log in, refresh stays logged in, forged cookie is rejected, log out"
  artifacts:
    - path: "src/app/api/settings/keys/route.ts"
      provides: "getUser() 401 gate on GET and POST"
      contains: "getUser"
  key_links:
    - from: "src/app/api/settings/keys/route.ts"
      to: "supabase.auth.getUser()"
      via: "requireUser() guard returning 401 when no user"
      pattern: "getUser\\(\\)"
---

<objective>
Close the last open security hole by gating the unauthenticated `/api/settings/keys` endpoint behind `getUser()` (AUTH-06), then run a single end-to-end human verification of the whole Phase 1 auth surface (sign up, log in, refresh, forged-cookie rejection, log out, settings 401).

Purpose: Success criterion 5 ("`/api/settings/keys` rejects unauthenticated requests") plus a consolidated human check that criteria 1 and 2 (interactive login/logout, forged-cookie rejection) actually hold in a browser. This runs last because the human-verify step exercises the real auth flow built in plan 03 and asserts the plan 02 RLS isolation test has already passed.
Output: A gated `src/app/api/settings/keys/route.ts` and a verified working auth flow.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-auth-rls-foundation/01-RESEARCH.md
@src/app/api/settings/keys/route.ts
@src/utils/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate /api/settings/keys behind getUser() (AUTH-06)</name>
  <files>src/app/api/settings/keys/route.ts</files>
  <action>
    Add an auth gate to both handlers per research Code Examples (AUTH-06). At the top of the file add a helper:
      `async function requireUser() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); return user; }`
      using `createClient` from `@/utils/supabase/server`.
    In `GET()`: `if (!(await requireUser())) return new Response(null, { status: 401 });` before returning the provider-flags JSON.
    In `POST(request)`: `if (!(await requireUser())) return new Response(null, { status: 401 });` before the existing key-write logic.
    Do NOT rewrite the existing fs-based `.env.local` write logic — that filesystem/global-keys redesign is explicitly deferred debt (research Open Questions #1). Add a brief `// DEFERRED (Phase 1+): keys are global + written to .env.local via fs; move to per-user encrypted storage / Supabase Vault. This phase only gates access.` comment so the debt is visible but not fixed now.
  </action>
  <verify>`grep -n "getUser" src/app/api/settings/keys/route.ts` matches in both handler paths; `grep -n "401" src/app/api/settings/keys/route.ts` matches; `npx tsc --noEmit` passes. Manual: `curl -i -X POST http://127.0.0.1:3000/api/settings/keys -H 'Content-Type: application/json' -d '{"provider":"gemini","key":"x"}'` with no auth cookie returns HTTP 401.</verify>
  <done>Both GET and POST return 401 when unauthenticated; authenticated requests behave as before; deferred-debt note added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end human verification of the Phase 1 auth flow</name>
  <action>
    This is a human-verify checkpoint (no code changes). Pause and ask the user to run the six-step verification below against the running app, then resume on their signal. Before pausing, confirm the local Supabase stack is running and start `npm run dev` so the user can test immediately.
  </action>
  <what-built>
    Full Phase 1 auth surface: real Supabase email/password sign-up/login (plan 03), server-side session validation in proxy.ts via getUser() (plan 03), logout from any page (plan 03), and the newly gated /api/settings/keys endpoint (this plan). The two-user RLS isolation test and shared-table (price_cache/news_items) write-hole test (plan 02) have already passed automatically.
  </what-built>
  <how-to-verify>
    Ensure the local Supabase stack is running (`npx supabase status`) and the app is up (`npm run dev`).
    1. Sign up: visit http://127.0.0.1:3000/login, create a new account with a fresh email + password. Expect to land on the dashboard (email confirmation is disabled locally).
    2. Log out: use the sidebar/profile "Sign Out". Expect redirect to /login.
    3. Log in: sign back in with the same credentials. Expect the dashboard, and the profile dropdown should show your real email (not abc@g.com).
    4. Refresh persistence: reload the dashboard — you stay logged in.
    5. Forged-cookie rejection: in DevTools > Application > Cookies, edit one of the `sb-...-auth-token` cookie values to garbage and reload. Expect to be bounced to /login (getUser() rejects the invalid token) — NOT to stay logged in.
    6. Settings gate: in a private window with no session, run `curl -i http://127.0.0.1:3000/api/settings/keys` — expect HTTP 401.
    Confirm nowhere in the UI does the demo `abc@g.com` account or a "Demo Account Details" banner appear.
  </how-to-verify>
  <verify>All six steps behave as described; unauthenticated `/api/settings/keys` returns 401; no demo account/banner visible.</verify>
  <done>User confirms the full auth flow works and the settings endpoint is gated.</done>
  <resume-signal>Type "approved" if all six checks pass, or describe which step failed.</resume-signal>
</task>

</tasks>

<verification>
- Unauthenticated GET and POST to `/api/settings/keys` → 401.
- Browser flow: sign up → dashboard, logout → /login, login → dashboard with real email, refresh stays in, forged cookie bounces to /login.
- No `abc@g.com` / demo banner anywhere in the running app.
</verification>

<success_criteria>
`/api/settings/keys` rejects unauthenticated requests, and the complete real-auth flow (sign up, login, server-validated refresh, forged-cookie rejection, logout) is human-verified working.
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-rls-foundation/01-04-SUMMARY.md`.
</output>
