---
phase: 01-auth-rls-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/utils/supabase/admin.ts
  - scripts/rls-isolation-test.ts
  - package.json
autonomous: true
requirements: [AUTH-04, AUTH-05]

must_haves:
  truths:
    - "A dedicated service-role admin client exists that bypasses RLS and is never importable into the browser"
    - "The service-role client is built from @supabase/supabase-js (not @supabase/ssr) so a user cookie can never override it"
    - "A second user cannot READ the first user's holdings rows"
    - "A second user cannot WRITE into the first user's account"
  artifacts:
    - path: "src/utils/supabase/admin.ts"
      provides: "Service-role client factory, server-only, persistSession:false"
      contains: "server-only"
    - path: "scripts/rls-isolation-test.ts"
      provides: "Two-client, two-JWT isolation proof run against the local anon key"
      min_lines: 20
    - path: "package.json"
      provides: "test:rls script + tsx dev dependency"
      contains: "test:rls"
  key_links:
    - from: "src/utils/supabase/admin.ts"
      to: "@supabase/supabase-js createClient"
      via: "createClient with service-role key + persistSession:false + import 'server-only'"
      pattern: "import 'server-only'"
    - from: "scripts/rls-isolation-test.ts"
      to: "public.holdings via anon key + two JWT sessions"
      via: "cross-user read returns 0 rows and cross-user insert errors"
      pattern: "RLS (read|write) leak"
---

<objective>
Create the dedicated service-role admin client (AUTH-05) and prove per-user isolation with a runnable two-user Node script (AUTH-04). This is the key verification artifact for the phase — the codebase has zero tests, and RLS failure is invisible in single-user dev.

Purpose: Success criterion 3 ("a second user cannot read or write the first user's rows, proven by a passing two-user isolation test") and criterion 4 ("scheduled/admin operations run through a dedicated service-role client the browser never receives") are proven here against the running local stack from plan 01.
Output: `src/utils/supabase/admin.ts`, `scripts/rls-isolation-test.ts`, and a `test:rls` npm script that passes.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-auth-rls-foundation/01-RESEARCH.md
@supabase/schema.sql
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the server-only service-role admin client</name>
  <files>src/utils/supabase/admin.ts</files>
  <action>
    Create `src/utils/supabase/admin.ts` exactly per research Code Examples (AUTH-05). Critical constraints — do NOT deviate:
      - Import `createClient` from `@supabase/supabase-js` (NOT `createServerClient` from `@supabase/ssr`). Using the SSR client here silently downgrades to the user's cookie session (research Pitfall #2 / #5, the SSR/service-role trap).
      - First line: `import 'server-only'` so any accidental client-side import fails the build.
      - Use `process.env.NEXT_PUBLIC_SUPABASE_URL!` and `process.env.SUPABASE_SERVICE_ROLE_KEY!` (the server-only key wired in plan 01).
      - Pass `{ auth: { autoRefreshToken: false, persistSession: false } }`.
      - Export a factory `createAdminClient()`.
    Add a comment noting this client BYPASSES RLS and must only be used by cron/admin server code (future price/news jobs), never to "make a user query work."
  </action>
  <verify>`grep -n "server-only" src/utils/supabase/admin.ts` and `grep -n "@supabase/supabase-js" src/utils/supabase/admin.ts` both match; `grep -n "persistSession: false" src/utils/supabase/admin.ts` matches. Confirm `server-only` is installed or add it: `npm ls server-only` — if absent, `npm install server-only`.</verify>
  <done>`createAdminClient()` is exported from a `server-only` module built on `@supabase/supabase-js` with `persistSession:false`.</done>
</task>

<task type="auto">
  <name>Task 2: Write and run the two-user RLS isolation test</name>
  <files>scripts/rls-isolation-test.ts, package.json</files>
  <action>
    Create `scripts/rls-isolation-test.ts` per research Code Examples (AUTH-04 test). The script uses the ANON key only (never the service role) and the local stack:
      - Read `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` from env.
      - Helper `signedIn(email)`: `createClient(URL, ANON)`, `signUp` (ignore "already exists"), then `signInWithPassword` with a fixed strong password. Returns the authed client.
      - Create user A and user B with unique timestamped emails.
      - As A: select A's default `investment_accounts` row (auto-created by the `handle_new_user` trigger), then insert a `holdings` row (account_id = A's account, symbol INFY, exchange NSE, asset_type stocks, quantity 1, avg_buy_price 1, currency INR).
      - As B: `select('*')` from `holdings` — assert length 0, else throw `FAIL: RLS read leak`.
      - As B: attempt to insert a holding into A's `account_id` — assert the insert returns an error, else throw `FAIL: RLS write leak`.
      - On success print `PASS: cross-user read and write are blocked` and exit 0; on failure exit non-zero.
    Load env for the script (e.g. import via `dotenv`/`node --env-file=.env.local`, or read `.env.local` inline) — prefer `node --env-file=.env.local` if the Node version supports it, otherwise inline-parse `.env.local`.
    Wire the runner: add `tsx` as a devDependency (`npm install -D tsx`) and add `"test:rls": "tsx scripts/rls-isolation-test.ts"` to package.json scripts. (If adding tsx is undesirable, author the script as `scripts/rls-isolation-test.mjs` and run with `node --env-file=.env.local`; keep whichever you choose consistent with the npm script.)
    Run `npm run test:rls` against the running local stack — it MUST print PASS. If it fails on read or write leak, that means the RLS fixes from plan 01 did not take — do not weaken the test to make it pass; fix the migration.
  </action>
  <verify>`npm run test:rls` exits 0 and prints `PASS: cross-user read and write are blocked`.</verify>
  <done>The isolation test runs green: a second user provably cannot read or write the first user's holdings.</done>
</task>

<task type="auto">
  <name>Task 3: Run the Supabase security advisor / lint check</name>
  <files>scripts/rls-isolation-test.ts</files>
  <action>
    Verify no RLS misconfiguration warnings remain (phase success criterion 5: "Supabase Security Advisor reports clean"). Run the local database linter: `npx supabase db lint --level warning` and, if available on this CLI version, the security advisors. Review output for any "RLS enabled but no policy" / "permissive policy" / "policy allows all" findings on `price_cache` or `news_items`.
    If any actionable RLS finding remains, document it in the plan SUMMARY (do not silently ignore). Known-acceptable: informational notices unrelated to the two shared tables. This task adds no new file — it records the advisor result in the SUMMARY; keep `scripts/rls-isolation-test.ts` as the durable proof artifact.
  </action>
  <verify>`npx supabase db lint --level warning` runs and shows no permissive-policy/RLS-missing findings on `price_cache` or `news_items`.</verify>
  <done>The security advisor / db lint shows no RLS write-hole findings on the shared tables; result recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `npm run test:rls` → PASS (cross-user read and write blocked).
- `src/utils/supabase/admin.ts` is `server-only`, uses `@supabase/supabase-js`, `persistSession:false`.
- `grep -rn "SUPABASE_SERVICE_ROLE_KEY" src/` shows it referenced only in `admin.ts` (server-only), never behind `NEXT_PUBLIC_`.
- `supabase db lint` clean on the shared tables.
</verification>

<success_criteria>
The two-user isolation test passes against real JWT sessions through the anon key, and a dedicated service-role admin client exists that the browser can never receive and a user cookie can never override.
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-rls-foundation/01-02-SUMMARY.md`.
</output>
