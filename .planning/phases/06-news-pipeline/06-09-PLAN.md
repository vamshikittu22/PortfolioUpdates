---
phase: 06-news-pipeline
plan: 09
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05", "06-07", "06-08"]
files_modified:
  - src/lib/news/ingest.ts
  - src/app/api/news/refresh/route.ts
autonomous: true
requirements: [NEWS-01, NEWS-02, NEWS-04, NEWS-05, ALRT-04]

must_haves:
  truths:
    - "refreshAllNews(admin) runs the whole pipeline honestly: discover held+watched instruments → fetch per-source (Finnhub for US symbols, Google News per NSE/BSE instrument, publisher feeds once) → canonicalize/dedup (URL + title_hash) → word-boundary match → insert ONLY matched items (no firehose) → batch-summarize pending → returns a summary whose every count is real."
    - "Dedup is enforced twice: a pre-query by url/title_hash skips known items, and the DB's unique constraints backstop races — a 23505 on insert is counted as itemsDuplicate, never an error."
    - "Summaries persist once and are never regenerated: only summary_status='pending' rows are ever sent to the AI; each successful batch is written IMMEDIATELY (a later 429 degrades only the remainder); on quota exhaustion remaining rows STAY pending (retry next run) and the summary reports aiDegraded: true — the run itself still succeeds (NEWS-05)."
    - "POST /api/news/refresh is a structural clone of the dispatch route: NEWS_REFRESH_SECRET guard via isAuthorizedRefreshRequest BEFORE createAdminClient; the sweepNewsAlerts + dispatchOutbox tail is wrapped in an inner try/catch so a Telegram problem can never fail the news refresh (prices/refresh/route.ts:23-27 precedent)."
  artifacts:
    - path: "src/lib/news/ingest.ts"
      provides: "refreshAllNews orchestration (fetch→dedup→match→insert→summarize) with honest NewsRefreshSummary"
      exports: ["refreshAllNews"]
    - path: "src/app/api/news/refresh/route.ts"
      provides: "secret-guarded pipeline entry with failure-isolated alerts tail"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/news/refresh/route.ts"
      to: "src/lib/news/ingest.ts"
      via: "refreshAllNews(createAdminClient()) after the secret guard"
      pattern: "refreshAllNews"
    - from: "src/app/api/news/refresh/route.ts"
      to: "src/lib/news/alert-sweep.ts + src/lib/notifications/outbox.ts"
      via: "inner-try/catch tail: sweepNewsAlerts then dispatchOutbox"
      pattern: "sweepNewsAlerts"
    - from: "src/lib/news/ingest.ts"
      to: "news_items + news_item_instruments"
      via: "service-role inserts (admin client injected by the route — the exact use case admin.ts's warning comment anticipates)"
      pattern: "news_item_instruments"
---

<objective>
Compose every Phase 6 module into the ingest orchestration (`refreshAllNews`) and give the pipeline its own secret-guarded entry point (`/api/news/refresh`, `NEWS_REFRESH_SECRET`) with the Phase-5 outbox dispatch tail — completing the write side of NEWS-01/02/04/05 and wiring ALRT-04 into the run.

Purpose: precedent is explicit (06-RESEARCH-codebase Q4): news gets its OWN route and OWN independently-rotatable secret, never piggybacks the 3-hourly price cron; the tail still reuses dispatchOutbox so significant-news messages go out on the same run. This is the refresh-service.ts of Phase 6 — wiring, not reimplementation.
Output: `src/lib/news/ingest.ts`, `src/app/api/news/refresh/route.ts`, plus gitignored .env.local placeholder/secret additions.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-news-pipeline/06-RESEARCH-codebase.md
@.planning/phases/06-news-pipeline/06-RESEARCH-external.md
@src/lib/prices/refresh-service.ts
@src/app/api/prices/refresh/route.ts
@src/app/api/notifications/dispatch/route.ts
@src/lib/news/fetch-news.ts
@src/lib/news/dedupe.ts
@src/lib/news/match.ts
@src/lib/news/ai.ts
@src/lib/news/alert-sweep.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: refreshAllNews — fetch, dedup, match, insert</name>
  <files>src/lib/news/ingest.ts</files>
  <action>
Create `src/lib/news/ingest.ts` mirroring refresh-service.ts's header discipline (accepts an ALREADY-CONSTRUCTED admin client; single orchestration point where news_items/news_item_instruments are ever written; wires 06-02/03/04/05/08 logic, reimplements none of it).

`export interface NewsRefreshSummary { instrumentsConsidered: number; sourcesFetched: number; sourceErrors: string[]; itemsSeen: number; itemsNew: number; itemsDuplicate: number; itemsUnmatched: number; summarizedNow: number; degradedNow: number; aiDegraded: boolean; timestamp: string }` — every count real, never fabricated.

`export async function refreshAllNews(admin: SupabaseClient): Promise<NewsRefreshSummary>` — first half (this task): 
  1. Discover instruments exactly like refresh-service.ts:43-57 (union of transactions + watchlist_items instrument_ids) then load `instruments.select('id, symbol, display_name, exchange')` for them. Build MatchCandidate[] (instrumentId, symbol, displayName) for the matcher.
  2. Fetch, SEQUENTIALLY per source class (politeness + Finnhub's per-minute limit — outbox sendSequentially precedent; a short `await new Promise(r => setTimeout(r, 300))` between calls):
     - US instruments (exchange 'NASDAQ' | 'NYSE'): fetchFinnhubCompanyNews(symbol, from = 7 days ago YYYY-MM-DD, to = today) per instrument. A 'not configured' fetchError is recorded ONCE in sourceErrors (not per-instrument spam) and Finnhub is skipped for the run — headline degradation, not failure.
     - NSE/BSE instruments: fetchGoogleNews(buildInstrumentNewsQuery(display_name, symbol)) per instrument.
     - INDIAN_PUBLISHER_FEEDS: fetchPublisherFeed once each per run.
     Every fetchError → sourceErrors[] (source-labeled); sourcesFetched counts successful fetches. The run NEVER aborts on a source failure.
  3. For every fetched RawNewsItem (itemsSeen++): canonicalUrl = canonicalizeUrl(url); titleHash = computeTitleHash(title). Pre-dedup: batch-query existing `news_items.select('url, title_hash')` filtered `.in('url', urls)` / `.in('title_hash', hashes)` (chunk .in() lists at 200) plus an in-run Set (the same story often appears in Google News AND a publisher feed within one run — dedup in-memory first). Known url OR title_hash → itemsDuplicate++, skip.
  4. Match: text = title + ' ' + (abstract ?? ''); matches = matchInstruments(text, candidates) — EXCEPT Finnhub items, which are already company-scoped: seed with the fetched-for instrument, then union word-boundary matches (the `related` field is ignored — bare symbols without exchange identity, the exact price_cache flaw). Zero matches → itemsUnmatched++, skip (portfolio-relevant only; the no-firehose rule).
  5. Insert per item (service-role): `news_items.insert({ headline: title, url: canonicalUrl, source, published_at: publishedAtIso, title_hash: titleHash, affected_symbols: matched display symbols, summary_status: 'pending' })` selecting the new id; error code 23505 (url or title_hash race) → itemsDuplicate++, continue — never fail the run on a duplicate. Then insert news_item_instruments rows [{ news_item_id, instrument_id, matched_via }] (ignore-duplicate on 23505 likewise). itemsNew++.
Leave a `// Task 2 continues: summarization pass` marker; return the summary with summarization fields zeroed for now. `npx tsc --noEmit` clean. Commit: `feat(06-09): news ingest — fetch, dedup, match, insert` (explicit pathspec).
  </action>
  <verify>`npx tsc --noEmit` clean; `grep -n "createAdminClient" src/lib/news/ingest.ts` returns NOTHING (client injected); `grep -nE "parseGoogleNewsRss|XMLParser" src/lib/news/ingest.ts` returns NOTHING (no interpretation re-implemented); grep shows canonicalizeUrl + computeTitleHash + matchInstruments all imported.</verify>
  <done>The fetch→dedup→match→insert half runs with honest per-source error accounting and DB-backstopped dedup.</done>
</task>

<task type="auto">
  <name>Task 2: Summarization pass — batched, durable per batch, quota-degrading</name>
  <files>src/lib/news/ingest.ts</files>
  <action>
Extend `refreshAllNews` with the NEWS-04/05 summarization pass (after inserts, same function):
  1. Load work: `news_items.select('id, headline, source, affected_symbols').eq('summary_status', 'pending').order('published_at', { ascending: false }).limit(30)` — newest-first cap keeps one run inside any plausible free-tier budget; older rows remain pending for later runs (comment this).
  2. Chunk into batches of 10. For each batch sequentially: `summarizeNewsBatch(batch.map(r => ({ id: r.id, title: r.headline, source: r.source ?? '', tickers: r.affected_symbols ?? [] })))`.
     - Outcome quotaExhausted → set aiDegraded = true, record the error in sourceErrors, BREAK immediately (research Pitfall 6: quota can die mid-run; do not hammer). Remaining rows stay 'pending' — eligible again next run when budget returns. The RUN STILL SUCCEEDS.
     - Other error (incl. 'not configured') → record once in sourceErrors, break as well (no point retrying this run) — rows stay pending. aiDegraded = true here too (feed is headlines-only this run).
     - Success → for each row in the batch: if results has the id → update that news_items row: `summary: `${r.summary} Why it matters: ${r.whyItMatters}`.trim()` (single composed column — the display and the alert sweep both read this one field), `sentiment_label: r.sentimentLabel`, `importance: r.importance`, `summary_status: 'summarized'`, `summarized_at: new Date().toISOString()`; summarizedNow++. If the id is MISSING from results (model skipped/invalid element) → `summary_status: 'degraded'` (permanent headlines-only for that item — never burn budget retrying a row the model can't handle; comment the pending-vs-degraded semantics: pending = not yet tried or budget ran out, degraded = tried and unusable); degradedNow++.
     Writes happen PER BATCH immediately after that batch succeeds — durability rule (a later 429 must not lose earlier batches).
  3. Guard the never-regenerate invariant in code: the UPDATE for 'summarized' rows adds `.eq('summary_status', 'pending')` so a concurrent run can never overwrite an existing summary (NEWS-04: persisted, never regenerated).
`npx tsc --noEmit` clean. Commit: `feat(06-09): batched persistent summarization with quota degradation` (explicit pathspec).
  </action>
  <verify>`npx tsc --noEmit` clean; grep shows `.eq('summary_status', 'pending')` on the summarized-update path, a `quotaExhausted` break, and per-batch (not end-of-run) writes; `grep -n "regenerat" src/lib/news/ingest.ts` shows the invariant comment.</verify>
  <done>Pending rows are summarized in durable batches of 10 (max 30/run); quota exhaustion degrades the remainder honestly; summarized rows are immutable to re-summarization.</done>
</task>

<task type="auto">
  <name>Task 3: /api/news/refresh route + env placeholders</name>
  <files>src/app/api/news/refresh/route.ts</files>
  <action>
Create `src/app/api/news/refresh/route.ts` as a structural clone of `src/app/api/notifications/dispatch/route.ts` (27-line shape, confirmed against the repo's bundled Next 16 route-handler docs in Phase 5 — same POST signature):
  1. Guard FIRST: `isAuthorizedRefreshRequest(request.headers.get('authorization'), process.env.NEWS_REFRESH_SECRET ?? '')` (shared pure predicate from `@/lib/prices/ingest` — empty/unset secret ALWAYS denies) → 401 before ANY Supabase client exists.
  2. `const admin = createAdminClient(); const summary = await refreshAllNews(admin);`
  3. Alerts tail, failure-isolated (prices/refresh/route.ts:23-27 clone): inner try/catch around `const alerts = await sweepNewsAlerts(admin); const dispatched = await dispatchOutbox(admin);` — catch → `alertsResult = { error: message }`; a Telegram/outbox problem must NEVER fail the news refresh. No revalidatePath (route runs outside render context — route.ts:27 precedent).
  4. Return `NextResponse.json({ success: true, ...summary, alerts: alertsResult })`; outer catch → 500 with the error message.
Header comment: why a separate route + separate secret (least-privilege/independent-rotation, the NOTIFY_DISPATCH_SECRET precedent from .env.local:44-46; news is heavier and independently schedulable — cadence knob `refresh_interval_news` exists but wiring a news cron migration is EXPLICITLY out of scope this phase: deploy-gated like price_refresh_cron.sql, deferred until a public deploy exists).
Also update the gitignored `.env.local` (NOT a committed file — no files_modified entry): append `FINNHUB_API_KEY=<labeled placeholder>` with a comment ("finnhub.io free account → dashboard; unset → US news skipped with an honest 'not configured' source error") and a real self-generated `NEWS_REFRESH_SECRET` via `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` with the standard one-secret-per-route comment. NEVER echo the generated secret into any committed file, plan, or SUMMARY.
`npm run build` must list /api/news/refresh. Commit (route file only): `feat(06-09): secret-guarded /api/news/refresh with failure-isolated alerts tail`.
  </action>
  <verify>`npx tsc --noEmit` + `npm run build` clean, `/api/news/refresh` in the route table; `grep -n "isAuthorizedRefreshRequest" src/app/api/news/refresh/route.ts` appears BEFORE `createAdminClient` in the file; unauthorized smoke: `npm run dev` + `curl -s -X POST http://localhost:3000/api/news/refresh` → 401 without touching Supabase; authorized smoke with the local secret runs the pipeline (against unpushed-migration DB it will honestly error on missing columns — record whatever ACTUALLY happens; the 401 path is the mandatory pass).</verify>
  <done>The pipeline has its own independently-rotatable secret-guarded entry composing ingest → news sweep → outbox dispatch with the house failure-isolation; env placeholders live only in gitignored .env.local.</done>
</task>

</tasks>

<verification>
- tsc + build clean; route listed; unauthorized POST → 401.
- `git show <hash> --stat` per commit: only this plan's two committed files ever land (env changes are gitignored by nature).
- Secret hygiene: `git grep -nE "[0-9]{8,10}:[A-Za-z0-9_-]{30,}|AIza[A-Za-z0-9_-]{10,}" -- src/lib/news src/app/api/news` returns NOTHING; no real secret value appears in any committed artifact.
</verification>

<success_criteria>
One authorized POST executes the entire honest pipeline: portfolio-scoped multi-source fetch, double-keyed dedup, word-boundary matching, batch-durable never-regenerated summaries, quota-honest degradation, and the significant-news→Telegram tail — with live end-to-end proof deferred to 06-10 (key + migration gated).
</success_criteria>

<output>
After completion, create `.planning/phases/06-news-pipeline/06-09-SUMMARY.md`.
</output>
