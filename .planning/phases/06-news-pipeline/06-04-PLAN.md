---
phase: 06-news-pipeline
plan: 04
type: tdd
wave: 2
depends_on: ["06-02"]
files_modified:
  - src/lib/news/parse-feeds.ts
  - scripts/news-parse-test.ts
autonomous: true
requirements: [NEWS-01]

must_haves:
  truths:
    - "parseGoogleNewsRss turns a real Google News RSS 2.0 document into RawNewsItem[] with: publisher taken from the <source> element (never 'Google News'), the Google redirect <link> as url, and RFC-822 GMT pubDate converted to ISO-8601 UTC."
    - "parseRssFeed handles CDATA-wrapped titles/links/descriptions (the live-observed ET Markets/LiveMint shape) and +0530 pubDates, producing ISO-8601 UTC publishedAtIso and the caller-supplied source name."
    - "parseFinnhubNews validates an unknown JSON value into RawNewsItem[] mapping headline→title and unix-SECONDS datetime→ISO (×1000), skipping malformed entries honestly."
    - "A single-item RSS channel still yields a one-element ARRAY (fast-xml-parser isArray pitfall pinned by test), and malformed XML/JSON returns { items: [], error } — parsers never throw."
  artifacts:
    - path: "src/lib/news/parse-feeds.ts"
      provides: "pure feed-interpretation layer (fetch wrappers stay dumb — fetch-prices.ts separation precedent)"
      exports: ["parseGoogleNewsRss", "parseRssFeed", "parseFinnhubNews"]
    - path: "scripts/news-parse-test.ts"
      provides: "node:assert/strict proof over inline fixture strings modeled on the LIVE-captured feed shapes"
  key_links:
    - from: "src/lib/news/parse-feeds.ts"
      to: "fast-xml-parser"
      via: "XMLParser with ignoreAttributes:false and isArray forcing 'item' arrays"
      pattern: "new XMLParser"
    - from: "src/lib/news/parse-feeds.ts"
      to: "src/lib/news/types.ts"
      via: "RawNewsItem import (06-02's file — safe now, wave 2)"
      pattern: "RawNewsItem"
---

<objective>
Implement the pure interpretation layer for all three news sources — Google News RSS, generic publisher RSS (ET Markets/LiveMint), and Finnhub company-news JSON — each producing the shared `RawNewsItem` shape with ISO-8601 UTC timestamps, proven by TDD against fixture strings modeled byte-for-byte on the live feeds captured in 06-RESEARCH-external.

Purpose: the house fetch-wrapper style (fetch-prices.ts) demands ALL response interpretation live in separate pure, TDD-tested functions — the 06-08 wrappers will only get bytes and hand them here. The three timestamp formats (RFC-822 GMT, RFC-822 +0530, unix seconds) and the CDATA/attribute/single-item XML quirks are exactly the bugs this plan pins before any network code exists.
Output: `src/lib/news/parse-feeds.ts` + `scripts/news-parse-test.ts` (script test:news-parse already registered by 06-02).
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-news-pipeline/06-RESEARCH-external.md
@src/lib/news/types.ts
@src/lib/prices/fetch-prices.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD RED — fixture-driven tests for all three parsers</name>
  <files>scripts/news-parse-test.ts</files>
  <action>
Write `scripts/news-parse-test.ts` (node:assert/strict, tsx, grouped cases). Build inline fixture strings copied structurally from the LIVE captures in 06-RESEARCH-external.md §2/§3 (invent innocuous headlines; keep the exact XML structure):
  - Google News fixture: `<rss version="2.0" xmlns:media="...">` → channel → 2 items, each with plain-text title ending in ` - The Economic Times`, a `https://news.google.com/rss/articles/CBMi...?oc=5` link, `<guid isPermaLink="false">`, `<pubDate>Fri, 17 Jul 2026 05:25:44 GMT</pubDate>`, an HTML-anchor description, and `<source url="https://m.economictimes.com">The Economic Times</source>`.
  - Publisher fixture (ET/LiveMint shape): CDATA-wrapped `<title>`, `<link>`, `<description>`, `<guid>`; `<pubDate>Fri, 17 Jul 2026 21:49:03 +0530</pubDate>`; real publisher article URLs.
  - Single-item publisher fixture: ONE `<item>` only (the fast-xml-parser object-not-array pitfall).
  - Finnhub fixture: JS array of objects with category/datetime (unix seconds, e.g. 1784295044)/headline/id/image/related/source/summary/url — per the swagger CompanyNews definition.
Assertions:
  1. Google: 2 items; title keeps the raw ` - Publisher` suffix (normalization is dedupe.ts's job, NOT the parser's); url is the google redirect link; source === 'The Economic Times' (from <source>, not hardcoded); publishedAtIso === '2026-07-17T05:25:44.000Z'; abstract === null (Google description is anchor soup — discarded).
  2. Publisher: CDATA unwrapped cleanly; +0530 pubDate converts to the correct UTC instant ('2026-07-17T16:19:03.000Z' for the fixture above); source === the caller-passed name; abstract === the description text.
  3. Single-item fixture yields items.length === 1 (array, not object).
  4. Finnhub: datetime 1784295044 → new Date(1784295044*1000).toISOString(); headline→title; summary→abstract; source passed through; entries missing url or headline are SKIPPED (not fabricated), remaining items still returned.
  5. Malformed inputs: parseGoogleNewsRss('not xml at all') and parseRssFeed('<html>…') → { items: [], error: <non-empty string> }; parseFinnhubNews('nope') / parseFinnhubNews({}) → { items: [], error }; a Finnhub JSON error object ({"error":"Invalid API key"} — the live-probed 401 body) → { items: [], error } mentioning the API message. No parser ever throws.
Run `npm run test:news-parse` — MUST fail (module missing). Commit RED: `test(06-04): add failing feed-parser tests` (explicit pathspec).
  </action>
  <verify>`npm run test:news-parse` fails with module-not-found (RED verified); the test file itself typechecks under `npx tsc --noEmit` apart from the missing import.</verify>
  <done>Fixture-driven failing tests exist encoding the live-observed shapes of all three sources.</done>
</task>

<task type="auto">
  <name>Task 2: TDD GREEN — implement the three parsers</name>
  <files>src/lib/news/parse-feeds.ts</files>
  <action>
Create `src/lib/news/parse-feeds.ts` — pure, zero I/O, imports `RawNewsItem` from `@/lib/news/types` and `XMLParser` from `fast-xml-parser` (installed by 06-02). Export a shared result shape `type ParseResult = { items: RawNewsItem[]; error: string | null }` (or reuse one declared in types.ts if 06-02 already added it — check first).

  - Shared XML setup: `new XMLParser({ ignoreAttributes: false, isArray: (name) => name === 'item' })`. Wrap `.parse()` in try/catch; also treat a result missing `rss.channel` as an error (fast-xml-parser parses plain text without throwing — the 'not xml at all' test catches this).
  - `parseGoogleNewsRss(xml: string): ParseResult` — per item: title (string node), url = link, source = the `<source>` element's text node (fast-xml-parser puts the text at `item.source['#text']` when attributes are on — handle both string and object forms; fallback 'Google News' only if absent), publishedAtIso = `new Date(pubDate).toISOString()` (RFC-822 GMT parses natively; guard invalid dates → skip item), abstract = null. Skip items missing title or link.
  - `parseRssFeed(xml: string, sourceName: string): ParseResult` — same channel walk; CDATA arrives merged as text with fast-xml-parser defaults but values may need `String(x).trim()`; abstract = description text or null; source = sourceName; same date conversion (+0530 offsets parse natively via Date). Skip items missing title or link.
  - `parseFinnhubNews(json: unknown, fallbackSource?: string): ParseResult` — if not an Array: if it's an object with a string `error` field return { items: [], error: that message } (the live 401 body shape); else generic error. Per entry: require string url + headline and numeric datetime; publishedAtIso = new Date(datetime * 1000).toISOString(); abstract = summary || null; source = entry.source || fallbackSource || 'Finnhub'. Skip invalid entries; never throw.
Run `npm run test:news-parse` — MUST pass. `npx tsc --noEmit` clean. Commit GREEN: `feat(06-04): implement Google News/publisher RSS + Finnhub parsers` (explicit pathspec).
  </action>
  <verify>`npm run test:news-parse` passes all groups; `npx tsc --noEmit` clean; `grep -n "fetch(" src/lib/news/parse-feeds.ts` returns NOTHING (pure — no I/O).</verify>
  <done>All three parsers produce RawNewsItem[] with ISO-UTC timestamps from the live-verified shapes; malformed input yields honest { items: [], error }.</done>
</task>

</tasks>

<verification>
- RED then GREEN commits present, each pathspec-scoped to this plan's files.
- `npm run test:news-parse` green; tsc clean.
- No network, no Date.now, no randomness in parse-feeds.ts.
</verification>

<success_criteria>
Given raw bytes from any of the three live-verified sources, the parse layer deterministically yields normalized RawNewsItem[] (or an honest error) — the 06-08 wrappers need only fetch and delegate.
</success_criteria>

<output>
After completion, create `.planning/phases/06-news-pipeline/06-04-SUMMARY.md`.
</output>
