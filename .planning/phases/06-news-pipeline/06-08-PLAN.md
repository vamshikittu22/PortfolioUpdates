---
phase: 06-news-pipeline
plan: 08
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - src/lib/news/sources.ts
  - src/lib/news/fetch-news.ts
autonomous: true
requirements: [NEWS-01]
user_setup:
  - service: finnhub
    why: "US company-news source (free tier)"
    env_vars:
      - name: FINNHUB_API_KEY
        source: "finnhub.io — free account → dashboard shows the API key (placeholder added to .env.local by plan 06-09; live use verified at 06-10)"

must_haves:
  truths:
    - "fetchGoogleNews and fetchPublisherFeed work LIVE right now (no key needed) — verified in this plan by actually fetching the Google News India-locale search feed and both Indian publisher feeds through the real wrappers and getting >0 parsed items each."
    - "fetchFinnhubCompanyNews with an unset/placeholder FINNHUB_API_KEY returns an honest { items: null, fetchError: 'not configured' } result — never throws, never fabricates (telegram/api.ts precedent); its live path is key-gated and defers to 06-10."
    - "Wrappers own ONLY 'get the bytes and survive the network failing' — every response is interpreted by 06-04's pure parsers; a non-200, a timeout, or a parse error all surface as honest per-source fetchError strings."
    - "URL construction is centralized in sources.ts: Google News search URL carries hl=en-IN&gl=IN&ceid=IN:en; per-instrument queries quote the suffix-stripped company name OR the symbol; the two publisher feeds are the live-verified ET Markets + LiveMint URLs."
  artifacts:
    - path: "src/lib/news/sources.ts"
      provides: "feed registry + URL builders (Google News search, Finnhub company-news, Indian publisher list)"
      exports: ["buildGoogleNewsSearchUrl", "buildInstrumentNewsQuery", "buildFinnhubCompanyNewsUrl", "INDIAN_PUBLISHER_FEEDS"]
    - path: "src/lib/news/fetch-news.ts"
      provides: "raw-fetch wrappers with discriminated honest results for all three sources"
      exports: ["fetchGoogleNews", "fetchPublisherFeed", "fetchFinnhubCompanyNews"]
  key_links:
    - from: "src/lib/news/fetch-news.ts"
      to: "src/lib/news/parse-feeds.ts"
      via: "wrapper fetches text/json then delegates ALL interpretation to the pure parsers"
      pattern: "parseGoogleNewsRss|parseRssFeed|parseFinnhubNews"
    - from: "src/lib/news/sources.ts"
      to: "src/lib/news/match.ts"
      via: "stripCompanySuffixes reuse for clean query phrases"
      pattern: "stripCompanySuffixes"
---

<objective>
Implement the NEWS-01 network layer: honest raw-fetch wrappers for Finnhub company-news (key-gated), Google News RSS search (India locale), and the Indian publisher feeds — and LIVE-verify the two keyless paths immediately, since they need no credentials, no DB, and no consent (the planning rule: keyless fetch checks belong HERE, not at the checkpoint).

Purpose: every upstream shape was live-verified on 2026-07-17 (06-RESEARCH-external §§1-3): exact URLs, params, auth behavior, and error bodies are known. The wrapper layer turns those into the house discriminated-result style so ingest (06-09) can compose without try/catch gymnastics.
Output: `src/lib/news/{sources,fetch-news}.ts` + a live smoke run recorded in the SUMMARY.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-news-pipeline/06-RESEARCH-external.md
@src/lib/prices/fetch-prices.ts
@src/lib/telegram/api.ts
@src/lib/news/parse-feeds.ts
@src/lib/news/match.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: sources.ts — feed registry + URL builders</name>
  <files>src/lib/news/sources.ts</files>
  <action>
Create `src/lib/news/sources.ts` (pure — string builders and constants only):
  - `export const INDIAN_PUBLISHER_FEEDS = [ { name: 'ET Markets', url: 'https://economictimes.indiatimes.com/markets/rssfeeds/1977021501.cms' }, { name: 'LiveMint Markets', url: 'https://www.livemint.com/rss/markets' } ] as const;` — both live-verified 2026-07-17 (06-RESEARCH-external §3); comment each with the verification date.
  - `export function buildInstrumentNewsQuery(displayName: string, symbol: string): string` — `"<stripCompanySuffixes(displayName)>" OR "<symbol>"` (quoted phrases; import stripCompanySuffixes from `@/lib/news/match` — 06-03 exported it for exactly this); if the stripped name is empty/too short (<3), fall back to just the quoted symbol.
  - `export function buildGoogleNewsSearchUrl(query: string): string` — `https://news.google.com/rss/search?q=${encodeURIComponent(query)}&hl=en-IN&gl=IN&ceid=IN:en` (the live-verified India-locale param triple; comment: ceid = country:language edition id).
  - `export function buildFinnhubCompanyNewsUrl(symbol: string, fromDate: string, toDate: string, token: string): string` — `https://finnhub.io/api/v1/company-news?symbol=...&from=...&to=...&token=...` with encodeURIComponent on all values; from/to are YYYY-MM-DD (required, per Finnhub's swagger); comment: North-American symbols only — callers must route NSE/BSE elsewhere.
  </action>
  <verify>`npx tsc --noEmit` clean; `node -e` quick check printing buildGoogleNewsSearchUrl('"Infosys" OR "INFY"') shows correctly-encoded q and all three locale params.</verify>
  <done>All feed URLs/queries are built in one pure module with the live-verified shapes and honest coverage comments.</done>
</task>

<task type="auto">
  <name>Task 2: fetch-news.ts wrappers + LIVE keyless verification</name>
  <files>src/lib/news/fetch-news.ts</files>
  <action>
Create `src/lib/news/fetch-news.ts` in the fetch-prices.ts style (wrapper owns bytes+network-failure only; discriminated results; browser User-Agent constant reused-in-spirit — declare a local `NEWS_FETCH_USER_AGENT` copying fetch-prices.ts:25's value, some publishers 403 bot UAs):
  - `export type NewsFetchResult = { items: RawNewsItem[]; fetchError: null } | { items: null; fetchError: string };`
  - `export async function fetchGoogleNews(query: string): Promise<NewsFetchResult>` — fetch(buildGoogleNewsSearchUrl(query), { headers: { 'User-Agent': ... } }); non-ok → { items: null, fetchError: `Google News HTTP ${status}` }; ok → text → parseGoogleNewsRss; parse error → fetchError with the parser's message; else items. try/catch network → honest message. AbortSignal.timeout(15000) on every fetch in this file (hung feeds must not wedge the pipeline).
  - `export async function fetchPublisherFeed(feed: { name: string; url: string }): Promise<NewsFetchResult>` — same shape via parseRssFeed(text, feed.name).
  - `export async function fetchFinnhubCompanyNews(symbol: string, fromDate: string, toDate: string): Promise<NewsFetchResult>` — key guard FIRST: `const token = process.env.FINNHUB_API_KEY`; unset or placeholder-looking (falsy, or containing 'your-', matching the .env.local labeled-placeholder convention) → { items: null, fetchError: 'FINNHUB_API_KEY not configured' } (telegram/api.ts:30-34 precedent). Fetch; ANY non-200 (the live-probed 401 bodies included) → read body text and return fetchError including status + body snippet (the JSON error message is the useful part); 200 → response.json() → parseFinnhubNews. Never throw.
Then LIVE-VERIFY the keyless paths NOW (no key, no DB, no consent needed — this is the plan's headline check): run
  `npx tsx -e "import('./src/lib/news/fetch-news').then(async m => { const g = await m.fetchGoogleNews('\"Infosys\" OR \"INFY\"'); const {INDIAN_PUBLISHER_FEEDS} = await import('./src/lib/news/sources'); const p1 = await m.fetchPublisherFeed(INDIAN_PUBLISHER_FEEDS[0]); const p2 = await m.fetchPublisherFeed(INDIAN_PUBLISHER_FEEDS[1]); console.log('google', g.fetchError ?? g.items.length, '| et', p1.fetchError ?? p1.items.length, '| mint', p2.fetchError ?? p2.items.length); if (!g.items?.length || !p1.items?.length || !p2.items?.length) process.exit(1); console.log('sample:', g.items[0].title, '|', g.items[0].source, '|', g.items[0].publishedAtIso); })"`
(adjust import specifiers/tsconfig-paths as needed — a throwaway scripts-free invocation; do NOT commit a new script file for it). All three must report >0 items with sane titles/ISO timestamps. Also verify the Finnhub not-configured path: run with FINNHUB_API_KEY unset and confirm the honest fetchError (no throw). Record all outputs verbatim in the SUMMARY. If a feed is down at execution time, record the honest failure and proceed (the wrapper handling IS the deliverable; retry once before recording).
Commit per task with explicit pathspecs.
  </action>
  <verify>`npx tsc --noEmit` + `npm run build` clean; the live smoke run printed >0 items for Google News + ET + LiveMint through the REAL wrappers; Finnhub not-configured path returns the honest error result; `grep -n "throw" src/lib/news/fetch-news.ts` shows no throws escaping the wrappers (internal try/catch only).</verify>
  <done>All three source wrappers exist with honest discriminated results; the two keyless paths are PROVEN LIVE (not deferred); the key-gated Finnhub live path is explicitly deferred to 06-10 with its not-configured behavior proven now.</done>
</task>

</tasks>

<verification>
- Live outputs recorded in SUMMARY (item counts + one sample item per keyless source).
- No parsing logic in fetch-news.ts (all interpretation delegated — grep confirms parser imports).
- tsc + build clean; commits pathspec-scoped to this plan's two files.
</verification>

<success_criteria>
NEWS-01's fetch layer is real and partially live-proven: India-side news (Google News + two publisher feeds) flows end-to-end through fetch→parse today; the US-side Finnhub path is fully coded with honest key-gating, awaiting only the user's free API key at 06-10.
</success_criteria>

<output>
After completion, create `.planning/phases/06-news-pipeline/06-08-SUMMARY.md`.
</output>
