---
phase: 06-news-pipeline
plan: 05
type: tdd
wave: 2
depends_on: ["06-02"]
files_modified:
  - src/lib/news/summarize.ts
  - src/lib/news/ai.ts
  - scripts/news-summarize-test.ts
autonomous: true
requirements: [NEWS-04, NEWS-05]

must_haves:
  truths:
    - "The pure layer (summarize.ts) owns prompt building, JSON-schema declaration, response validation, and AI-error classification — the wrapper (ai.ts) owns ONLY the @google/genai call, mirroring the fetch-prices/parse split."
    - "classifyAiError returns 'quota' for an ApiError-shaped error with status 429 or a RESOURCE_EXHAUSTED/quota message, and 'other' otherwise — the single NEWS-05 degrade trigger."
    - "parseSummarizeResponse tolerates undefined text, non-JSON, wrong-shape JSON, unknown ids, and invalid enum values by omitting the offending items — it never throws and never fabricates a summary."
    - "summarizeNewsBatch passes { apiKey: process.env.GEMINI_API_KEY } EXPLICITLY (the SDK auto-reads GOOGLE_API_KEY, which this repo does not set — research §4 trap) and returns an honest 'not configured' result when the key is unset/placeholder, never throwing."
  artifacts:
    - path: "src/lib/news/summarize.ts"
      provides: "pure prompt/schema/parse/classify logic for batched news summarization"
      exports: ["buildSummarizePrompt", "NEWS_SUMMARY_JSON_SCHEMA", "parseSummarizeResponse", "classifyAiError", "NEWS_AI_MODEL"]
    - path: "src/lib/news/ai.ts"
      provides: "@google/genai wrapper: one generateContent call per batch, JSON output mode, honest error results"
      exports: ["summarizeNewsBatch"]
    - path: "scripts/news-summarize-test.ts"
      provides: "node:assert/strict proof of the pure layer"
  key_links:
    - from: "src/lib/news/ai.ts"
      to: "@google/genai"
      via: "GoogleGenAI + ai.models.generateContent with responseMimeType 'application/json' + responseJsonSchema"
      pattern: "GoogleGenAI"
    - from: "src/lib/news/ai.ts"
      to: "src/lib/news/summarize.ts"
      via: "buildSummarizePrompt/parseSummarizeResponse/classifyAiError composition"
      pattern: "parseSummarizeResponse"
---

<objective>
Build the NEWS-04 batched summarization module on the NEW `@google/genai` SDK (v2.x, installed by 06-02): a pure logic layer (prompt, JSON schema, response validation, 429/quota classification) proven by TDD, plus a thin I/O wrapper that makes exactly one `generateContent` call per batch of headlines and degrades honestly (NEWS-05) instead of ever throwing at callers.

Purpose: REQUIREMENTS.md names `@google/genai` for NEWS-04; the resolved decision (06-RESEARCH-external §4) is coexistence — this module uses the new SDK, the three legacy `@google/generative-ai` call sites stay untouched. Free-tier quota numbers are unpublished, so 429 detection at runtime IS the budget model.
Output: `src/lib/news/{summarize,ai}.ts` + `scripts/news-summarize-test.ts` (script test:news-summarize registered by 06-02).
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-news-pipeline/06-RESEARCH-external.md
@src/lib/news/types.ts
@src/lib/telegram/api.ts
@src/lib/gemini.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD — pure summarization logic (RED then GREEN)</name>
  <files>src/lib/news/summarize.ts, scripts/news-summarize-test.ts</files>
  <action>
Write `scripts/news-summarize-test.ts` FIRST (node:assert/strict, tsx). Cases:
  1. buildSummarizePrompt: given 2 batch items ({ id, title, source, tickers: string[] }), the prompt contains both ids, both titles, the tickers, and an explicit instruction to return ONLY a JSON array where each element has id/summary/whyItMatters/sentimentLabel/importance, with sentimentLabel restricted to Bullish|Bearish|Mixed|Neutral and importance to High|Medium|Low, and 'High' importance reserved for genuinely price-moving news (earnings surprises, M&A, regulatory action, major contract wins/losses — the ALRT-04 significance bar). Deterministic: same input → identical string.
  2. parseSummarizeResponse(validJsonText, expectedIds): returns a Map keyed by id with all fields; ids NOT in expectedIds are dropped.
  3. parseSummarizeResponse(undefined, ids) → { results: empty Map, error: non-empty } (the response.text-can-be-undefined trap, research Pitfall 5). Same for 'not json' and for JSON that is an object not an array.
  4. Per-item validation: an element with sentimentLabel 'VeryBullish' or importance 'Critical' or missing summary is OMITTED while valid siblings survive; whyItMatters missing → item kept with whyItMatters '' (summary is the required core).
  5. classifyAiError: ({ name:'ApiError', status: 429, message:'…' }) → 'quota'; (new Error('RESOURCE_EXHAUSTED: quota exceeded')) → 'quota'; (new Error('quota exceeded for model')) → 'quota'; ({ status: 500 }) / (new Error('fetch failed')) → 'other'. Never throws on weird shapes (null, string).
  6. NEWS_SUMMARY_JSON_SCHEMA is a plain JSON-Schema object: type 'array', items.required includes id/summary/sentimentLabel/importance, and enum arrays match the news_items CHECK constraints exactly ('Bullish','Bearish','Mixed','Neutral' / 'High','Medium','Low').
Run `npm run test:news-summarize` — MUST fail. Commit RED: `test(06-05): add failing summarization-logic tests` (explicit pathspec).

Then implement `src/lib/news/summarize.ts` (pure, zero I/O, no env access):
  - `export const NEWS_AI_MODEL = 'gemini-2.5-flash';` — single named constant (research §4: verified current, same model/key/quota pool as the legacy call sites; a later swap to a 3.x flash is one line).
  - `export const NEWS_SUMMARY_JSON_SCHEMA = {...}` as tested above (plain JSON Schema — used via the SDK's `responseJsonSchema` config, confirmed on GenerateContentConfig typedoc; avoids depending on the SDK's Type enum).
  - `buildSummarizePrompt(items: SummarizeBatchItem[]): string` — instructs: 2-3 sentence summary, one-sentence whyItMatters ("why it matters to a holder of <tickers>"), the enum constraints, the High-importance bar, and "return ONLY the JSON array". Declare `SummarizeBatchItem { id: string; title: string; source: string; tickers: string[] }` here or in types.ts (06-02's file is safe to EDIT? NO — types.ts belongs to 06-02 which has completed by wave 2, but files_modified ownership: this plan does NOT list types.ts. Declare SummarizeBatchItem locally in summarize.ts and export it).
  - `parseSummarizeResponse(text: string | undefined, expectedIds: string[]): { results: Map<string, NewsSummaryResult>; error: string | null }` — JSON.parse in try/catch; validate per-item as tested; import NewsSummaryResult from '@/lib/news/types'.
  - `classifyAiError(err: unknown): 'quota' | 'other'` — numeric `status === 429` on the error object (the @google/genai ApiError carries .status per its typedoc) OR /RESOURCE_EXHAUSTED|quota/i.test(message).
Run tests — MUST pass; `npx tsc --noEmit` clean. Commit GREEN: `feat(06-05): implement pure summarization logic` (explicit pathspec).
  </action>
  <verify>`npm run test:news-summarize` green; `npx tsc --noEmit` clean; `grep -nE "process\.env|fetch\(|GoogleGenAI" src/lib/news/summarize.ts` returns NOTHING (purity).</verify>
  <done>Prompt/schema/parse/classify logic is pure, pinned by tests, with enums matching the DB CHECK constraints and 429 classification covering both ApiError.status and message shapes.</done>
</task>

<task type="auto">
  <name>Task 2: @google/genai wrapper — summarizeNewsBatch</name>
  <files>src/lib/news/ai.ts</files>
  <action>
Create `src/lib/news/ai.ts`, the ONLY file in the news module that touches the AI network. Mirror `src/lib/telegram/api.ts`'s header/style conventions (including whether it imports 'server-only' — copy that file's choice exactly).

```ts
import { GoogleGenAI } from '@google/genai';
```
  - `export type SummarizeBatchOutcome = { results: Map<string, NewsSummaryResult>; error: string | null; quotaExhausted: boolean }`.
  - `export async function summarizeNewsBatch(items: SummarizeBatchItem[]): Promise<SummarizeBatchOutcome>`:
    1. Key guard: `const apiKey = process.env.GEMINI_API_KEY`; if unset or a labeled placeholder (reuse the exact placeholder-detection idiom from gemini.ts:31 — value falsy or 'your-gemini-api-key') → return { results: empty, error: 'GEMINI_API_KEY not configured', quotaExhausted: false } — honest "not configured", never a throw (telegram/api.ts:30-34 precedent).
    2. `const ai = new GoogleGenAI({ apiKey })` — MUST pass apiKey explicitly; the SDK auto-reads GOOGLE_API_KEY (not GEMINI_API_KEY) so implicit construction silently breaks (06-RESEARCH-external §4 trap — put this in a comment).
    3. One call: `await ai.models.generateContent({ model: NEWS_AI_MODEL, contents: buildSummarizePrompt(items), config: { responseMimeType: 'application/json', responseJsonSchema: NEWS_SUMMARY_JSON_SCHEMA, temperature: 0.2 } })`.
    4. `const parsed = parseSummarizeResponse(response.text, items.map(i => i.id))` → return { ...parsed, quotaExhausted: false }.
    5. catch (err): `classifyAiError(err) === 'quota'` → { results: empty, error: message, quotaExhausted: true }; else quotaExhausted: false with the error message. NEVER rethrow — a summarization failure must not fail ingest (NEWS-05; inner-try/catch house pattern).
No batching/looping here — the caller (06-09 ingest) owns batch sizing and the stop-on-quota loop; this function is one batch = one API call.
  </action>
  <verify>`npx tsc --noEmit` clean; `npm run build` clean; `grep -n "GoogleGenAI" src/lib/news/ai.ts` and `grep -n "apiKey" src/lib/news/ai.ts` show explicit-apiKey construction; `grep -rn "@google/generative-ai" src/lib/news/` returns NOTHING (news module uses only the new SDK).</verify>
  <done>summarizeNewsBatch performs exactly one JSON-mode generateContent call per batch, returns honest not-configured/quota/other outcomes, and never throws.</done>
</task>

</tasks>

<verification>
- `npm run test:news-summarize` green; tsc + build clean.
- Legacy SDK untouched: `git diff HEAD~2 --stat` (this plan's commits) shows no changes to src/lib/gemini.ts, src/lib/ai-provider.ts, or package.json.
- The quota path is reachable: classifyAiError('quota') result flows to quotaExhausted: true in ai.ts (grep `quotaExhausted: true`).
</verification>

<success_criteria>
A batch of matched headlines can be summarized in ONE schema-enforced JSON call with "why it matters" per item; an exhausted budget or unset key yields honest degrade signals the ingest layer can act on — never an exception, never a fabricated summary.
</success_criteria>

<output>
After completion, create `.planning/phases/06-news-pipeline/06-05-SUMMARY.md`.
</output>
