---
phase: 06-news-pipeline
plan: 07
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/lib/news/build-news-message.ts
  - src/lib/news/alert-sweep.ts
  - scripts/news-alert-test.ts
autonomous: true
requirements: [ALRT-04]

must_haves:
  truths:
    - "sweepNewsAlerts enqueues a Telegram outbox row (kind 'news_alert') for every HIGH-importance summarized news item matched to an instrument the user actually HOLDS (derived net quantity > 0 via deriveHoldings — a fully-sold or watch-only instrument never alerts)."
    - "The dedupe key is exactly the shape prescribed at src/lib/alerts/evaluate.ts:102 — news_alert:{userId}:{urlHash} — with urlHash = sha256 of the canonical URL, making the sweep idempotent FOREVER for one (user, article) pair with zero schema state (no notified_at column, no stamping step)."
    - "The message text is fully pre-rendered at enqueue via escapeHtml on EVERY external string (headline, symbols, source — news headlines are attacker-adjacent external text), HTML parse_mode, 4096-char truncated — the dispatcher stays 100% kind-agnostic."
    - "Degradation is honest: unsummarized/degraded items (importance null) NEVER alert — significance requires an AI judgment; when the budget is exhausted there is no significance signal, so there is no alert (documented in-code)."
  artifacts:
    - path: "src/lib/news/build-news-message.ts"
      provides: "pure buildNewsAlertMessage + computeNewsAlertDedupeKey"
      exports: ["buildNewsAlertMessage", "computeNewsAlertDedupeKey"]
    - path: "src/lib/news/alert-sweep.ts"
      provides: "sweepNewsAlerts(admin) — cross-user significant-news → outbox enqueue"
      exports: ["sweepNewsAlerts"]
    - path: "scripts/news-alert-test.ts"
      provides: "node:assert/strict proof of the pure message/key functions"
  key_links:
    - from: "src/lib/news/alert-sweep.ts"
      to: "src/lib/notifications/outbox.ts"
      via: "enqueueNotifications(admin, rows) with kind 'news_alert' — the ONLY sending path (never sendTelegramMessage directly)"
      pattern: "enqueueNotifications"
    - from: "src/lib/news/build-news-message.ts"
      to: "src/lib/telegram/build-message.ts"
      via: "escapeHtml reuse (3-entity, order-correct, tested)"
      pattern: "escapeHtml"
    - from: "src/lib/news/alert-sweep.ts"
      to: "src/lib/portfolio/derive-holdings.ts"
      via: "deriveHoldings per account to compute genuinely-held instruments"
      pattern: "deriveHoldings"
---

<objective>
Implement ALRT-04 as a clone of the proven Phase 5 sweep pattern: a pure HTML message builder + permanent URL-hash dedupe key, and an admin-client sweep that finds recent High-importance summarized news matched to held instruments and enqueues `news_alert` outbox rows — enqueue-only, idempotent by dedupe key, zero outbox migration (the kind is pre-enumerated in both the TS union and the SQL CHECK).

Purpose: the outbox contract (06-RESEARCH-codebase Q3) makes this a thin composition: pre-render text → enqueueNotifications → done. Unlike price alerts there is NO cooldown state to stamp — the URL-hash dedupe key means one article can never notify the same user twice, ever, which is the crash-safe contract by construction.
Output: `src/lib/news/{build-news-message,alert-sweep}.ts` + `scripts/news-alert-test.ts` (script test:news-alert registered by 06-02).
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-news-pipeline/06-RESEARCH-codebase.md
@src/lib/alerts/sweep.ts
@src/lib/telegram/build-message.ts
@src/lib/notifications/types.ts
@src/lib/portfolio/derive-holdings.ts
@.planning/phases/06-news-pipeline/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure message builder + dedupe key (tested)</name>
  <files>src/lib/news/build-news-message.ts, scripts/news-alert-test.ts</files>
  <action>
Create `src/lib/news/build-news-message.ts` (pure, no I/O), mirroring build-message.ts's header conventions (HTML parse_mode ONLY — never legacy markdown; 4096 truncation):
  - `export function buildNewsAlertMessage(input: { displaySymbols: string[]; headline: string; whyItMatters: string | null; source: string; url: string }): string` — shape:
    `📰 <b>{symbols joined ', '}</b>: {headline}` then, when whyItMatters is a non-empty string, `\n{whyItMatters}` then `\n<a href="{url}">{source}</a>`.
    escapeHtml (imported from `@/lib/telegram/build-message` — do NOT re-implement) EVERY interpolated value: symbols, headline, whyItMatters, source. For the href attribute value, escapeHtml is acceptable for Telegram's parser but ALSO strip any `"` characters from the url before interpolation (attribute context; document why). Truncate the final string to 4096.
  - `export function computeNewsAlertDedupeKey(userId: string, canonicalUrl: string): string` — `` `news_alert:${userId}:${createHash('sha256').update(canonicalUrl).digest('hex')}` `` — the EXACT shape prescribed by evaluate.ts:102. Not time-bucketed on purpose: one article per user, forever (replicate the reasoning as a comment).

Create `scripts/news-alert-test.ts` (node:assert/strict, tsx — this plan is execute-type, but the pure functions still get the house proof):
  1. Message contains 📰, bold symbols, headline; a headline containing `<script>&` arrives entity-escaped (`&lt;script&gt;&amp;`), & escaped FIRST (no double-escape).
  2. whyItMatters null → no orphan blank line; non-empty → present, escaped.
  3. A url containing `"` interpolates without the quote; the anchor tag closes correctly.
  4. A 5000-char headline → output length exactly 4096.
  5. Dedupe key: matches /^news_alert:[^:]+:[0-9a-f]{64}$/; same (user, url) → identical; different url or user → different; determinism across calls.
Run `npm run test:news-alert` — MUST pass (write implementation and test together in this task; commit as one feat commit — this is not a TDD plan, but never commit with the test failing). `npx tsc --noEmit` clean. Commit: `feat(06-07): news alert message builder + permanent dedupe key` (explicit pathspec).
  </action>
  <verify>`npm run test:news-alert` green; `npx tsc --noEmit` clean; `grep -n "escapeHtml" src/lib/news/build-news-message.ts` shows the import from '@/lib/telegram/build-message' (no local re-implementation).</verify>
  <done>Pre-rendered, escaped, truncated news-alert HTML and the prescribed news_alert:{userId}:{urlHash} key exist as pure tested functions.</done>
</task>

<task type="auto">
  <name>Task 2: sweepNewsAlerts — cross-user significant-news enqueue</name>
  <files>src/lib/news/alert-sweep.ts</files>
  <action>
Create `src/lib/news/alert-sweep.ts` as a structural clone of `src/lib/alerts/sweep.ts` (copy its header discipline: accepts an ALREADY-CONSTRUCTED admin client, never builds one; admin because the sweep is cross-user; replicate the enqueue-first/dedupe-backstop reasoning comment adapted to the stateless news case):

`export async function sweepNewsAlerts(admin: SupabaseClient): Promise<{ candidates: number; enqueued: number }>`:
  1. Load recent significant items: `admin.from('news_items').select('id, headline, url, source, summary, importance, published_at, news_item_instruments ( instrument_id )').eq('summary_status', 'summarized').eq('importance', 'High').gte('published_at', <now - NEWS_ALERT_RECENCY_HOURS>)` with `const NEWS_ALERT_RECENCY_HOURS = 48` (module constant, commented: prevents alert storms from historical backfill on first run; the dedupe key already prevents repeats — this bounds first-contact volume). Items with no join rows → skip.
     Honest-degradation comment: importance is ONLY ever set by successful AI summarization, so degraded/pending items can never reach this query — no AI budget ⇒ no significance signal ⇒ no alert (ALRT-04 requires "significant", and we refuse to fabricate significance).
  2. Load the holdings universe: `admin.from('transactions').select('account_id, instrument_id, transaction_type, quantity, price, transaction_date, investment_accounts ( user_id )')`. Normalize the to-one relation with a local `firstOf` (sweep.ts:57-60). Group transactions per account_id, map each into the `Transaction` shape `deriveHoldings` expects (from `@/lib/portfolio/derive-holdings` + `@/lib/types` — both IMPORT-ONLY; types.ts is dirty, do not edit), and derive per-account held instrument ids (net quantity > 0 — deriveHoldings omits sold-out positions by design). Build userId → Set<heldInstrumentId> (via the account's user_id) and a held-instrument display map from `admin.from('instruments').select('id, symbol')` for the involved ids.
  3. For each (item × user) where the item's matched instrument_ids intersect the user's held set:
     (a) pre-render: `buildNewsAlertMessage({ displaySymbols: <held∩matched symbols sorted>, headline, whyItMatters: item.summary ?? null, source, url })` — the stored summary column already holds the composed 'summary… Why it matters: …' text (06-09 writes it as one column), so it IS the message's context line; null (shouldn't occur for a summarized row, defensive) → the builder omits the line. Keep this decision in one comment.
     (b) `enqueueNotifications(admin, [{ userId, kind: 'news_alert', payload: { text, newsItemId: item.id, url: item.url }, dedupeKey: computeNewsAlertDedupeKey(userId, item.url) }])` — enqueue is the ONLY write; there is deliberately NO step (c): no stamp, no state — the permanent URL-hash dedupe key absorbs every re-sweep (`ignoreDuplicates` upsert). Count enqueued.
  4. Return honest counts { candidates, enqueued } (enqueued counts enqueue CALLS — the DB may silently absorb duplicates; note this honestly in the return type's doc comment).
No dispatch here — the caller (06-09 route) composes dispatchOutbox after the sweep, mirroring how price refresh composes evaluateAndEnqueueAlerts + dispatchOutbox.
  </action>
  <verify>`npx tsc --noEmit` clean; `npm run build` clean; `grep -n "sendTelegramMessage" src/lib/news/alert-sweep.ts` returns NOTHING (outbox-only); `grep -n "createAdminClient" src/lib/news/alert-sweep.ts` returns NOTHING (client injected); `grep -n "deriveHoldings" src/lib/news/alert-sweep.ts` present.</verify>
  <done>sweepNewsAlerts enqueues pre-rendered news_alert rows for held-instrument High-importance news with permanent idempotence and no schema state; commit pathspec-scoped.</done>
</task>

</tasks>

<verification>
- `npm run test:news-alert` green; tsc + build clean.
- Kind correctness: `grep -n "kind: 'news_alert'" src/lib/news/alert-sweep.ts` present (pre-enumerated in notifications/types.ts:8 + the SQL CHECK — confirm NO migration was added by this plan).
- Both commits touched only this plan's three files.
</verification>

<success_criteria>
ALRT-04's write path is complete and statically verified: significant (AI-judged High) news on a genuinely-held ticker becomes a durable, deduped, pre-rendered outbox row that the untouched Phase 5 dispatcher can deliver; live delivery is exercised at the 06-10 checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/06-news-pipeline/06-07-SUMMARY.md`.
</output>
