---
phase: 06-news-pipeline
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/news/match.ts
  - scripts/news-match-test.ts
autonomous: true
requirements: [NEWS-02]

must_haves:
  truths:
    - "matchInstruments matches a symbol ONLY on a case-sensitive whole-token hit (custom lookaround boundaries, not bare \\b — symbols like M&M, BAJAJ-AUTO, BRK.B contain regex specials and \\b misfires next to & and .): INFY matches 'INFY jumps 4%' but never 'INFYX' or 'infy'."
    - "matchInstruments matches a company name case-INSENSITIVELY on the FULL legal-suffix-stripped name as a phrase: 'Infosys Ltd' matches 'Infosys shares rally'; 'Tata Motors Ltd' matches 'tata motors unveils' but NOT a text containing only 'Tata'."
    - "Known false-positive traps are pinned by tests: symbol 'LT' does not match 'Lt. Governor' (case-sensitivity), symbol 'TCS' does not match 'attics' (boundaries), and candidates whose stripped name is shorter than 3 chars never name-match."
    - "The function is pure (string + candidates in, matches out), declares its OWN local MatchCandidate input type (no import from src/lib/news/types.ts — that file belongs to the concurrently-running plan 06-02), and is proven RED→GREEN by scripts/news-match-test.ts."
  artifacts:
    - path: "src/lib/news/match.ts"
      provides: "word-boundary/company-name instrument matcher + exported stripCompanySuffixes helper"
      exports: ["matchInstruments", "stripCompanySuffixes"]
    - path: "scripts/news-match-test.ts"
      provides: "node:assert/strict proof incl. false-positive traps"
  key_links:
    - from: "scripts/news-match-test.ts"
      to: "src/lib/news/match.ts"
      via: "direct import, tsx-run via npm run test:news-match (script registered by 06-02)"
      pattern: "from '\\.\\./src/lib/news/match'|@/lib/news/match"
---

<objective>
Build the NEWS-02 instrument matcher as a pure, TDD-proven function: given article text and a list of portfolio instruments (symbol + display name), return which instruments the text genuinely mentions — word-boundary rules for symbols, full-phrase rules for company names — with the false-positive traps that motivated the requirement pinned as tests.

Purpose: the market-wide Indian publisher feeds (ET Markets, LiveMint — live-verified in 06-RESEARCH-external §3) are firehoses; the matcher is the only thing standing between "portfolio intelligence" and noise. The naive substring `crossReferenceHoldings` in gemini.ts is explicitly NOT sufficient precedent (06-RESEARCH-codebase Q6).
Output: `src/lib/news/match.ts` + `scripts/news-match-test.ts` (npm script test:news-match was registered by 06-02).
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-news-pipeline/06-RESEARCH-codebase.md
@.planning/phases/06-news-pipeline/06-RESEARCH-external.md
@src/lib/alerts/evaluate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD — word-boundary/company-name matcher (RED then GREEN)</name>
  <files>src/lib/news/match.ts, scripts/news-match-test.ts</files>
  <action>
Write `scripts/news-match-test.ts` FIRST (RED — house pattern: node:assert/strict, tsx, grouped cases, exit 1 on failure). NOTE: `npm run test:news-match` may not exist yet if the concurrent 06-02 executor hasn't landed package.json — run `npx tsx scripts/news-match-test.ts` directly in that case; do NOT edit package.json (06-02 owns it).

Test cases to pin (candidates modeled on the real seed instruments — INFY/Infosys Ltd (NSE), TCS/Tata Consultancy Services Ltd, LT/Larsen & Toubro Ltd, M&M/Mahindra & Mahindra Ltd, AAPL/Apple Inc (NASDAQ), TSLA/Tesla Inc):
  1. Symbol hit: 'INFY jumps 4% after results' → INFY via 'symbol'.
  2. Symbol boundary: 'INFYX Corp lists' and 'reinfy' → NO match. 'attics look great' → NO TCS match.
  3. Symbol case-sensitivity: 'infy gains' → NO symbol match; 'Lt. Governor speaks' → NO LT match; 'L&T wins order, LT stock up' → LT matches (token 'LT' present).
  4. Regex specials in symbols: candidate M&M — 'M&M launches new SUV' matches; 'MM launches' does not. (Boundaries must be custom lookarounds `(?<![A-Za-z0-9])sym(?![A-Za-z0-9])` with the symbol regex-escaped; bare \b around '&' misbehaves — this is the case that catches it.)
  5. Company name: 'Infosys shares rally on buyback' → INFY via 'company-name' (from 'Infosys Ltd' with suffix stripped). 'Tata Consultancy Services wins deal' → TCS. Case-insensitive: 'APPLE unveils…' → AAPL via company-name ('Apple Inc' → 'Apple').
  6. Full-phrase rule: candidate 'Tata Motors Ltd' (add to candidates for this case) — text 'Tata group expands' does NOT match Tata Motors (only the full phrase 'tata motors' counts).
  7. Min-length guard: a candidate whose stripped name is < 3 chars never name-matches.
  8. Multi-match + dedup: 'Infosys and TCS both up; INFY leads' → exactly one entry each for INFY (symbol beats company-name when both hit — matchedVia 'symbol') and TCS.
  9. Purity/determinism: same inputs twice → deeply-equal results; empty text or empty candidates → [].
Run it — MUST fail (module missing). Commit RED: `test(06-03): add failing instrument-matcher tests` with explicit pathspec.

Then write `src/lib/news/match.ts` (GREEN):
  - Declare LOCALLY (do not import from src/lib/news/types.ts — concurrent 06-02 owns that file; wave-1 isolation): `export interface MatchCandidate { instrumentId: string; symbol: string; displayName: string }` and return type `{ instrumentId: string; matchedVia: 'symbol' | 'company-name' }[]`.
  - `export function stripCompanySuffixes(name: string): string` — case-insensitively strip trailing legal tokens (repeat until stable): Ltd, Limited, Inc, Incorporated, Corp, Corporation, Plc, Co, Company, and a trailing '&' left dangling; also strip trailing periods/commas. Exported because 06-08 reuses it to build Google News queries.
  - `export function matchInstruments(text: string, candidates: MatchCandidate[]): InstrumentMatchResult[]` — for each candidate: (a) symbol rule: build `new RegExp('(?<![A-Za-z0-9])' + escapeRegex(symbol) + '(?![A-Za-z0-9])')` — NO 'i' flag (case-sensitive); (b) name rule: stripped name, skip if < 3 chars, `new RegExp('(?<![A-Za-z0-9])' + escapeRegex(strippedName).replace(/\s+/g, '\\s+') + '(?![A-Za-z0-9])', 'i')`. Symbol match wins the matchedVia label when both hit. One result per candidate max. Include a private `escapeRegex` helper (standard `[.*+?^${}()|[\]\\]` escape). Header comment: honest limitation note — single-common-word names ('Apple') can false-positive on generic text; accepted because queries are already portfolio-scoped and NEWS-02's bar is word-boundary/company-name rules, not NER.
Run tests — MUST pass. `npx tsc --noEmit` clean. Commit GREEN: `feat(06-03): implement word-boundary instrument matcher` with explicit pathspec.
  </action>
  <verify>`npx tsx scripts/news-match-test.ts` (or `npm run test:news-match` once 06-02 landed) passes all groups; `npx tsc --noEmit` clean; `git show HEAD --stat` shows only this plan's two files across the RED/GREEN commits.</verify>
  <done>matchInstruments enforces case-sensitive whole-token symbol matches and case-insensitive full-phrase name matches with the trap cases pinned; stripCompanySuffixes is exported for 06-08's query builder.</done>
</task>

</tasks>

<verification>
- All test groups green; tsc clean.
- `grep -n "from '@/lib/news/types'" src/lib/news/match.ts` returns NOTHING (wave-1 file isolation held).
- Both commits touched only files_modified.
</verification>

<success_criteria>
A pure matcher exists that later plans can feed (title + abstract) text and portfolio candidates into, returning per-instrument matches labeled 'symbol'/'company-name', with documented false-positive limits and the misfire traps regression-pinned.
</success_criteria>

<output>
After completion, create `.planning/phases/06-news-pipeline/06-03-SUMMARY.md`.
</output>
