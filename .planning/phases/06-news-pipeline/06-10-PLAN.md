---
phase: 06-news-pipeline
plan: 10
type: execute
wave: 5
depends_on: ["06-01", "06-06", "06-09"]
files_modified: []
autonomous: false
requirements: [NEWS-01, NEWS-02, NEWS-03, NEWS-04, NEWS-05, ALRT-04]
user_setup:
  - service: finnhub
    why: "US company-news fetch needs a real (free) API key"
    env_vars:
      - name: FINNHUB_API_KEY
        source: "finnhub.io — create free account → dashboard shows the key → replace the placeholder in .env.local"
  - service: supabase
    why: "Three authored-but-unapplied migrations must be pushed (consent-gated)"
    dashboard_config:
      - task: "Consent to selective `supabase db push` of csv_import + alerts_telegram + news_pipeline migrations ONLY (never price_refresh_cron)"
        location: "CLI: supabase login && supabase link --project-ref ozkorwkhtamyaavuphhm"

must_haves:
  truths:
    - "Every key-gated / live-DB check for Phase 6 is either genuinely executed and its REAL output recorded, or explicitly recorded as DEFERRED with the exact reason — never fabricated (03-06/04-07/05-09 precedent)."
    - "Before pausing for the user, every check Claude CAN run now has been re-run and recorded: tsc, build, all five test:news-* scripts, the keyless live fetch smoke, and the 401 guard smoke."
  artifacts: []
  key_links: []
---

<objective>
The Phase-6-closing live-verification checkpoint: funnel EVERY consent-gated, key-gated, or live-DB check into one place — migration push, Finnhub key, live Gemini summarization, the real /news feed, and the Telegram news alert — mirroring 05-09's structure and its honest-DEFERRED discipline.

Purpose: the dev machine has no Docker and live pushes are consent-gated (standing memory + STATE.md STILL-OPEN items 6/7). All Phase 6 code is designed to be statically verifiable without these; this plan is where reality gets exercised, or where deferral gets recorded honestly.
Output: 06-10-SUMMARY.md recording verified-vs-deferred, plus updated live-state notes.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-news-pipeline/06-RESEARCH-external.md
@.planning/phases/05-alerts-telegram/05-09-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint honest-state sweep (everything Claude can run alone)</name>
  <files></files>
  <action>
Re-run and record VERBATIM results for: `npx tsc --noEmit`; `npm run build` (confirm /news and /api/news/refresh in the route table); `npm run test:news-dedupe`, `test:news-match`, `test:news-parse`, `test:news-summarize`, `test:news-alert`; the regression set `test:telegram`, `test:alerts`, `test:import-parse`; `npm run test:rls` (EXPECT honest FAIL at `import_batches not found` while migrations remain unpushed — record the actual failure point); the keyless live fetch smoke from 06-08 (Google News + ET + LiveMint through real wrappers, >0 items each); and the unauthorized 401 smoke on POST /api/news/refresh. Any unexpected failure BLOCKS the checkpoint — fix or record before involving the user.
  </action>
  <verify>All runnable checks recorded with real output; no fabricated pass.</verify>
  <done>The honest current state is fully documented before any user-gated step.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User-gated setup — Finnhub key + migration-push consent</name>
  <what-built>The full Phase 6 pipeline (schema, dedup, matching, fetch, AI summaries, feed UI, news→Telegram alerts) is code-complete and statically verified. Two things only a human can provide remain.</what-built>
  <how-to-verify>
1. FINNHUB key (2 min): create a free account at finnhub.io → copy the API key from the dashboard → replace the FINNHUB_API_KEY placeholder in .env.local. (Skippable: without it, US news is skipped with an honest source error; Indian news still works.)
2. Migration push consent: reply approving `supabase db push` restricted to EXACTLY these pending migrations: 20260715230011_csv_import.sql, 20260716221450_alerts_telegram.sql, and the Phase 6 news migration from 06-01 — and NEVER 20260714220438_price_refresh_cron.sql (deploy-gated; STATE.md STILL-OPEN item 1). Requires `supabase login` + `supabase link --project-ref ozkorwkhtamyaavuphhm` if not done.
3. (Optional, for the ALRT-04 live leg) If you have since created the Telegram bot from 05-09, confirm TELEGRAM_BOT_TOKEN is set; otherwise the news-alert delivery leg will be recorded DEFERRED exactly like 05-09.
  </how-to-verify>
  <resume-signal>Reply with: key added (or 'skip finnhub'), push approved (or 'defer'), bot status. On 'defer', record ALL live legs as DEFERRED in the SUMMARY (03-06/04-07/05-09 precedent) and close the phase honestly.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live E2E — ingest, feed, dedup idempotency, degradation, news alert</name>
  <what-built>Runs only with Task 2's consent. Claude automates every step below and pauses for your visual confirmation of the feed.</what-built>
  <how-to-verify>
Claude will (recording real outputs at each step): (a) push the three approved migrations selectively, then `npm run test:rls` — must now PASS end-to-end including the news_item_instruments write-hole checks; (b) POST /api/news/refresh with the local NEWS_REFRESH_SECRET — expect real itemsSeen/itemsNew counts, sourceErrors honestly listing any unconfigured source; (c) verify NEWS-04 live: pending rows became summarized with non-empty summaries + "Why it matters", re-POST → summarizedNow 0 for those rows (never regenerated) and itemsDuplicate > 0 (idempotent dedup); (d) you then open /news in the browser: portfolio-filtered items, newest first, source + timestamp visible, headline-only rendering for any unsummarized rows; confirm what you see; (e) ALRT-04 leg if a bot exists: a High-importance held-ticker item produced exactly one Telegram message (dedupe key absorbs the re-run) — else DEFERRED; (f) NEWS-05 spot-check: if any batch hit 429 during (b), confirm the run still returned success with aiDegraded true — if quota never triggered, record 'not exercised (quota not hit)' honestly, do NOT simulate.
  </how-to-verify>
  <resume-signal>Type "approved" if the feed matches your portfolio honestly, or describe any discrepancy.</resume-signal>
</task>

</tasks>

<verification>
06-10-SUMMARY.md lists every check as VERIFIED (with real output) or DEFERRED (with reason + resume path), and STATE.md's still-open items are updated accordingly by the orchestrator.
</verification>

<success_criteria>
Phase 6's success criteria are live-proven where consent/keys exist, and every deferred leg is recorded with an exact resume path — no silent gaps, no fabricated verification.
</success_criteria>

<output>
After completion, create `.planning/phases/06-news-pipeline/06-10-SUMMARY.md`.
</output>
