---
phase: 06-news-pipeline
plan: 06
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/lib/news/read.ts
  - src/app/(dashboard)/news/page.tsx
autonomous: true
requirements: [NEWS-03, NEWS-05]

must_haves:
  truths:
    - "The /news page renders REAL persisted news filtered to the caller's portfolio (held + watched instruments), newest first, each with source and timestamp — the hardcoded news={[]} is gone."
    - "The read path uses the COOKIE-BOUND client (RLS-scoped) end to end — never the admin client — via getHoldings/getWatchlist from the dirty src/lib/supabase/portfolio.ts (IMPORT-ONLY, file not modified)."
    - "Headline-only degradation is honest: a row with null summary renders with an empty summary line (never fabricated text), and a null sentiment_label maps to 'Neutral' — documented in-code as 'no directional signal analyzed', the only honest value the required display enum admits."
    - "src/lib/types.ts is NOT touched: the display NewsItem is imported as-is; category is computed per-user at read time (held → 'Holdings', else watched → 'Watchlist'); the inert Filters/Preferences toolbar buttons are removed (honest removal — their wiring moment arrived and per-item filtering already lives in NewsFeed's chips)."
  artifacts:
    - path: "src/lib/news/read.ts"
      provides: "getNewsFeed(supabase, accountId): Promise<NewsItem[]> — cookie-client portfolio-filtered feed read"
      exports: ["getNewsFeed"]
    - path: "src/app/(dashboard)/news/page.tsx"
      provides: "auth-guarded RSC wiring getNewsFeed into <NewsFeed news={...} />"
      contains: "getNewsFeed"
  key_links:
    - from: "src/app/(dashboard)/news/page.tsx"
      to: "src/lib/news/read.ts"
      via: "server-side await getNewsFeed(supabase, accountId)"
      pattern: "getNewsFeed"
    - from: "src/lib/news/read.ts"
      to: "news_item_instruments + news_items"
      via: "cookie-client select joined through the 06-01 join table, .in() on the user's instrument ids"
      pattern: "news_item_instruments"
---

<objective>
Wire the already-real `/news` RSC and `NewsFeed` component to a real, RLS-scoped read layer: portfolio-filtered (held ∪ watched), newest-first, source + timestamp, with honest headline-only rendering for unsummarized/degraded rows.

Purpose: NEWS-03 is entirely a read-side requirement and needs nothing from the fetch/AI plans — the tables from 06-01 (empty until ingest runs) plus the existing honest empty state make this independently shippable in wave 2. NewsFeed.tsx already renders the exact display shape; only the data source is missing (06-RESEARCH-codebase Q2).
Output: `src/lib/news/read.ts` + a rewired `src/app/(dashboard)/news/page.tsx`.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-news-pipeline/06-RESEARCH-codebase.md
@src/app/(dashboard)/news/page.tsx
@src/components/dashboard/NewsFeed.tsx
@src/lib/supabase/portfolio.ts
@.planning/phases/06-news-pipeline/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: getNewsFeed read layer (cookie client, portfolio-filtered, newest first)</name>
  <files>src/lib/news/read.ts</files>
  <action>
Create `src/lib/news/read.ts` modeled on `src/lib/alerts/read.ts` conventions (accepts an already-constructed cookie-bound SupabaseClient; never constructs one; never the admin client — news_items/news_item_instruments carry authenticated SELECT policies precisely so this read is RLS-clean).

`export async function getNewsFeed(supabase: SupabaseClient, accountId: string): Promise<NewsItem[]>`:
  1. In parallel (Promise.all): `getHoldings(supabase, accountId)` and `getWatchlist(supabase, accountId)` from `@/lib/supabase/portfolio` — IMPORT ONLY; that file is dirty with unrelated uncommitted work and MUST NOT be edited (06-RESEARCH-codebase Q5 dirty-file rule).
  2. Build heldIds (Set of holding.instrumentId), watchedIds (Set of watchlist instrumentId), and a tickerById Map (instrumentId → display ticker) from both lists. If both are empty → return [] immediately (honest empty portfolio ⇒ empty feed).
  3. Query: `supabase.from('news_item_instruments').select('instrument_id, news_items ( id, headline, summary, url, source, published_at, sentiment_label )').in('instrument_id', [...heldIds, ...watchedIds])`. Normalize the to-one `news_items` relation with a local `firstOf` helper (object-or-array quirk — sweep.ts:57-60 precedent).
  4. Group rows by news item id (one article matching several portfolio instruments appears ONCE), collecting the matched instrument ids per article.
  5. Map each article to the display `NewsItem` (imported from `@/lib/types` — IMPORT ONLY, do not touch that dirty file):
     - id, title: headline, url, source: source ?? 'Unknown', publishedAt: published_at,
     - sentiment: sentiment_label ?? 'Neutral' — comment: null means "not analyzed / no directional signal"; 'Neutral' is the only honest value the required Sentiment enum admits; NEVER map null to Bullish/Bearish/Mixed,
     - summary: summary ?? '' — headline-only degradation (NEWS-05): empty string renders as a blank line, never fabricated text,
     - tickers: matched instrument ids → tickerById lookups (portfolio display symbols only; skip ids without a ticker),
     - category: any matched id in heldIds → 'Holdings'; else → 'Watchlist'. ('Macro' remains honestly unreachable — no macro source is in scope this phase; note it in a comment.)
  6. Sort newest-first by publishedAt desc (String compare on ISO is insufficient if timezones vary — compare `new Date(...).getTime()`), cap at 100 items.
  </action>
  <verify>`npx tsc --noEmit` clean; `grep -n "createAdminClient" src/lib/news/read.ts` returns NOTHING; `grep -n "from '@/lib/supabase/portfolio'" src/lib/news/read.ts` shows import-only usage; `git status --short src/lib/supabase/portfolio.ts src/lib/types.ts` shows both files still ONLY carrying their pre-existing unrelated modifications (no new hunks from this plan).</verify>
  <done>getNewsFeed returns portfolio-filtered NewsItem[] newest-first from the cookie client, with honest null-handling for summary/sentiment and per-user category derivation.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire /news page (real data in, inert buttons out)</name>
  <files>src/app/(dashboard)/news/page.tsx</files>
  <action>
Edit `src/app/(dashboard)/news/page.tsx` in place (05-08 /alerts rewrite-in-place precedent, but surgical — the page is already a correct auth-guarded RSC):
  1. Import getNewsFeed; after the existing `getAccountId` call, fetch `const [watchlist, news] = await Promise.all([getPricedWatchlist(supabase, accountId), getNewsFeed(supabase, accountId)])`.
  2. Replace `<NewsFeed news={[]} />` with `<NewsFeed news={news} />` and DELETE the now-false "no real source yet" comment block (lines 8-18 header comment: rewrite to state the feed reads persisted news_items matched to the caller's portfolio; keep the middleware defense-in-depth note).
  3. REMOVE the two inert toolbar buttons (Filters, Settings2/Preferences, lines 48-57) and their now-unused lucide imports — honest removal: they were left "pending their own future wiring" (02-06), the wiring moment is now, and the only filtering this phase ships is NewsFeed's own working chips. Do not add new dead affordances.
  4. Leave NewsFeed.tsx, WatchlistTable, and the "Why this matters" card untouched.
  </action>
  <verify>`npx tsc --noEmit` clean; `npm run build` clean with `/news` in the route table; `grep -n "news={\[\]}" src/app/(dashboard)/news/page.tsx` returns NOTHING; `grep -nE "Filter|Settings2" src/app/(dashboard)/news/page.tsx` returns NOTHING; dev-server smoke: page renders the honest empty state when tables are empty/absent locally (an error from a missing news_item_instruments table pre-migration-push must NOT crash the page — if the query errors, getNewsFeed should return [] with a console.warn, matching the honest-degradation rule; add that guard in read.ts if the smoke test shows a crash).</verify>
  <done>/news renders real portfolio-filtered news (or the honest empty state), the dead toolbar buttons are gone, and no client component needed changes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run build` clean; `/news` listed in build output.
- Per-task commits pathspec-scoped; `git show HEAD --stat` confirms only this plan's two files across its commits.
- src/lib/types.ts and src/lib/supabase/portfolio.ts diffs unchanged from before this plan ran.
</verification>

<success_criteria>
NEWS-03 observable truths hold as far as statically verifiable now: the feed reads persisted news filtered to the caller's holdings+watchlist, newest first, with source + timestamp; unsummarized rows show headline-only; empty tables show the existing honest empty state. Live click-through with real ingested rows lands at the 06-10 checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/06-news-pipeline/06-06-SUMMARY.md`.
</output>
