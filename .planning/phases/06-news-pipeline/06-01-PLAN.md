---
phase: 06-news-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260717120000_news_pipeline.sql
  - scripts/rls-isolation-test.ts
autonomous: true
requirements: [NEWS-02, NEWS-04]

must_haves:
  truths:
    - "A NEW timestamped migration (never edits an existing one — house rule from the 20260714220333 / 20260716221450 headers) extends the Phase-1 news_items table for Phase 6: it ADDs title_hash (normalized-title dedup, NEWS-02), summary_status ('pending'|'summarized'|'degraded', default 'pending') and summarized_at (NEWS-04 persist-once state) — it does NOT drop or re-create news_items and does NOT touch its RLS policies."
    - "news_items keeps its EXACT Phase-1 RLS posture: authenticated SELECT allowed, NO authenticated write policy (writes are service-role only). The migration adds no permissive write policy — the closed posture the rls_fixes migration deliberately established and that scripts/rls-isolation-test.ts check 4 asserts."
    - "A new news_item_instruments join table (news_item_id → news_items, instrument_id → instruments, both ON DELETE CASCADE, composite PK) links one article to many portfolio instruments with FK integrity (the shape that fixes the affected_symbols TEXT[] pre-Phase-2-identity flaw — INFY-NSE vs INFY-NYSE now distinct). It carries the SAME closed RLS posture as news_items: authenticated SELECT USING(true), no authenticated write policy."
    - "Dedup is DB-backstopped: a partial UNIQUE index on title_hash WHERE title_hash IS NOT NULL (mirrors uniq_notifications_outbox_dedupe / uniq_transactions_import_row_hash), alongside the pre-existing url UNIQUE constraint — the two NEWS-02 dedup keys."
    - "scripts/rls-isolation-test.ts is EXTENDED (not rewritten) with a news_item_instruments block mirroring the existing news_items check: an authenticated (anon-key+JWT) INSERT is REJECTED and an authenticated SELECT still works. npx tsc --noEmit stays clean."
  artifacts:
    - path: "supabase/migrations/20260717120000_news_pipeline.sql"
      provides: "ALTER news_items (title_hash, summary_status, summarized_at) + title_hash partial-unique index + news_item_instruments join table with closed RLS + indexes"
      contains: "news_item_instruments"
    - path: "scripts/rls-isolation-test.ts"
      provides: "two-user isolation coverage extended to news_item_instruments (authenticated INSERT rejected, SELECT works)"
      contains: "news_item_instruments"
  key_links:
    - from: "supabase/migrations/20260717120000_news_pipeline.sql"
      to: "public.news_items + public.instruments"
      via: "ALTER TABLE ADD COLUMN on news_items; new news_item_instruments with FKs to both, RLS enabled, no authenticated write policy"
      pattern: "ALTER TABLE public.news_items"
    - from: "scripts/rls-isolation-test.ts"
      to: "news_item_instruments write-hole assertion"
      via: "authenticated INSERT must error; authenticated SELECT must succeed"
      pattern: "news_item_instruments"
---

<objective>
Extend the pre-provisioned Phase-1 `news_items` table (symbol-keyed, mock-era, applied live with zero rows) for the Phase 6 pipeline via a NEW migration, and add a `news_item_instruments` join table that links articles to real ISIN+exchange instrument identity with FK integrity. This is the schema foundation the dedup (NEWS-02) and summarization-state (NEWS-04) work builds on.

Purpose: NEWS-02 needs a normalized-title-hash dedup key (the table only has `url UNIQUE`) and instrument-identity linkage (the table only has bare `affected_symbols TEXT[]` — the exact pre-Phase-2 flaw `price_cache` was re-keyed to fix). NEWS-04 needs an honest "summarized/pending/degraded" state so summaries persist and are never regenerated. All of this must preserve the deliberately-closed service-role-write-only RLS posture that the rls_fixes migration established and `test:rls` guards.
Output: one new migration file + an extended RLS isolation test. Live apply is consent-gated and DEFERRED to the 06-10 checkpoint (STATE.md STILL-OPEN items 6/7 — csv_import + alerts_telegram are also still pending; a push must remain selective).
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-news-pipeline/06-RESEARCH-codebase.md
@supabase/migrations/20260716221450_alerts_telegram.sql
@scripts/rls-isolation-test.ts

# The current news_items DDL + its two policy migrations (read before writing):
# supabase/migrations/20260714032952_initial_schema.sql (lines 154-171)
# supabase/migrations/20260714032957_rls_fixes.sql (line 11 drops the news insert hole)
</context>

<tasks>

<task type="auto">
  <name>Task 1: New migration — extend news_items + news_item_instruments join table (closed RLS preserved)</name>
  <files>supabase/migrations/20260717120000_news_pipeline.sql</files>
  <action>
Create ONE new timestamped migration (never edit an existing migration — house rule stated in the 20260714220333 and 20260716221450 headers). If a migration with a later timestamp than 20260716221450 already exists, bump this filename's timestamp so it sorts last. Structure it after the 20260716221450_alerts_telegram.sql precedent (header comment explaining what/why, then DDL).

1. ALTER the EXISTING public.news_items (do NOT drop/recreate it — it is applied live with zero rows; ADD-column is safe without a backfill, exactly the argument the 20260714220333 price_cache re-key made):
   - ADD COLUMN title_hash TEXT;                       -- NEWS-02 normalized-title dedup key (sha256, set by the ingest writer)
   - ADD COLUMN summary_status TEXT NOT NULL DEFAULT 'pending' CHECK (summary_status IN ('pending','summarized','degraded'));  -- NEWS-04/05 state
   - ADD COLUMN summarized_at TIMESTAMPTZ;             -- when the summary was written; NEVER re-summarize a 'summarized' row (NEWS-04)
   Do NOT alter, drop, or re-create any news_items RLS policy. Do NOT add an authenticated INSERT/UPDATE/DELETE policy. The service-role-only write posture (authenticated SELECT USING(true); no write policy — the hole the rls_fixes migration deliberately closed) MUST remain byte-for-byte intact. Leave affected_symbols/sentiment/sentiment_label/importance columns as-is (still used: affected_symbols stays NOT NULL, the ingest writer supplies it).

2. Add the NEWS-02 dedup backstop index (partial-unique, same philosophy as uniq_notifications_outbox_dedupe and uniq_transactions_import_row_hash — non-null keys only, so pre-existing null rows never collide):
   CREATE UNIQUE INDEX IF NOT EXISTS uniq_news_items_title_hash ON public.news_items(title_hash) WHERE title_hash IS NOT NULL;
   (url is already UNIQUE from the initial schema — the two NEWS-02 dedup keys are url + title_hash.)

3. CREATE TABLE public.news_item_instruments — the article↔instrument join that gives news real ISIN+exchange identity (the join-table shape the research recommends: the only shape that supports "one article, many instruments" with FK integrity):
   - news_item_id UUID REFERENCES public.news_items(id) ON DELETE CASCADE NOT NULL
   - instrument_id UUID REFERENCES public.instruments(id) ON DELETE CASCADE NOT NULL
   - matched_via TEXT   -- optional provenance: 'symbol' | 'company-name' (honest audit of the match rule; nullable)
   - created_at TIMESTAMPTZ DEFAULT NOW()
   - PRIMARY KEY (news_item_id, instrument_id)
   ENABLE ROW LEVEL SECURITY. Add ONLY the closed posture (copy news_items' shape exactly): one authenticated SELECT policy USING (TRUE); NO authenticated write policy (writes are service-role only). Add a comment stating the closed posture is deliberate, do NOT add a permissive write policy (the exact caution repeated in the instruments/fx/alerts migration headers).
   Add helper indexes: CREATE INDEX ON public.news_item_instruments(instrument_id) (the read path filters by the user's held/watched instrument_ids); the composite PK already covers news_item_id lookups.
  </action>
  <verify>
`grep -n "ALTER TABLE public.news_items" supabase/migrations/20260717120000_news_pipeline.sql` shows only ADD COLUMN (no DROP/CREATE of news_items, no news_items policy statement); `grep -nE "title_hash|summary_status|summarized_at|news_item_instruments|uniq_news_items_title_hash" ...` all present; `grep -niE "policy .* insert .* news_item|policy .* update .* news_item|policy .* delete .* news_item" ...` returns NOTHING (no authenticated write policy on either table); `git diff --stat` shows exactly ONE new migration file and ZERO existing migrations changed.
  </verify>
  <done>The migration ALTERs news_items (title_hash + summary_status + summarized_at) with the title_hash partial-unique index, creates news_item_instruments with FK-integrity to instruments and the closed service-role-write RLS posture, edits no existing migration, and adds no authenticated write policy to either table.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the two-user RLS isolation test for news_item_instruments</name>
  <files>scripts/rls-isolation-test.ts</files>
  <action>
EXTEND (do not rewrite) scripts/rls-isolation-test.ts, mirroring the existing news_items write-hole block (check 4, around lines 185-198). Add a news_item_instruments block that asserts the closed posture:
  - An authenticated client (anon key + a real user JWT — the same `a` client the existing checks use, NOT the service role) attempting `.from('news_item_instruments').insert({ news_item_id: <any uuid>, instrument_id: <any uuid> })` MUST return an error (write hole closed). Throw a FAIL with a clear message if no error.
  - An authenticated `.from('news_item_instruments').select('news_item_id').limit(1)` MUST still succeed (read policy kept). Throw a FAIL if it errors.
Follow the file's existing style exactly (same `if (!err) throw new Error('FAIL: ...')` shape, same reuse of the already-created `a` client). Do NOT restructure the file or reorder existing checks — append the new block alongside the other closed-table checks. Since instrument/news uuids need not exist for an INSERT that RLS rejects before row validation, literal uuids (e.g. two `gen_random_uuid()`-shaped constants) are fine.
Note: this test cannot pass live until BOTH still-pending migrations (csv_import, alerts_telegram) AND this one are pushed — it currently honest-FAILs at `import_batches not found` (STATE.md). The task's bar is: the new assertions are present, correctly shaped, and `npx tsc --noEmit` is clean. Live run is DEFERRED to 06-10.
  </action>
  <verify>`grep -n "news_item_instruments" scripts/rls-isolation-test.ts` shows the new INSERT-rejected and SELECT-works assertions; `npx tsc --noEmit` clean (no new type errors from the edit).</verify>
  <done>The isolation test asserts news_item_instruments rejects authenticated writes and permits authenticated reads, matching the news_items precedent; tsc is clean. Live execution deferred to the 06-10 checkpoint.</done>
</task>

</tasks>

<verification>
- `git diff --stat` confirms exactly one new migration file and the test file changed; no existing migration touched.
- The migration adds NO authenticated write policy to news_items or news_item_instruments (grep confirms).
- `npx tsc --noEmit` clean.
- Live apply + live `npm run test:rls` are DEFERRED (consent-gated, no Docker/live-push consent this session — STATE.md). Confirm the honest deferred state by running `npm run test:rls` and observing it fail before the news assertions (at `import_batches not found`), proving the deferral is real, not fabricated.
</verification>

<success_criteria>
news_items is extended (title_hash + summary_status + summarized_at + title_hash partial-unique index) and a closed-RLS news_item_instruments join table exists, both preserving the service-role-write-only posture, and the RLS test covers the new table — all statically verified, live apply deferred to 06-10.
</success_criteria>

<output>
After completion, create `.planning/phases/06-news-pipeline/06-01-SUMMARY.md`.
</output>
