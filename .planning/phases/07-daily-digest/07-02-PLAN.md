---
phase: 07-daily-digest
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/digest/types.ts
  - src/lib/digest/compose.ts
  - scripts/digest-compose-test.ts
  - package.json
autonomous: true
requirements: [DGST-01]

must_haves:
  truths:
    - "istDateKey(now) returns the YYYY-MM-DD date in IST (fixed UTC+5:30, no DST, no timezone library) with the midnight rollover proven at exactly 18:30:00Z, and computeDigestDedupeKey(userId, now) returns daily_digest:{userId}:{istDate} — the once-per-day idempotency bucket the outbox partial unique index enforces."
    - "selectTopMovers returns at most N holdings sorted by absolute dayChangePct descending, considering ONLY status==='priced' holdings with non-null dayChangePct — pending/failed-price holdings are excluded honestly, never shown as fabricated 0% movers."
    - "buildDailyDigestMessage renders a single HTML-parse-mode message containing total value, signed day P&L, top movers, and the day's news items — with every externally-sourced string (ticker, headline, summary) passed through escapeHtml, and with honest empty/degraded states: no holdings, all-prices-pending, fxUnavailable exclusion note, and 'No summarized portfolio news today.' when the news list is empty or degraded."
    - "The message NEVER exceeds 4096 chars and truncation NEVER cuts inside an HTML tag: news items are appended whole-item-at-a-time within the budget (proven by a test feeding many oversized items), unlike buildPriceAlertMessage's naive slice which is only safe for one-line alerts."
    - "All of the above is pure (zero I/O, injected now/inputs), proven RED then GREEN by scripts/digest-compose-test.ts via node:assert/strict, registered as npm run test:digest-compose."
  artifacts:
    - path: "src/lib/digest/types.ts"
      provides: "DigestHoldingInput / DigestNewsItem / DigestMessageInput shared vocabulary (declarations-only)"
      contains: "DigestMessageInput"
    - path: "src/lib/digest/compose.ts"
      provides: "istDateKey, computeDigestDedupeKey, selectTopMovers, buildDailyDigestMessage — pure"
      exports: ["istDateKey", "computeDigestDedupeKey", "selectTopMovers", "buildDailyDigestMessage"]
    - path: "scripts/digest-compose-test.ts"
      provides: "node:assert/strict proof of all composition behaviors incl. IST rollover + whole-item truncation"
      min_lines: 80
    - path: "package.json"
      provides: "test:digest-compose script (tsx scripts/digest-compose-test.ts)"
      contains: "test:digest-compose"
  key_links:
    - from: "src/lib/digest/compose.ts"
      to: "src/lib/telegram/build-message.ts"
      via: "imports escapeHtml (never a new escaper — Don't Hand-Roll)"
      pattern: "escapeHtml"
    - from: "scripts/digest-compose-test.ts"
      to: "src/lib/digest/compose.ts"
      via: "node:assert/strict assertions over pure functions"
      pattern: "buildDailyDigestMessage"
---

<objective>
TDD the entire pure heart of the digest: IST date bucketing + dedupe key, top-mover selection, and the HTML digest message builder with truncation that can never produce a Telegram-rejected mid-tag cut. Zero I/O — no Supabase, no fetch, no env.

Purpose: DGST-01's "single Telegram digest with total value, day P&L, top movers, and the day's summarized news" is a deterministic text-composition problem with clear input/output cases — the strongest TDD candidate in this phase. Locking it pure now means 07-03's orchestration is thin glue.
Output: src/lib/digest/{types,compose}.ts proven by scripts/digest-compose-test.ts (npm run test:digest-compose).
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-daily-digest/07-RESEARCH.md
@src/lib/telegram/build-message.ts
@src/lib/prices/pnl-calculator.ts
@src/lib/alerts/evaluate.ts
@scripts/alerts-eval-test.ts
</context>

<tasks>

<task type="tdd">
  <name>TDD Task 1: istDateKey + computeDigestDedupeKey + selectTopMovers</name>
  <files>src/lib/digest/types.ts, src/lib/digest/compose.ts, scripts/digest-compose-test.ts, package.json</files>
  <action>
RED: create `scripts/digest-compose-test.ts` (node:assert/strict, house style of scripts/alerts-eval-test.ts — grouped cases, injectable `now`) covering:
- istDateKey: `new Date('2026-07-17T18:29:59Z')` → `'2026-07-17'`... careful: 18:29Z + 5:30 = 23:59 IST SAME day → `'2026-07-17'`; `new Date('2026-07-17T18:30:00Z')` → `'2026-07-18'` (IST midnight rollover); a mid-day UTC time maps to the same IST date; a 31-Dec 18:30Z rolls the YEAR.
- computeDigestDedupeKey('user-uuid', now) → `daily_digest:user-uuid:2026-07-18` shape (kind prefix matches the outbox kind string exactly).
- selectTopMovers: given a mix of priced holdings with dayChangePct {+5.2, -8.1, +0.3, null} and one status 'pending' holding, returns [-8.1, +5.2, +0.3] tickers in that order for n=3 (absolute-value sort, sign preserved in output); null-dayChangePct and pending holdings never appear; n larger than the list returns all; empty input returns [].
Register `"test:digest-compose": "tsx scripts/digest-compose-test.ts"` in package.json (this plan is the SOLE wave-1 package.json owner; name per the taken-list — test:rls/derive-holdings/price-pnl/import-*/telegram/alerts are taken, sibling Phase 6 is taking test:news-*). Run: MUST fail (module doesn't exist). Commit `test(07-02): add failing digest composition tests (RED)`.

GREEN: create `src/lib/digest/types.ts` — declarations only (notifications/types.ts style):
```ts
export type DigestHoldingInput = { ticker: string; status: 'pending' | 'priced'; dayChangePct: number | null };
export type DigestNewsItem = { headline: string; summary: string | null; url: string };
export type DigestMessageInput = {
  istDate: string;                       // from istDateKey
  baseCurrency: string;                  // 'INR' | 'USD' display string
  totalCurrentValue: number | null;      // null == nothing priced yet (honest pending)
  totalDayChange: number | null;
  hasHoldings: boolean;
  fxUnavailable: boolean;
  fxExcludedCurrency: string | null;     // e.g. 'USD' when fxUnavailable excluded it
  topMovers: Array<{ ticker: string; dayChangePct: number }>;
  news: DigestNewsItem[];
  newsDegraded: boolean;
};
```
Then `src/lib/digest/compose.ts` (pure, header comment citing DGST-01 + research Q3/Q5): `istDateKey(now: Date): string` via fixed-offset math `new Date(now.getTime() + 5.5 * 3600 * 1000).toISOString().slice(0, 10)` (IST has no DST — no Intl/timezone dependency, fully deterministic); `computeDigestDedupeKey(userId: string, now: Date): string` returning `` `daily_digest:${userId}:${istDateKey(now)}` ``; `selectTopMovers(holdings: DigestHoldingInput[], n = 3)` filtering `status === 'priced' && dayChangePct !== null` then sorting by `Math.abs(dayChangePct)` desc, slicing n. Run test: MUST pass. Commit `feat(07-02): implement IST date bucket, digest dedupe key, top movers (GREEN)`.
  </action>
  <verify>`npm run test:digest-compose` fails at RED commit, passes at GREEN commit; `npx tsc --noEmit` clean.</verify>
  <done>Date bucketing (with the 18:30Z rollover), dedupe key, and honest mover selection are pure, tested, and committed RED then GREEN.</done>
</task>

<task type="tdd">
  <name>TDD Task 2: buildDailyDigestMessage — HTML digest with whole-item truncation</name>
  <files>src/lib/digest/compose.ts, scripts/digest-compose-test.ts</files>
  <action>
RED: extend scripts/digest-compose-test.ts with buildDailyDigestMessage cases:
- Full happy path (INR base, priced totals, 3 movers, 2 news items): output contains `<b>` header with the istDate, the formatted total + signed day change, each mover ticker with signed percent, each news headline; output length ≤ 4096.
- Escaping: a ticker `M&M`, a headline containing `<script>alert(1)</script> & more` — output contains `M&amp;M` and `&lt;script&gt;` and NEVER raw `<script>` (headlines are external text — the escapeHtml requirement from research Q3).
- Honest empties: hasHoldings=false → "No holdings" wording present, no fabricated totals; totalCurrentValue=null with hasHoldings=true → "pending" wording, never a fabricated number/0-as-value; fxUnavailable=true with fxExcludedCurrency='USD' → an explicit exclusion note naming USD; news=[] or newsDegraded=true → exact string "No summarized portfolio news today." present.
- Truncation: 50 news items each with a ~300-char headline → output ≤ 4096, AND the output never ends inside a tag (assert the output, if it contains any `<`, has balanced `<b>`/`</b>` pairs and does not end with a partial entity/tag — concretely: for every included news item its FULL rendered line is present verbatim; the first omitted item is entirely absent), AND the portfolio section is fully intact (truncation only ever drops news items, never the snapshot).
Run: MUST fail. Commit `test(07-02): add failing digest message builder tests (RED)`.

GREEN: implement `buildDailyDigestMessage(input: DigestMessageInput): string` in compose.ts:
- Import `escapeHtml` from `@/lib/telegram/build-message` (never a new escaper).
- Compose sections in order: (1) header `<b>Daily Digest — {istDate}</b>`; (2) snapshot: if !hasHoldings → honest no-holdings line; else if totalCurrentValue === null → honest prices-pending line; else `Portfolio: <code>{formatted total} {baseCurrency}</code>` + signed day change line (use Intl.NumberFormat with 'en-IN' for INR / 'en-US' otherwise, maximumFractionDigits 0 — the dashboard's currencyFmt precedent; deterministic given fixed inputs); (3) if fxUnavailable → the exclusion note naming fxExcludedCurrency; (4) movers section only when topMovers non-empty: one line per mover `{escapeHtml(ticker)} {sign}{dayChangePct.toFixed(1)}%`; (5) news: if newsDegraded or empty → "No summarized portfolio news today."; else `• <b>{escapeHtml(headline)}</b> — {escapeHtml(summary ?? '')}` per item.
- Truncation algorithm: build the non-news skeleton first; then append news lines one WHOLE line at a time only while `current.length + line.length + 1 <= 4096`; stop at the first line that does not fit (drop it and all subsequent). Keep a final defensive `slice(0, 4096)` as a belt (tests prove it never actually cuts). This is deliberately different from buildPriceAlertMessage's naive slice — document why in a comment (mid-tag cut → Telegram 400 "can't parse entities", research Pitfall 3).
Run test: MUST pass. `npx tsc --noEmit` clean. Commit `feat(07-02): implement digest HTML message builder with whole-item truncation (GREEN)`. REFACTOR only if needed.
  </action>
  <verify>`npm run test:digest-compose` passes all groups; `npx tsc --noEmit` clean; `grep -n "escapeHtml" src/lib/digest/compose.ts` shows the import from '@/lib/telegram/build-message' (no local escaper defined).</verify>
  <done>A single-message HTML digest renders every DGST-01 element with honest degraded states, escapes all external text, and provably never exceeds 4096 chars nor cuts mid-tag.</done>
</task>

</tasks>

<verification>
- `npm run test:digest-compose` green; RED commits genuinely failed first (visible in git history).
- `npx tsc --noEmit` clean.
- No file outside this plan's four files touched (`git show HEAD --stat` per commit — a parallel executor owns disjoint files this wave; use explicit trailing pathspecs on every commit).
- `src/lib/digest/*` imports NOTHING from src/lib/news/ (does not exist until Phase 6 executes) and NOTHING that does I/O.
</verification>

<success_criteria>
The digest's dedupe bucketing, mover selection, and message rendering are pure, TDD-proven (including the IST 18:30Z rollover and the oversized-news truncation case), and registered as npm run test:digest-compose.
</success_criteria>

<output>
After completion, create `.planning/phases/07-daily-digest/07-02-SUMMARY.md`.
</output>
