---
phase: 07-daily-digest
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/lib/digest/news.ts
  - src/lib/digest/read.ts
  - src/lib/digest/run.ts
  - src/app/api/digest/run/route.ts
  - src/server-actions/digest.ts
autonomous: true
requirements: [DGST-01, DGST-02]

# NOTE (cross-phase dependency, by design): src/lib/digest/news.ts reads the
# tables Phase 6's migration creates (news_items.summary_status +
# news_item_instruments, per 06-01-PLAN.md's authored contract). Phase 6 is
# NOT executed yet — this seam therefore (a) queries by TABLE NAME only,
# NEVER importing from src/lib/news/* (which does not exist; a TS import
# would break tsc today), and (b) degrades honestly to a portfolio-only
# digest on undefined-table/column errors (42P01/42703) or any other news
# failure. Live news inclusion is verified at 07-05 AFTER Phase 6 executes.

must_haves:
  truths:
    - "runDailyDigest(admin) sweeps all enabled digest_preferences users, skips (and counts) users without a 'linked' telegram_links row, composes each remaining user's portfolio snapshot via getPortfolioPnL + top movers + news via getDailyDigestNews, and enqueues ONE notifications_outbox row per user with kind 'daily_digest' and dedupeKey daily_digest:{userId}:{istDate} — a second run the same IST day enqueues nothing (DB-enforced by the partial unique index)."
    - "A news failure or missing Phase-6 schema NEVER kills the digest: getDailyDigestNews returns { items: [], degraded: true } on 42P01/42703 or any error, and the digest renders the honest 'No summarized portfolio news today.' line (portfolio-only digest)."
    - "One user's compose failure never aborts the sweep for other users (per-user try/catch with honest failed counter), mirroring dispatchOutbox's one-poisoned-row-does-not-abort rule."
    - "POST /api/digest/run rejects requests without the DIGEST_RUN_SECRET bearer BEFORE any Supabase client is constructed (isAuthorizedRefreshRequest clone of the dispatch route), then runs runDailyDigest + dispatchOutbox and returns honest counts."
    - "setDigestEnabled(enabled) upserts the caller's OWN digest_preferences row via the cookie-bound RLS client (never admin) and revalidates /alerts; sendTestDigest() auth-gates via getUser, returns an inline 'Telegram not linked' error when unlinked, otherwise composes for the CALLER ONLY, enqueues with dedupeKey null (one-off — bypasses the daily bucket by design), and dispatches, returning honest sent/retried/failed counts."
  artifacts:
    - path: "src/lib/digest/news.ts"
      provides: "getDailyDigestNews(admin, instrumentIds, sinceIso) — narrow Phase-6 seam, honest degradation"
      exports: ["getDailyDigestNews"]
    - path: "src/lib/digest/read.ts"
      provides: "getDigestPreference(supabase, userId) — cookie-bound own-row read, no row == disabled"
      exports: ["getDigestPreference"]
    - path: "src/lib/digest/run.ts"
      provides: "composeDigestForUser + runDailyDigest orchestration (admin client passed in, never built)"
      exports: ["runDailyDigest", "composeDigestForUser"]
    - path: "src/app/api/digest/run/route.ts"
      provides: "secret-guarded daily digest entry point (DIGEST_RUN_SECRET)"
      exports: ["POST"]
    - path: "src/server-actions/digest.ts"
      provides: "setDigestEnabled + sendTestDigest Server Actions"
      contains: "'use server'"
  key_links:
    - from: "src/lib/digest/run.ts"
      to: "src/lib/notifications/outbox.ts"
      via: "enqueueNotifications with kind 'daily_digest' + computeDigestDedupeKey"
      pattern: "enqueueNotifications"
    - from: "src/lib/digest/run.ts"
      to: "src/lib/prices/get-portfolio-pnl.ts"
      via: "getPortfolioPnL(admin, accountId, baseCurrency) per enabled+linked user"
      pattern: "getPortfolioPnL"
    - from: "src/app/api/digest/run/route.ts"
      to: "src/lib/prices/ingest.ts"
      via: "isAuthorizedRefreshRequest guard BEFORE createAdminClient"
      pattern: "isAuthorizedRefreshRequest"
    - from: "src/server-actions/digest.ts"
      to: "digest_preferences"
      via: "cookie-bound upsert of the caller's own row + revalidatePath('/alerts')"
      pattern: "digest_preferences"
---

<objective>
Build the digest's I/O layer: the Phase-6 news seam, the preference read, the cross-user orchestration sweep, the secret-guarded daily route, and the two Server Actions (enable/disable + send-test). Everything composes 07-02's pure functions with the existing outbox/P&L subsystems — no new rendering or math here.

Purpose: DGST-01's once-daily send needs a cron-reachable entry point and an idempotent sweep; DGST-02 needs the toggle mutation and the linked-account respect. The send-test action is the ONLY locally-verifiable live path (the cron is deploy-gated), so it must exist before 07-05.
Output: runDailyDigest + /api/digest/run + setDigestEnabled/sendTestDigest, tsc- and build-clean.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-daily-digest/07-RESEARCH.md
@src/lib/alerts/sweep.ts
@src/lib/notifications/outbox.ts
@src/lib/prices/get-portfolio-pnl.ts
@src/lib/supabase/portfolio.ts
@src/app/api/notifications/dispatch/route.ts
@src/server-actions/telegram.ts
@src/lib/telegram/read.ts
@.planning/phases/06-news-pipeline/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: getDailyDigestNews seam + getDigestPreference read</name>
  <files>src/lib/digest/news.ts, src/lib/digest/read.ts</files>
  <action>
Create `src/lib/digest/news.ts` — the NARROW Phase-6 seam (header comment MUST state the cross-phase contract: reads tables authored by Phase 6's 06-01 migration by NAME only; never imports src/lib/news/*; degrades honestly while Phase 6 is unexecuted/unapplied):

```ts
export type DailyDigestNewsResult = { items: DigestNewsItem[]; degraded: boolean; error: string | null };
export async function getDailyDigestNews(
  admin: SupabaseClient, instrumentIds: string[], sinceIso: string
): Promise<DailyDigestNewsResult>
```
- Empty instrumentIds → `{ items: [], degraded: false, error: null }` (nothing to match — not a failure).
- Query: `admin.from('news_item_instruments').select('news_item_id, news_items!inner ( id, headline, summary, url, published_at, summary_status )').in('instrument_id', instrumentIds)` filtered to `news_items.summary_status = 'summarized'` and `news_items.published_at >= sinceIso` (use the `!inner` join + referenced-table filter syntax `.eq('news_items.summary_status', 'summarized').gte('news_items.published_at', sinceIso)`); de-duplicate by news_item_id in TS (one article matching several held instruments must appear once); sort newest-first by published_at; cap at 5 items; map to `DigestNewsItem { headline, summary, url }` (import the type from `@/lib/digest/types`).
- Error handling — the load-bearing part: if the query errors, return `{ items: [], degraded: true, error: error.message }`. NEVER throw. Postgres 42P01 (undefined table) / 42703 (undefined column) are the expected "Phase 6 not applied yet" signatures — mention them in the comment; but ALL errors degrade (a news problem must never kill the portfolio digest — the refresh-tail failure-isolation rationale).

Create `src/lib/digest/read.ts` — clone of `src/lib/telegram/read.ts`'s shape: `getDigestPreference(supabase: SupabaseClient, userId: string): Promise<{ enabled: boolean }>` — cookie-bound RLS client (NEVER admin), `.from('digest_preferences').select('enabled').eq('user_id', userId).maybeSingle()`; no row → `{ enabled: false }` (no row == disabled, the honest opt-in default); a query error throws (read path, RSC will surface it — matches getTelegramLink).
  </action>
  <verify>`npx tsc --noEmit` clean; `grep -rn "from '@/lib/news" src/lib/digest/` returns NOTHING; `grep -n "42P01\|degraded" src/lib/digest/news.ts` shows the degradation contract.</verify>
  <done>The news seam queries Phase-6 tables by name with honest degradation and the preference read returns enabled:false when no row exists; nothing imports Phase-6 code.</done>
</task>

<task type="auto">
  <name>Task 2: runDailyDigest orchestration sweep</name>
  <files>src/lib/digest/run.ts</files>
  <action>
Create `src/lib/digest/run.ts` cloning `src/lib/alerts/sweep.ts`'s discipline (header comment: accepts an ALREADY-CONSTRUCTED admin client, never builds one; admin required because the sweep is cross-user; enqueue idempotency is DB-backed by the dedupe key so a crash/rerun can never double-send).

```ts
export type DigestRunSummary = {
  considered: number; enqueued: number; skippedUnlinked: number;
  skippedDuplicate: number; failed: number; newsDegraded: number;
};
export type ComposeOutcome =
  | { ok: true; text: string; newsDegraded: boolean }
  | { ok: false; error: string };
export async function composeDigestForUser(admin: SupabaseClient, userId: string, now: Date): Promise<ComposeOutcome>
export async function runDailyDigest(admin: SupabaseClient, now = new Date()): Promise<DigestRunSummary>
```

composeDigestForUser (shared by the sweep AND 07-03 Task 3's sendTestDigest — single compose path, they can never drift):
1. Resolve account: `admin.from('investment_accounts').select('id, base_currency').eq('user_id', userId).single()`; baseCurrency defaults 'INR' (dashboard precedent). Failure → `{ ok: false, error }`.
2. `const pnl = await getPortfolioPnL(admin, accountId, baseCurrency)` — admin-safe: every inner query is explicitly account/instrument-scoped (research Q3).
3. `const watchlist = await getWatchlist(admin, accountId)`; instrument scope = held ∪ watched instrument ids (Set union — the discoverInstrumentIds precedent, per-user).
4. `const news = await getDailyDigestNews(admin, ids, new Date(now.getTime() - 24 * 3600 * 1000).toISOString())` (the day's news == last 24h, research Q4).
5. Map to `DigestMessageInput`: istDate from `istDateKey(now)`; hasHoldings = pnl.holdings.length > 0; totalCurrentValue/totalDayChange from pnl.portfolioTotal but pass null for both when NO holding has status 'priced' (honest pending — never render a 0 total as if it were a price); fxUnavailable + fxExcludedCurrency from pnl (the non-base currency with costBasis > 0, dashboard page.tsx:64-66 precedent); topMovers = `selectTopMovers(pnl.holdings.map(h => ({ ticker: h.ticker, status: h.status, dayChangePct: h.dayChangePct })))`; news.items + newsDegraded = news.degraded.
6. `text = buildDailyDigestMessage(input)`; return `{ ok: true, text, newsDegraded: news.degraded }`.

runDailyDigest:
1. `admin.from('digest_preferences').select('user_id').eq('enabled', true)` → considered = rows.length; empty → zero summary.
2. Batch-load `telegram_links` for those user_ids (`select('user_id, status').in('user_id', ids)`); users without a row or with status !== 'linked' → skippedUnlinked++ and skip (DGST-02 "respects their linked Telegram account"; the dispatcher would fail them honestly anyway — outbox.ts:105-113 — but the sweep skips politely and counts).
3. For each remaining user, inside a per-user try/catch (one bad user never aborts the sweep — failed++ with console.error, continue): compose via composeDigestForUser; on ok, `enqueueNotifications(admin, [{ userId, kind: 'daily_digest', payload: { text, istDate, newsDegraded }, dedupeKey: computeDigestDedupeKey(userId, now) }])` — audit metadata beyond text is allowed by the payload contract; enqueued++; newsDegraded++ when degraded. NOTE on skippedDuplicate: enqueueNotifications' ignoreDuplicates upsert is silent — a same-day rerun reports enqueued but the DB inserts nothing; document in a comment that `skippedDuplicate` is best-effort (pre-check `notifications_outbox` for the dedupe_key with a single `.in('dedupe_key', keys)` select BEFORE the loop and count/skip matches — an honest counter, while the unique index remains the real guarantee).
4. Sequential loop, never Promise.all (mirrors dispatchOutbox — and keeps per-user failures attributable).
  </action>
  <verify>`npx tsc --noEmit` clean; `grep -n "createAdminClient\|createClient" src/lib/digest/run.ts` returns NOTHING (client passed in, never built); `grep -n "computeDigestDedupeKey\|enqueueNotifications\|selectTopMovers\|buildDailyDigestMessage" src/lib/digest/run.ts` shows all four composition points wired.</verify>
  <done>One shared compose path feeds both the sweep and the test action; the sweep is idempotent per IST day, skips unlinked users with honest counters, and isolates per-user failures.</done>
</task>

<task type="auto">
  <name>Task 3: /api/digest/run route + digest Server Actions + env placeholder</name>
  <files>src/app/api/digest/run/route.ts, src/server-actions/digest.ts</files>
  <action>
Create `src/app/api/digest/run/route.ts` as a structural clone of `src/app/api/notifications/dispatch/route.ts` (read it first — same ~27-line shape): guard `isAuthorizedRefreshRequest(request.headers.get('authorization'), process.env.DIGEST_RUN_SECRET ?? '')` BEFORE `createAdminClient()` (empty/unset secret ALWAYS denies); then `const digest = await runDailyDigest(admin); const dispatched = await dispatchOutbox(admin);` and return `NextResponse.json({ success: true, digest, dispatched })`; catch → 500 with the error message. No revalidatePath (route runs outside render context — prices route precedent). Header comment: this is the pg_cron target (deploy-gated, 07-01's cron migration) AND the manual curl lever.

Create `src/server-actions/digest.ts` (file-level `'use server'`; NEVER a bare `export { } from` re-export — the alerts.ts Rule-3 bundler trap; declare real async functions only):
- `type ActionResult = { success: true } | { success: false; error: string }` (alerts.ts shape).
- `setDigestEnabled(enabled: boolean): Promise<ActionResult>` — cookie client `createClient()` → `getUser()` → unauthorized error result; then `.from('digest_preferences').upsert({ user_id: user.id, enabled, updated_at: new Date().toISOString() }, { onConflict: 'user_id' })` (own-row INSERT+UPDATE policies from 07-01 authorize this; NEVER the admin client — user-owned row); error → `{ success: false, error }`; then `revalidatePath('/alerts')` and `{ success: true }`.
- `type TestDigestResult = { success: true; dispatched: { claimed: number; sent: number; retried: number; failed: number }; newsDegraded: boolean } | { success: false; error: string }`.
- `sendTestDigest(): Promise<TestDigestResult>` — the auth-gate-then-admin precedent (telegram.ts checkTelegramLink): cookie `getUser()` gate; `getTelegramLink(supabase, user.id)` — if status !== 'linked', return `{ success: false, error: 'Telegram is not linked — link it above first.' }` (inline honest error, no doomed enqueue); else `admin = createAdminClient()`; `composeDigestForUser(admin, user.id, new Date())` — on !ok return the error; `enqueueNotifications(admin, [{ userId: user.id, kind: 'daily_digest', payload: { text, test: true }, dedupeKey: null }])` — null dedupe key: a one-off test must send even after today's real digest (alerts_telegram.sql:97 anticipated exactly this); `const dispatched = await dispatchOutbox(admin)`; return honest counts + newsDegraded. Note in a comment: dispatchOutbox claims ALL due pending rows, not just this one — other pending notifications may ride along; that is correct behavior, report the honest summary. With no TELEGRAM_BOT_TOKEN configured the send fails honestly inside the dispatcher ("not configured" classification) — counts surface it; never fabricate success.
- Both actions end with `revalidatePath('/alerts')` where state changed (setDigestEnabled always; sendTestDigest not needed — no page-visible rows change... actually the outbox is not rendered; skip revalidate there).
Also add to gitignored `.env.local` a labeled placeholder following the house convention: `DIGEST_RUN_SECRET=your-digest-run-secret` with a comment ("self-generate via node -e crypto.randomBytes(32).toString('hex'); guards POST /api/digest/run; kept SEPARATE from PRICE_REFRESH_SECRET/NOTIFY_DISPATCH_SECRET for independent rotation; unset == route always denies"). NEVER write any realistic-format secret to a committed file.
  </action>
  <verify>`npx tsc --noEmit` clean; `npm run build` clean AND `/api/digest/run` appears in the route table (the build is what catches the 'use server' re-export trap); `grep -n "isAuthorizedRefreshRequest" src/app/api/digest/run/route.ts` shows the guard before createAdminClient (guard line number lower); `grep -n "export {" src/server-actions/digest.ts` returns NOTHING; `git grep -E "[0-9]{8,10}:[A-Za-z0-9_-]{30,}" -- src/` returns NOTHING.</verify>
  <done>The cron-facing route is secret-guarded before any Supabase construction; enable/disable is a cookie-bound own-row upsert; send-test composes via the shared path, bypasses the daily bucket with a null dedupe key, respects the linked account inline, and reports honest dispatch counts.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` and `npm run build` both clean; `/api/digest/run` listed in the build route table.
- Greps: no `src/lib/news` import anywhere under src/lib/digest/; no client construction inside src/lib/digest/run.ts; guard-before-admin in the route; no bare re-exports in src/server-actions/digest.ts.
- Secret scan over all new files: `git grep -E "[0-9]{8,10}:[A-Za-z0-9_-]{30,}" -- src/ supabase/` returns nothing.
- Live behavior (real enqueue/dispatch, cron POST, Phase-6 news rows) is NOT claimable here — DEFERRED to 07-05.
- Commit per task with explicit trailing pathspecs; `git show HEAD --stat` after each (a parallel Phase 6 planning/execution agent may share the git index).
</verification>

<success_criteria>
runDailyDigest + composeDigestForUser + the secret-guarded route + both Server Actions exist, statically verified, composing exclusively through the Phase 5 outbox and the 07-02 pure layer, with the Phase-6 news seam degrading honestly while unexecuted.
</success_criteria>

<output>
After completion, create `.planning/phases/07-daily-digest/07-03-SUMMARY.md`.
</output>
