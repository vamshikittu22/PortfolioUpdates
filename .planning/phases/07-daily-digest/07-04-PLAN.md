---
phase: 07-daily-digest
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - src/components/dashboard/DigestSettingsCard.tsx
  - src/app/(dashboard)/alerts/page.tsx
autonomous: true
requirements: [DGST-02]

must_haves:
  truths:
    - "On /alerts, directly under the TelegramLinkCard, the user sees a Daily Digest card showing the real persisted enabled/disabled state (read server-side via getDigestPreference — never a guessed or hardcoded state)."
    - "Toggling the digest calls setDigestEnabled inside a useTransition; failures surface inline ({ success: false, error } text shown next to the control, never swallowed); success flips the card via the parent RSC re-read after revalidatePath('/alerts')."
    - "A 'Send test digest' button calls sendTestDigest and surfaces the HONEST outcome inline: sent count on success, the 'Telegram is not linked' message when unlinked, a visible note when the news section was degraded (portfolio-only), and the dispatcher's honest failure text when the bot token is unconfigured — never a fabricated 'Sent!'."
    - "When Telegram is not linked, the card says so and points at the link card above (the digest respects the linked account, DGST-02) — the toggle remains usable (preference persists independently of link state) but the card is honest that nothing will be delivered until linked."
  artifacts:
    - path: "src/components/dashboard/DigestSettingsCard.tsx"
      provides: "'use client' digest toggle + send-test card (TelegramLinkCard structural clone)"
      min_lines: 60
    - path: "src/app/(dashboard)/alerts/page.tsx"
      provides: "server read of getDigestPreference added to the existing parallel reads; DigestSettingsCard rendered under TelegramLinkCard"
      contains: "DigestSettingsCard"
  key_links:
    - from: "src/components/dashboard/DigestSettingsCard.tsx"
      to: "src/server-actions/digest.ts"
      via: "setDigestEnabled / sendTestDigest inside useTransition"
      pattern: "setDigestEnabled"
    - from: "src/app/(dashboard)/alerts/page.tsx"
      to: "src/lib/digest/read.ts"
      via: "getDigestPreference(supabase, user.id) in the RSC's Promise.all"
      pattern: "getDigestPreference"
---

<objective>
Give DGST-02 its user surface: a Daily Digest card on /alerts (the Telegram-centric page — the settings page is client-only mock-era localStorage UI, research Q6) with the enable/disable toggle and the send-test button, wired to 07-03's Server Actions with every outcome surfaced honestly inline.

Purpose: DGST-02 says the user can enable/disable the digest; this is the only human-facing control in the phase, and the send-test button is the human's local verification lever for 07-05.
Output: DigestSettingsCard + the /alerts RSC reading and passing the real preference.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-daily-digest/07-RESEARCH.md
@src/components/dashboard/TelegramLinkCard.tsx
@src/app/(dashboard)/alerts/page.tsx
@src/server-actions/digest.ts
@src/lib/digest/read.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DigestSettingsCard client component</name>
  <files>src/components/dashboard/DigestSettingsCard.tsx</files>
  <action>
Create `src/components/dashboard/DigestSettingsCard.tsx` as a structural clone of TelegramLinkCard.tsx (same 'use client' + useTransition + inline-error discipline, same glass-card styling vocabulary):

Props: `{ enabled: boolean; telegramLinked: boolean }` (both from the RSC — never fetched client-side, never defaulted optimistically).

Layout (one card, two zones):
- Header row: an icon (e.g. lucide `CalendarClock` or `Newspaper`), title "Daily Digest", subtitle "One Telegram message a day — portfolio value, day P&L, top movers, and your portfolio news (~08:45 IST)."
- Toggle: use the existing dependency `@radix-ui/react-switch` if a styled Switch exists in the repo (check src/components/ui/ first — reuse it; if none exists, a plain accessible button-toggle in the house button style is fine; do NOT add a new dependency). onCheckedChange → useTransition → `setDigestEnabled(next)`; on `{ success: false, error }` set inline error state and do NOT flip local state (the card's displayed state comes from the `enabled` prop; the RSC re-render after revalidatePath('/alerts') is what flips it — the TelegramLinkCard status-prop pattern; a brief pending state via isPending is enough feedback).
- When `!telegramLinked`: an honest inline note under the toggle — "Telegram is not linked — the digest will not be delivered until you link it above." (amber/warning styling like TelegramLinkCard's checkMessage). The toggle stays enabled (the preference is user-owned and persists independently; delivery respect is enforced server-side).
- "Send test digest" button (secondary style): useTransition → `sendTestDigest()`; result state rendered inline: success → "Test digest queued — sent {dispatched.sent}, failed {dispatched.failed}." plus, when `newsDegraded`, an extra honest line "News section degraded — portfolio-only (news pipeline not live yet)."; failure → the returned error text in the danger style. Disable the button while pending. Never render any success wording unless the action returned success: true.
All user-visible strings live in this file; no fabricated counts — everything shown comes from the action's returned values.
  </action>
  <verify>`npx tsc --noEmit` clean; `grep -n "setDigestEnabled\|sendTestDigest" src/components/dashboard/DigestSettingsCard.tsx` shows both actions imported from '@/server-actions/digest'; `grep -n "useState\|useTransition" ...` shows the client state machine; no fetch/Supabase import in the component.</verify>
  <done>A client card renders the real preference, toggles via the Server Action with inline errors, warns honestly when unlinked, and reports send-test outcomes (including degraded news and dispatcher failures) verbatim from the action result.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the card into the /alerts RSC</name>
  <files>src/app/(dashboard)/alerts/page.tsx</files>
  <action>
Edit `src/app/(dashboard)/alerts/page.tsx` (currently clean in git, but commit with an explicit trailing pathspec anyway — house rule while parallel agents run):
- Import `getDigestPreference` from '@/lib/digest/read' and `DigestSettingsCard` from '@/components/dashboard/DigestSettingsCard'.
- Extend the existing `Promise.all` (lines 31-34) to three parallel reads: `const [alerts, link, digestPref] = await Promise.all([getPriceAlerts(supabase, accountId), getTelegramLink(supabase, user.id), getDigestPreference(supabase, user.id)]);`
- Render `<DigestSettingsCard enabled={digestPref.enabled} telegramLinked={link.status === 'linked'} />` directly BELOW `<TelegramLinkCard ... />` (the digest depends on the link — reading order matches dependency order).
- Optionally update the bottom info card's copy to mention the digest alongside price alerts (keep it honest — the digest exists now). Touch nothing else on the page: the auth-guard flow, getPriceAlerts, AlertsTable stay byte-for-byte.
  </action>
  <verify>`npx tsc --noEmit` clean; `npm run build` clean with /alerts still in the route table; `grep -n "DigestSettingsCard\|getDigestPreference" "src/app/(dashboard)/alerts/page.tsx"` shows the read in Promise.all and the render under TelegramLinkCard; `git diff --stat` shows ONLY this file changed by this task.</verify>
  <done>/alerts server-reads the real digest preference in parallel with the existing reads and renders the digest card under the Telegram link card; nothing else on the page changed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run build` clean; /alerts present in the route table.
- The card's displayed state is prop-driven from the RSC read (grep: no useState initialized from anything but action results/inline messages).
- Per-task commits with explicit pathspecs; `git show HEAD --stat` confirms exactly one intended file per commit (parallel-agent index race precaution).
- Live click-through (real toggle persistence across refresh, real test-digest delivery) DEFERRED to 07-05.
</verification>

<success_criteria>
The user can see and change their real digest preference on /alerts and fire a test digest, with every outcome — including unlinked Telegram, degraded news, and unconfigured bot token — surfaced honestly inline.
</success_criteria>

<output>
After completion, create `.planning/phases/07-daily-digest/07-04-SUMMARY.md`.
</output>
