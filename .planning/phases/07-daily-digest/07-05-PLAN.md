---
phase: 07-daily-digest
plan: 05
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified: []
autonomous: false
requirements: [DGST-01, DGST-02]

user_setup:
  - service: telegram-bot
    why: "A real bot token (still outstanding from 05-09) is required for any live digest delivery. All Phase 7 code was built token-free."
    account_setup: "Message @BotFather -> /newbot (if not already done for 05-09). Same bot serves alerts and the digest."
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "@BotFather /newbot reply"
      - name: TELEGRAM_BOT_USERNAME
        source: "the bot username chosen in BotFather (without @)"
      - name: DIGEST_RUN_SECRET
        source: "self-generate: node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\" — guards POST /api/digest/run"
    dashboard_config:
      - task: "At DEPLOY time only: ALTER DATABASE postgres SET app.settings.digest_run_url / app.settings.digest_run_secret, then apply the daily_digest_cron migration"
        location: "Supabase SQL editor against the live project (never from localhost; never via a committed file)"

must_haves:
  truths:
    - "A human confirms, once the bot token exists and the pending migrations are applied with consent: enabling the digest and sending a test digest delivers ONE Telegram message containing total value, day P&L, top movers, and either the day's summarized news or the honest portfolio-only degraded line (DGST-01)."
    - "A human confirms the once-per-day idempotency: after the daily sweep has enqueued for today's IST date, running POST /api/digest/run again enqueues ZERO new digest rows (dedupe key bucket), while a 'Send test digest' still goes through (null dedupe key)."
    - "A human confirms DGST-02: disabling the toggle stops the sweep from enqueueing for that user; an enabled-but-unlinked user is skipped with an honest counter; re-linking restores delivery."
    - "The pg_cron daily schedule itself is verified only after a public deploy (deploy-gated, same as PRICE-02's cron) — or explicitly recorded DEFERRED."
  artifacts: []
  key_links: []
---

<objective>
Close Phase 7's live-verification debt — the phase's honesty gate. Every prior plan (07-01..07-04) is authored and statically verified (`npx tsc --noEmit`, `npm run build`, `npm run test:digest-compose`, grep gates) but nothing has run against a real bot token, an applied migration, live Phase-6 news rows, or a delivered Telegram message. Three debts are inherited/known: no `TELEGRAM_BOT_TOKEN` exists yet (05-09 deferred for the same reason), the `daily_digest.sql` migration is consent-gated alongside csv_import/alerts_telegram/news_pipeline, and the daily cron is deploy-gated (Supabase cloud cannot reach localhost).

Purpose: DGST-01/02's success criteria are observed behaviors — one message a day, with the right content, respecting the toggle and the linked account. None is claimable from static checks.
Output: either a completed live verification, or an explicit DEFERRED record in the SUMMARY (reasons: no bot token / no migration-push consent / Phase 6 not yet live / no public deploy) — never a fabricated pass, matching the 03-06 / 04-07 / 05-09 precedent.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-daily-digest/07-RESEARCH.md
@.planning/phases/07-daily-digest/07-01-PLAN.md
@.planning/phases/07-daily-digest/07-03-PLAN.md
@.planning/phases/05-alerts-telegram/05-09-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live end-to-end daily digest verification (or honest deferral)</name>
  <files>none (verification-only; no source is written by this task)</files>
  <what-built>
The complete Phase 7 feature: digest_preferences schema + deploy-gated daily cron (07-01), the pure TDD'd composition layer — IST bucketing, dedupe key, top movers, 4096-safe HTML builder (07-02), the cross-user sweep + secret-guarded /api/digest/run + setDigestEnabled/sendTestDigest actions with the honestly-degrading Phase-6 news seam (07-03), and the /alerts digest card (07-04).

No bot token exists (unchanged since 05-09's deferral), the preferences migration is intentionally unapplied pending consent, Phase 6's news pipeline may or may not be live yet, and the cron is deploy-gated — everything above is statically/unit verified but never run live. This checkpoint closes that gap or records it honestly.
  </what-built>
  <action>
See how-to-verify. Verification-only — no source is written here. If a step fails, describe observed vs. expected for a follow-up gap-closure plan; do NOT silently patch code from within this checkpoint. Applying migrations is a live-DB change requiring EXPLICIT user consent; creating the bot is a user action — if either is unavailable, record the whole checkpoint DEFERRED (matching 03-06/04-07/05-09).

CAUTION carried from STATE.md and 07-01: a blanket `supabase db push` would ALSO apply the deliberately-held-back `price_refresh_cron.sql` AND the new `daily_digest_cron.sql` (both deploy-gated, both silently-failing from localhost) plus any still-unpushed consent-gated migrations. Push SELECTIVELY, per-file, with consent: the apply list here is `daily_digest.sql` (+ the still-pending csv_import / alerts_telegram / news_pipeline files if consented); the hold-back list is BOTH cron files until a public deploy exists.
  </action>
  <how-to-verify>
Prerequisites: bot token + username in `.env.local` (from 05-09's checklist — same bot), `DIGEST_RUN_SECRET` self-generated and set, consent to selectively apply the pending migrations (at minimum alerts_telegram + daily_digest; news_pipeline too if Phase 6 has executed).

Token-gated checks — runnable from localhost, NO deploy needed:
1. **Migrations applied selectively (with consent):** confirm `digest_preferences` exists live and `notifications_outbox`'s kind CHECK was NOT modified (it already contained 'daily_digest'); confirm NEITHER cron migration was applied. `npm run test:rls` → PASS including the new digest_preferences isolation block (requires the other pending migrations applied too — if only partially applied, record which assertions ran honestly).
2. **Toggle persistence (DGST-02):** `npm run dev`, sign in, /alerts → Daily Digest card shows disabled; enable it; hard-refresh (F5) → still enabled (persisted row, not client state); disable → persists too.
3. **Unlinked honesty (DGST-02):** with Telegram NOT linked, the card shows the "will not be delivered until you link" note; "Send test digest" returns the inline "Telegram is not linked" error — no outbox row enqueued for it.
4. **Test digest delivery (DGST-01 content):** link Telegram (05-09 handshake if not already linked), then "Send test digest" → exactly ONE Telegram message arrives containing: the IST date header, total value + signed day P&L in the account's base currency (or the honest pending/no-holdings wording), top movers with signed percents, and — if Phase 6 is not live — the exact "No summarized portfolio news today." line (degraded portfolio-only digest is a PASS here, with the card's degraded note shown). Verify prices in the message match the dashboard (same price_cache).
5. **Sweep + once-per-day idempotency (DGST-01):** `curl -X POST http://localhost:3000/api/digest/run -H "Authorization: Bearer $DIGEST_RUN_SECRET"` → response shows considered/enqueued counts; ONE digest message arrives; the outbox row has dedupe_key `daily_digest:<user_id>:<today-IST>`. Run the SAME curl again → `skippedDuplicate` (or enqueued with zero new rows — verify in notifications_outbox) and NO second message. Then "Send test digest" → still delivers (null dedupe key bypass).
6. **Disabled respected:** toggle the digest OFF, delete today's digest outbox row (or wait for the next IST day), re-run the curl → that user is not enqueued (considered excludes them).
7. **Route guard:** the curl WITHOUT the bearer (and with a wrong secret) returns 401; confirm `DIGEST_RUN_SECRET` unset also denies (guard-before-Supabase).
8. **News inclusion (only if Phase 6 has executed AND news_pipeline migration is applied with summarized rows):** re-send the test digest → summarized headlines for held/watched instruments appear, escaped, newest-first, ≤5 items; a headline containing & or < renders correctly in Telegram (HTML escaping).

Deploy-gated checks — DEFERRED until a public HTTPS deploy exists (same treatment as PRICE-02's cron):
9. **Daily cron:** at deploy time, set `app.settings.digest_run_url` + `app.settings.digest_run_secret` via ALTER DATABASE (SQL editor, never a committed file), apply `daily_digest_cron.sql`, then next morning confirm cron.job_run_details shows the 03:15 UTC run succeeded and exactly one digest arrived ~08:45 IST.
  </how-to-verify>
  <verify>All token-gated steps (1-8, with 8 conditional on Phase 6 being live) pass against a real bot + live Supabase, and the deploy-gated step (9) passes on a deployment — OR the checkpoint is recorded DEFERRED with the specific reasons (no bot token / no migration consent / Phase 6 not live / no deploy).</verify>
  <done>One daily digest with total value, day P&L, top movers, and (real or honestly-degraded) news is observed delivered exactly once per IST day, the toggle and linked-account respect are observed working, and the cron fires on a deploy — OR the checkpoint is explicitly and honestly recorded DEFERRED (matching 03-06/04-07/05-09). Phase 7 is not claimed verified on static checks alone.</done>
  <resume-signal>Type "approved" once the token-gated steps pass (note step 9 as deploy-deferred if not yet deployed, and step 8 as phase-6-deferred if news is not live), or describe which step failed (observed vs. expected) so it can be fixed before Phase 7 is marked complete.</resume-signal>
</task>

</tasks>

<verification>
**DEFERRED / human checkpoint (Task 1):** everything requiring the bot token, consent-gated migration applies, live Phase-6 news rows, a delivered Telegram message, and (for the cron) a public deploy. If unavailable when this plan executes, record the checkpoint DEFERRED in the SUMMARY rather than fabricating a pass — this is the plan's entire purpose and its only task. Steps 1-8 are localhost-runnable once token + consent exist; step 9 is deploy-gated; step 8 additionally gates on Phase 6 execution.
</verification>

<success_criteria>
The live verification either passes (steps 1-8 locally, step 9 on deploy, step 8 once Phase 6 is live) or is explicitly and honestly recorded DEFERRED pending the bot token, migration consent, Phase 6 execution, and/or a public deploy. Phase 7 — and with it the milestone's digest requirements DGST-01/DGST-02 — is not marked verified on static checks alone.
</success_criteria>

<output>
After completion, create `.planning/phases/07-daily-digest/07-05-SUMMARY.md`.
</output>
