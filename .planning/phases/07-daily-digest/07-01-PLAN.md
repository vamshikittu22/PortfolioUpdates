---
phase: 07-daily-digest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260718090000_daily_digest.sql
  - supabase/migrations/20260718090500_daily_digest_cron.sql
  - scripts/rls-isolation-test.ts
autonomous: true
requirements: [DGST-01, DGST-02]

must_haves:
  truths:
    - "A NEW timestamped migration creates a user-level digest_preferences table (user_id PK -> auth.users, enabled BOOLEAN NOT NULL DEFAULT FALSE) with own-row SELECT/INSERT/UPDATE RLS — no row means disabled, an honest opt-in default (DGST-02)."
    - "NO migration touches notifications_outbox — the kind CHECK already enumerates 'daily_digest' (20260716221450_alerts_telegram.sql:91, verified in research). Extending it would be wrong by construction."
    - "A SEPARATE deploy-gated cron migration schedules a once-daily pg_cron POST ('15 3 * * *' UTC = 08:45 IST, 15 min after the 03:00 UTC price refresh) to /api/digest/run, reading url + secret from app.settings.* database settings exactly like 20260714220438_price_refresh_cron.sql — no secret in git, authored but NEVER applied locally (DGST-01)."
    - "scripts/rls-isolation-test.ts is EXTENDED (not rewritten) to prove a second user cannot read or write the first user's digest_preferences row, and npx tsc --noEmit stays clean."
  artifacts:
    - path: "supabase/migrations/20260718090000_daily_digest.sql"
      provides: "digest_preferences table with own-row RLS (SELECT/INSERT/UPDATE, auth.uid() = user_id)"
      contains: "digest_preferences"
    - path: "supabase/migrations/20260718090500_daily_digest_cron.sql"
      provides: "deploy-gated once-daily pg_cron + pg_net POST to /api/digest/run via app.settings custom settings"
      contains: "cron.schedule"
    - path: "scripts/rls-isolation-test.ts"
      provides: "two-user isolation coverage extended to digest_preferences"
      contains: "digest_preferences"
  key_links:
    - from: "supabase/migrations/20260718090500_daily_digest_cron.sql"
      to: "/api/digest/run"
      via: "net.http_post with Authorization Bearer from current_setting('app.settings.digest_run_secret', true)"
      pattern: "digest_run_secret"
    - from: "scripts/rls-isolation-test.ts"
      to: "digest_preferences cross-user isolation assertion"
      via: "user B reading/writing user A's row must fail or return zero rows"
      pattern: "digest_preferences"
---

<objective>
Create Phase 7's schema foundation: the user-level `digest_preferences` opt-in table (DGST-02) and the deploy-gated once-daily cron migration (DGST-01's "once per day"), plus RLS-test coverage. Deliberately NO change to `notifications_outbox` — Phase 5 pre-enumerated `daily_digest` in the kind CHECK.

Purpose: DGST-02 needs a durable enable/disable flag that survives Telegram re-linking. Research (07-RESEARCH.md Q1) ruled out a column on `telegram_links` (rows are DELETE+INSERT'd on relink, and the table deliberately has no authenticated UPDATE policy — the allowlist boundary) and `account_settings` (account-scoped mock-era; every digest concern is user-level). DGST-01's daily cadence follows the `price_refresh_cron.sql` deploy-gated pattern exactly.
Output: two new migration files + an extended RLS isolation test. Live apply of the preferences migration is consent-gated and DEFERRED to 07-05; the cron migration is deploy-gated and joins `price_refresh_cron.sql` on the never-apply-locally hold-back list.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-daily-digest/07-RESEARCH.md
@supabase/migrations/20260716221450_alerts_telegram.sql
@supabase/migrations/20260714220438_price_refresh_cron.sql
@scripts/rls-isolation-test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: digest_preferences migration (user-level opt-in, own-row RLS)</name>
  <files>supabase/migrations/20260718090000_daily_digest.sql</files>
  <action>
Create ONE new timestamped migration `supabase/migrations/20260718090000_daily_digest.sql` (house rule: never edit an existing migration — stated in the 20260714220333 and 20260716221450 headers). The timestamp must sort AFTER every existing migration including Phase 6's `20260717120000_news_pipeline.sql`; if an even-later-timestamped migration exists at execution time, bump this filename accordingly (and keep it before Task 2's cron file). Header comment: what/why, including the two REJECTED alternatives so future readers do not "fix" this into a telegram_links column (it would be lost on the DELETE+INSERT token regeneration in src/server-actions/telegram.ts and would require the deliberately-absent UPDATE policy).

```sql
CREATE TABLE IF NOT EXISTS public.digest_preferences (
    user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE PRIMARY KEY,
    enabled BOOLEAN NOT NULL DEFAULT FALSE,   -- opt-in: no row == disabled (honest default)
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

ENABLE ROW LEVEL SECURITY. Add exactly three own-row policies (the telegram_links policy shape, alerts_telegram.sql:73-75, PLUS an UPDATE policy — allowed here, unlike telegram_links, because a plain boolean preference has no allowlist boundary and toggling is the point; state this in a comment):
- SELECT USING (auth.uid() = user_id)
- INSERT WITH CHECK (auth.uid() = user_id)
- UPDATE USING (auth.uid() = user_id)
No DELETE policy (toggle is an upsert, never a delete). The digest sweep reads this table with the service role (bypasses RLS) — no extra policy needed for that.

Do NOT touch notifications_outbox in any way: its kind CHECK already enumerates 'daily_digest' (alerts_telegram.sql:90-91 — "All three roadmapped kinds enumerated NOW so Phases 6/7 need no migration").
  </action>
  <verify>
`grep -n "digest_preferences" supabase/migrations/20260718090000_daily_digest.sql` shows the table + three policies; `grep -in "notifications_outbox" supabase/migrations/20260718090000_daily_digest.sql` returns NOTHING (except possibly the header comment explaining why not); `grep -inE "policy .* delete .* digest_preferences" ...` returns NOTHING; `git diff --stat` shows zero existing migrations changed.
  </verify>
  <done>digest_preferences exists in a new migration with own-row SELECT/INSERT/UPDATE RLS and enabled DEFAULT FALSE; notifications_outbox is untouched; no existing migration edited.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy-gated once-daily digest cron migration</name>
  <files>supabase/migrations/20260718090500_daily_digest_cron.sql</files>
  <action>
Create `supabase/migrations/20260718090500_daily_digest_cron.sql` as a structural clone of `20260714220438_price_refresh_cron.sql` (read it first — copy its header discipline verbatim, adapted):

- Header comment: DEFERRED/deploy-gated — this job POSTs to a public URL Supabase's cloud must reach; applying it while running only on localhost schedules a job that fails every day. NEVER apply locally; apply only at deploy time (07-05 records this as deploy-deferred, same as price_refresh_cron/STATE.md STILL-OPEN item 1). Also document the one-time operator setup (never via a migration file, so no secret enters git):
  `ALTER DATABASE postgres SET app.settings.digest_run_url = 'https://<deployed-domain>/api/digest/run';`
  `ALTER DATABASE postgres SET app.settings.digest_run_secret = '<value of DIGEST_RUN_SECRET from .env.local / Vercel env>';`
  (Defanged placeholder text ONLY — never a realistic-format secret value anywhere in this committed file.)
- `CREATE EXTENSION IF NOT EXISTS pg_cron; CREATE EXTENSION IF NOT EXISTS pg_net;` (idempotent re-declare, same as the precedent file).
- Schedule (pg_cron evaluates cron expressions in UTC — note this in a comment): once daily at 03:15 UTC = 08:45 IST, deliberately 15 minutes after the 03:00 UTC tick of the 3-hourly price refresh so the digest composes from prices at most ~15 min old:

```sql
SELECT cron.schedule(
  'daily-digest-0845-ist',
  '15 3 * * *', -- 03:15 UTC == 08:45 IST daily (IST is fixed UTC+5:30, no DST); runs 15 min after the 03:00 UTC price refresh
  $$
  SELECT net.http_post(
    url := current_setting('app.settings.digest_run_url', true),
    headers := jsonb_build_object(
      'Authorization', 'Bearer ' || current_setting('app.settings.digest_run_secret', true),
      'Content-Type', 'application/json'
    ),
    body := '{}'::jsonb
  );
  $$
);
```

Until the operator settings exist, current_setting(..., true) returns NULL and the job fails loudly in cron.job_run_details — never silently (same honesty note as the precedent file).
  </action>
  <verify>
`grep -n "cron.schedule" supabase/migrations/20260718090500_daily_digest_cron.sql` shows the job with `'15 3 * * *'`; `grep -n "digest_run_url\|digest_run_secret" ...` shows both custom settings read via current_setting; `git grep -E "[0-9]{8,10}:[A-Za-z0-9_-]{30,}" -- supabase/migrations/20260718090500_daily_digest_cron.sql` returns NOTHING (no realistic secret); `git diff --stat` shows zero existing migrations changed.
  </verify>
  <done>A deploy-gated once-daily cron migration exists, secret-free, UTC-documented, cloning the price_refresh_cron precedent, and is understood to be authored-but-never-applied-locally.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the two-user RLS isolation test for digest_preferences</name>
  <files>scripts/rls-isolation-test.ts</files>
  <action>
EXTEND (do not rewrite or reorder) scripts/rls-isolation-test.ts with a digest_preferences block following the file's existing user-owned-table style (the telegram_links block from 05-01 is the closest precedent — find it and mirror):
- User A (authenticated client) INSERTs their own row `{ user_id: <A's id>, enabled: true }` — MUST succeed (own-row INSERT policy).
- User B SELECTing rows MUST NOT see A's row (own-row SELECT: either zero rows or only B's own).
- User B attempting UPDATE of A's row (e.g. `.update({ enabled: false }).eq('user_id', <A's id>)`) MUST affect ZERO rows (RLS filters it out — assert the returned data is empty / no row changed, matching how the existing test proves the telegram_links UPDATE closure).
- User A UPDATEs their own row to `enabled: false` — MUST succeed (the toggle path DGST-02 depends on).
- Clean up A's row via the service-role client if the file's style cleans up after itself (match existing style).
Note in a comment: this block cannot pass live until the still-pending migrations (csv_import, alerts_telegram, news_pipeline if authored, daily_digest) are pushed — the test currently honest-FAILs earlier at `import_batches not found` (STATE.md). The bar for THIS task: assertions present and correctly shaped, `npx tsc --noEmit` clean. Live run DEFERRED to 07-05.
  </action>
  <verify>`grep -n "digest_preferences" scripts/rls-isolation-test.ts` shows insert-own/cross-read-blocked/cross-update-zero-rows/own-update assertions; `npx tsc --noEmit` clean. Run `npm run test:rls` and observe the honest pre-existing failure (at import_batches, before the new block) proving the deferral is real, not fabricated.</verify>
  <done>The isolation test proves digest_preferences is user-isolated (cross-user read empty, cross-user update zero rows, own insert/update work); tsc clean; live run deferred to 07-05.</done>
</task>

</tasks>

<verification>
- `git diff --stat` / `git show HEAD --stat` per commit: exactly the two new migration files + scripts/rls-isolation-test.ts changed; NO existing migration touched; NO notifications_outbox DDL anywhere.
- `git grep -E "[0-9]{8,10}:[A-Za-z0-9_-]{30,}" -- supabase/migrations scripts` returns nothing (secret-scanning rule).
- `npx tsc --noEmit` clean.
- Live apply + live `npm run test:rls` DEFERRED to 07-05 (consent-gated preferences migration; deploy-gated cron migration — the latter must NEVER be applied from localhost).
</verification>

<success_criteria>
digest_preferences exists in a new migration with honest opt-in default and own-row RLS; the once-daily cron is authored deploy-gated and secret-free; the RLS test covers the new table — all statically verified, live checks deferred to 07-05.
</success_criteria>

<output>
After completion, create `.planning/phases/07-daily-digest/07-01-SUMMARY.md`.
</output>
