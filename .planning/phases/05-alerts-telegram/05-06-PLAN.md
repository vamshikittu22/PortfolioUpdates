---
phase: 05-alerts-telegram
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-04"]
files_modified:
  - src/lib/telegram/redeem.ts
  - src/lib/telegram/read.ts
  - src/server-actions/telegram.ts
  - src/app/api/telegram/webhook/route.ts
autonomous: true
requirements: [ALRT-01]

must_haves:
  truths:
    - "A signed-in user can generate a single-use, high-entropy link token (43-char base64url, 15-min TTL) and see a `https://t.me/<TELEGRAM_BOT_USERNAME>?start=<token>` link — generated by a cookie-bound Server Action that writes a pending telegram_links row for THAT user (ALRT-01)."
    - "redeemStartToken binds the chat_id to a token's user only if the token exists, is unexpired, and is still pending — an unknown/expired/used token binds nothing, and marking it linked is idempotent (a replayed /start update cannot double-bind); the chat_id/status='linked' write happens with the admin client because telegram_links has NO authenticated UPDATE policy (the allowlist closure) (ALRT-01)."
    - "The dev-mode handshake works WITHOUT a public URL: an auth-gated 'I've sent /start' Server Action polls getTelegramUpdates(timeout:0) from the server, finds `/start <token>` via the pure parseStartPayload, and redeems it — no persistent poller, no webhook set in dev (05-RESEARCH-telegram-api Q3)."
    - "The webhook route verifies the X-Telegram-Bot-Api-Secret-Token header against TELEGRAM_WEBHOOK_SECRET BEFORE any Supabase client is constructed (clone of the /api/prices/refresh guard ordering), then redeems via the same redeemStartToken — but live setWebhook is deploy-gated and DEFERRED (Telegram cannot reach localhost), exactly like price_refresh_cron.sql (ALRT-01)."
    - "A user can unlink (DELETE their telegram_links row), and getTelegramLink reports the current status/username so the UI can show a linked/pending/unlinked state honestly."
  artifacts:
    - path: "src/lib/telegram/redeem.ts"
      provides: "redeemStartToken(admin, token, chatId) — validate token + bind chat_id + mark linked (idempotent)"
      exports: ["redeemStartToken"]
    - path: "src/server-actions/telegram.ts"
      provides: "generateTelegramLink / checkTelegramLink / unlinkTelegram Server Actions"
      exports: ["generateTelegramLink", "checkTelegramLink", "unlinkTelegram"]
    - path: "src/lib/telegram/read.ts"
      provides: "getTelegramLink(supabase, userId) → link status for the RSC"
      exports: ["getTelegramLink"]
    - path: "src/app/api/telegram/webhook/route.ts"
      provides: "secret-header-guarded POST that redeems /start updates (deploy-gated setWebhook)"
      contains: "x-telegram-bot-api-secret-token"
  key_links:
    - from: "src/server-actions/telegram.ts (checkTelegramLink)"
      to: "getTelegramUpdates (05-04) + parseStartPayload (05-02) + redeemStartToken"
      via: "auth gate → admin client poll → parse → redeem (dev handshake, no public URL)"
      pattern: "getTelegramUpdates"
    - from: "src/lib/telegram/redeem.ts"
      to: "public.telegram_links"
      via: "admin client: validate pending+unexpired token, set chat_id + status='linked' + linked_at (idempotent)"
      pattern: "status.*linked|'linked'"
    - from: "src/app/api/telegram/webhook/route.ts"
      to: "TELEGRAM_WEBHOOK_SECRET header check before createAdminClient"
      via: "guard-before-Supabase, then redeemStartToken; live setWebhook deferred"
      pattern: "TELEGRAM_WEBHOOK_SECRET"
---

<objective>
Build the Telegram linking handshake (ALRT-01): the shared `redeemStartToken` binding logic (validate a single-use token, bind chat_id, mark linked — idempotent, admin client because telegram_links UPDATE is service-role-only), the user-facing Server Actions (`generateTelegramLink` renders the `t.me/<username>?start=<token>` deep link, `checkTelegramLink` does the dev-mode on-demand getUpdates poll + redeem, `unlinkTelegram` removes the link), the `getTelegramLink` read for the settings/alerts RSC, and the deploy-gated `POST /api/telegram/webhook` route (secret-header-guarded, same redeem path).

Purpose: this is the "link their Telegram account via a bot /start handshake, chat id captured + allowlisted" requirement. The single-use random token IS the auth (never encode user identity in the payload — 05-RESEARCH-telegram-api Q2), and the closed telegram_links UPDATE posture (05-01) IS the allowlist. Dev uses on-demand polling (works from localhost, no public URL); the webhook is authored but its live activation is deferred exactly like the price cron.
Output: `src/lib/telegram/{redeem,read}.ts`, `src/server-actions/telegram.ts`, `src/app/api/telegram/webhook/route.ts`. Static-verified; live handshake is exercised in 05-09.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-alerts-telegram/05-RESEARCH-telegram-api.md
@.planning/phases/05-alerts-telegram/05-01-PLAN.md
@.planning/phases/05-alerts-telegram/05-02-PLAN.md
@.planning/phases/05-alerts-telegram/05-04-PLAN.md
@src/server-actions/prices.ts
@src/server-actions/portfolio.ts
@src/app/api/prices/refresh/route.ts
@src/utils/supabase/admin.ts
@src/utils/supabase/server.ts
@src/proxy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: redeemStartToken binding + getTelegramLink read</name>
  <files>src/lib/telegram/redeem.ts, src/lib/telegram/read.ts</files>
  <action>
Create `src/lib/telegram/redeem.ts`. `export async function redeemStartToken(admin, token: string, chatId: number): Promise<{ ok: true; userId: string } | { ok: false; reason: string }>`:
1. Look up the token: `admin.from('telegram_links').select('user_id, status, token_expires_at, chat_id').eq('link_token', token).maybeSingle()`. Unknown token → `{ ok:false, reason:'unknown token' }` (do not leak which part failed beyond a generic reason).
2. If `status === 'linked'` and `chat_id === chatId` → treat as idempotent success `{ ok:true, userId }` (a replayed /start update must not error or double-bind — 05-RESEARCH-telegram-api Q2 replay note).
3. Reject if `status !== 'pending'` (already used/revoked) or `token_expires_at < now` → `{ ok:false, reason:'expired or already used' }`.
4. Bind: `admin.from('telegram_links').update({ chat_id: chatId, status: 'linked', linked_at: nowIso, updated_at: nowIso }).eq('link_token', token).eq('status', 'pending')` (the `.eq('status','pending')` makes the transition a compare-and-set — a concurrent redeem of the same token affects zero rows the second time). Admin client is REQUIRED (no authenticated UPDATE policy exists — 05-01's allowlist closure). Handle the `telegram_links_chat_unique` violation (this chat already backs another user) → `{ ok:false, reason:'this Telegram chat is already linked to another account' }`. On success return `{ ok:true, userId }`.

Create `src/lib/telegram/read.ts`. `export async function getTelegramLink(supabase, userId: string): Promise<{ status: 'pending'|'linked'|'revoked'|'none'; linkedAt: string | null }>` — cookie-bound RLS-scoped read (`from('telegram_links').select('status, linked_at').eq('user_id', userId).maybeSingle()`); no row → `{ status:'none', linkedAt:null }`. Plain lib function for the RSC (like get-portfolio-pnl.ts), never the admin client.
  </action>
  <verify>
1. `grep "status: 'linked'" src/lib/telegram/redeem.ts` → matches; `grep ".eq('status', 'pending')" src/lib/telegram/redeem.ts` → matches (compare-and-set bind).
2. Eyeball: an already-linked token with the SAME chat_id is idempotent success; expired/used tokens are rejected; the chat-unique violation is mapped to an honest reason; the bind uses the admin client.
3. `grep -c "from '@/utils/supabase/admin'" src/lib/telegram/read.ts` → 0 (read is cookie-bound).
4. `npx tsc --noEmit` passes.
  </verify>
  <done>redeemStartToken validates + idempotently binds a chat to a single-use unexpired token via the admin client (the only path to status='linked'), and getTelegramLink reports link status for the RSC via a cookie-bound read.</done>
</task>

<task type="auto">
  <name>Task 2: Telegram handshake Server Actions (generate / check-poll / unlink)</name>
  <files>src/server-actions/telegram.ts</files>
  <action>
Create `src/server-actions/telegram.ts` with `'use server';`. Import `generateLinkToken` + `parseStartPayload` (05-02), `getTelegramUpdates` (05-04), `redeemStartToken` (Task 1), `createClient` (`@/utils/supabase/server`) and `createAdminClient` (`@/utils/supabase/admin`), and `revalidatePath`.

- `generateTelegramLink(): Promise<{ ok: true; url: string } | { ok: false; error: string }>` — cookie-bound: `getUser()` gate (throw/return Unauthorized if null). Generate `const token = generateLinkToken();` and an `expiresAt = now + 15 min`. Upsert the user's pending link row (DELETE any existing row for this user then INSERT, or upsert on `user_id` PK) `{ user_id, link_token: token, token_expires_at: expiresAt, status: 'pending', chat_id: null }` via the COOKIE-bound client (INSERT own is allowed by RLS; re-generate = replace). Render `const url = \`https://t.me/${process.env.TELEGRAM_BOT_USERNAME}?start=${token}\`;` — `TELEGRAM_BOT_USERNAME` is an env var (NOT derivable from the token without getMe; keep link rendering token-free — 05-RESEARCH-telegram-api Q1); add it as a labeled placeholder to `.env.local` if absent. `revalidatePath('/alerts')`; return `{ ok:true, url }`.
- `checkTelegramLink(): Promise<{ ok: true; linked: boolean } | { ok: false; error: string }>` — the dev-mode on-demand poll. `getUser()` gate (cookie client). Then, using the ADMIN client (mirroring refreshPricesNow's auth-gate-then-admin precedent — the poll + redeem writes are service-role), call `const res = await getTelegramUpdates();` (timeout:0). If `!res.ok` surface its description (e.g. a 409 "webhook active — deleteWebhook for local dev" is honest, never swallowed). For each update, `const tok = parseStartPayload(update.message?.text ?? '');` if `tok` and `update.message?.chat?.id`, call `redeemStartToken(admin, tok, chatId)`. Acknowledge by calling `getTelegramUpdates(maxUpdateId + 1)` once after processing (confirms/clears; offset bookkeeping is stateless per 05-RESEARCH-telegram-api Q3 — re-scanning ≤24h backlog is negligible for a single-user app, and single-use `status`/`.eq('status','pending')` makes reprocessing idempotent). Determine `linked` by re-reading the caller's own link status. `revalidatePath('/alerts')`; return `{ ok:true, linked }`.
- `unlinkTelegram(): Promise<{ ok: true } | { ok: false; error: string }>` — cookie-bound `getUser()` gate; `delete from telegram_links where user_id = user.id` (DELETE own allowed by RLS). `revalidatePath('/alerts')`.

Do NOT set a webhook anywhere in dev (05-RESEARCH-telegram-api Pitfall 2 — a webhook makes getUpdates 409).
  </action>
  <verify>
1. `grep "generateLinkToken" src/server-actions/telegram.ts` → matches; `grep "TELEGRAM_BOT_USERNAME" src/server-actions/telegram.ts` → matches (link rendered from env, token-free).
2. `grep "getTelegramUpdates" src/server-actions/telegram.ts` → matches; `grep "parseStartPayload" src/server-actions/telegram.ts` → matches; `grep "redeemStartToken" src/server-actions/telegram.ts` → matches (poll → parse → redeem).
3. Eyeball: `generateTelegramLink`/`unlinkTelegram` use the COOKIE client; `checkTelegramLink` uses `getUser()` THEN the admin client (like refreshPricesNow); a non-ok getUpdates (e.g. 409) is surfaced, never swallowed; no `setWebhook` call anywhere.
4. `npx tsc --noEmit` passes.
  </verify>
  <done>The three handshake Server Actions exist: generateTelegramLink (pending row + t.me deep link), checkTelegramLink (auth-gated admin-client getUpdates poll → parse → redeem, honest 409 surfacing), unlinkTelegram (delete own) — the no-public-URL dev handshake.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy-gated webhook route (secret header guard + redeem)</name>
  <files>src/app/api/telegram/webhook/route.ts</files>
  <action>
Create `src/app/api/telegram/webhook/route.ts` — the production inbound path, authored now but with live activation DEFERRED (Telegram cannot reach localhost; setWebhook belongs to the deploy checkpoint, gated exactly like price_refresh_cron.sql — 05-RESEARCH-telegram-api Q3/Prescription 4). Verify the route-handler signature against this repo's bundled Next 16.2.9 docs per AGENTS.md (`export async function POST(request: Request)`, same as the refresh route).

```ts
import { NextResponse } from 'next/server';
import { parseStartPayload } from '@/lib/telegram/parse-start-payload';
import { redeemStartToken } from '@/lib/telegram/redeem';
import { createAdminClient } from '@/utils/supabase/admin';

// ALRT-01 — production inbound webhook. Telegram delivers its secret in the
// X-Telegram-Bot-Api-Secret-Token header (set via setWebhook's secret_token).
// The header is checked BEFORE any Supabase client is constructed — same
// guard-before-Supabase ordering as /api/prices/refresh. /api/* is already
// exempt from proxy login redirects (proxy.ts:54), so this handler is reached.
// DEPLOY-GATED: live setWebhook is deferred to 05-09 — never set a webhook while
// only running locally (it would 409 the dev getUpdates poll).
export async function POST(request: Request) {
  const secret = request.headers.get('x-telegram-bot-api-secret-token');
  const expected = process.env.TELEGRAM_WEBHOOK_SECRET ?? '';
  if (!expected || secret !== expected) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }
  try {
    const update = await request.json();
    const text: string = update?.message?.text ?? '';
    const chatId: number | undefined = update?.message?.chat?.id;
    const token = parseStartPayload(text);
    if (token && typeof chatId === 'number') {
      await redeemStartToken(createAdminClient(), token, chatId);
    }
    // Always 200 so Telegram does not retry a non-/start update forever.
    return NextResponse.json({ ok: true });
  } catch (error) {
    console.error('Telegram webhook failed:', error);
    return NextResponse.json({ ok: false }, { status: 200 });
  }
}
```

Mirror the empty-secret-always-denies property of isAuthorizedRefreshRequest (an unset `TELEGRAM_WEBHOOK_SECRET` must reject, never become an open endpoint). Add `TELEGRAM_WEBHOOK_SECRET` as a labeled `.env.local` placeholder if absent.
  </action>
  <verify>
1. `grep "x-telegram-bot-api-secret-token" src/app/api/telegram/webhook/route.ts` → matches.
2. `grep "TELEGRAM_WEBHOOK_SECRET" src/app/api/telegram/webhook/route.ts` → matches; eyeball: an empty/unset expected secret rejects (no open endpoint).
3. Eyeball: the header check returns 401 BEFORE `createAdminClient()`; redemption reuses `redeemStartToken`; no `setWebhook` call (activation deferred).
4. `npx tsc --noEmit` passes; `npm run build` compiles (route registered).
  </verify>
  <done>POST /api/telegram/webhook verifies the Telegram secret header before any Supabase call and redeems /start updates via the shared redeemStartToken; live setWebhook activation is deferred to the deploy checkpoint (05-09), never enabled locally.</done>
</task>

</tasks>

<verification>
**Static (run now):** grep checks above; `npx tsc --noEmit` clean; `npm run build` succeeds; review confirms the token-is-the-auth binding, admin-client-only linking (allowlist closure), cookie-bound generate/unlink, honest 409 surfacing, and guard-before-Supabase on the webhook.

**DEFERRED (needs a real bot token, TELEGRAM_BOT_USERNAME, the 05-01 migration applied, and — for the webhook — a public deploy):**
- Token-gated, runnable from localhost once BotFather is done: the real /start handshake E2E (click Link → open t.me link → tap START → "I've sent /start" polls getUpdates and binds the real chat_id).
- Deploy-gated: `setWebhook` with the secret_token + `getWebhookInfo` verification (same treatment as price_refresh_cron.sql — Telegram cannot reach localhost).
Both exercised in 05-09.
</verification>

<success_criteria>
`src/lib/telegram/{redeem,read}.ts`, `src/server-actions/telegram.ts`, and `src/app/api/telegram/webhook/route.ts` implement the full handshake: single-use token generation + deep link, idempotent admin-client binding (the only path to 'linked'), the no-public-URL dev poll, unlink, status read, and a secret-header-guarded deploy-gated webhook. tsc + build clean; live handshake + setWebhook deferred to 05-09.
</success_criteria>

<output>
After completion, create `.planning/phases/05-alerts-telegram/05-06-SUMMARY.md`
</output>
