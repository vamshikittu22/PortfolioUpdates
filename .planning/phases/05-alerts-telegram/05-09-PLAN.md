---
phase: 05-alerts-telegram
plan: 09
type: execute
wave: 5
depends_on: ["05-05", "05-06", "05-08"]
files_modified: []
autonomous: false
requirements: [ALRT-01, ALRT-02, ALRT-03, ALRT-05]

user_setup:
  - service: telegram-bot
    why: "A real bot token is required to make any live Telegram call (handshake, sends). No token exists yet; the entire logic layer was built and TDD'd token-free."
    account_setup: "Message @BotFather → /newbot → supply a display name and a username ending in 'bot' (e.g. folio_intel_bot). BotFather returns the auth token."
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "@BotFather /newbot reply (format <bot-id>:<secret>)"
      - name: TELEGRAM_BOT_USERNAME
        source: "the bot username chosen in BotFather (without @) — needed to render t.me/<username>?start=<token>"
      - name: TELEGRAM_WEBHOOK_SECRET
        source: "generate a high-entropy string (A-Z a-z 0-9 _ -, 1-256 chars); used as setWebhook secret_token AND the webhook route's header check (deploy-time only)"
      - name: NOTIFY_DISPATCH_SECRET
        source: "generate a high-entropy string; used as the Authorization bearer for POST /api/notifications/dispatch"

must_haves:
  truths:
    - "A human confirms, once BotFather is done and the 05-01 migration is applied to the live DB with consent: linking Telegram via the /start handshake captures the real chat_id into telegram_links (status flips to 'linked') and the two-user RLS test passes green for price_alerts/telegram_links/notifications_outbox (ALRT-01, AUTH-04)."
    - "A human confirms setting a per-ticker price alert (threshold up/down) persists and shows current price vs threshold (ALRT-02)."
    - "A human confirms a real threshold crossing enqueues exactly one outbox row and delivers ONE Telegram message, and that re-running the refresh inside the cooldown window sends NO repeat (ALRT-03)."
    - "A human confirms a delivery failure is retried not lost: a row left pending after a simulated failure is re-attempted on the next dispatch/refresh, a permanent 403-blocked marks the row failed and revokes the link, and the standalone POST /api/notifications/dispatch lever works with its secret (ALRT-05)."
  artifacts: []
  key_links: []
---

<objective>
Close Phase 5's live-verification debt. Every prior plan (05-01 through 05-08) was authored and statically/unit verified (`npx tsc --noEmit`, `npm run test:telegram`, `npm run test:alerts` / `npx tsx scripts/alerts-eval-test.ts`, grep gates, `npm run build`) but nothing has run against a real bot token, the live database, or a delivered Telegram message. Three debts are specific to this phase: no `TELEGRAM_BOT_TOKEN` exists yet (user has not created a bot), the 05-01 migration is deliberately unapplied until consent, and the production webhook path is deploy-gated (Telegram cannot reach localhost).

Purpose: this is the phase's honesty gate. Phase 5's success criteria are observed behaviors — the handshake captures a real chat id, an alert fires exactly once with a cooldown, the outbox retries a failure rather than losing it. None is claimable from static checks alone.
Output: either a completed live verification, or an explicit DEFERRED record in the SUMMARY (reasons: no bot token / no migration-push consent / no public deploy) — never a fabricated pass, matching the 03-06 / 04-07 precedent.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-alerts-telegram/05-RESEARCH-telegram-api.md
@.planning/phases/05-alerts-telegram/05-RESEARCH-schema-outbox.md
@.planning/phases/05-alerts-telegram/05-01-PLAN.md
@.planning/phases/05-alerts-telegram/05-05-PLAN.md
@.planning/phases/05-alerts-telegram/05-06-PLAN.md
@.planning/phases/04-csv-import/04-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Bot provisioning + live end-to-end alerts/Telegram verification</name>
  <files>none (verification-only; no source is written by this task)</files>
  <what-built>
The complete Phase 5 feature: schema for price_alerts + telegram_links + notifications_outbox + claim_due_notifications (05-01), pure Telegram logic and alert evaluation TDD'd token-free (05-02, 05-03), the raw-fetch Telegram wrapper + retryable outbox + dispatch route (05-04), the alert CRUD + read (05-07), the evaluate-and-enqueue sweep piggybacked onto both refresh entry points (05-05), the linking handshake Server Actions + deploy-gated webhook (05-06), and the rewritten /alerts UI with the Telegram link card (05-08).

No bot token exists yet, the 05-01 migration is intentionally unapplied, and the webhook is deploy-gated — everything above was built and statically/unit verified but never run live. This checkpoint is where that gap closes (or is honestly deferred).
  </what-built>
  <action>
See how-to-verify. Verification-only — no source is written here. If a step fails, describe observed vs. expected so it can be fixed in a follow-up plan before Phase 5 is marked complete; do NOT silently patch code from within this checkpoint. Applying the 05-01 migration is a live-DB schema change requiring EXPLICIT user consent, and creating the bot is a user action — if either is unavailable, record the whole checkpoint DEFERRED (matching 03-06/04-07).

CAUTION carried from STATE.md: a blanket `supabase db push` would ALSO apply the deliberately-held-back `price_refresh_cron.sql` (and the still-unpushed `csv_import.sql`). Push SELECTIVELY / with per-file consent.
  </action>
  <how-to-verify>
Prerequisites: the user creates the bot via @BotFather and sets `TELEGRAM_BOT_TOKEN` / `TELEGRAM_BOT_USERNAME` / `TELEGRAM_WEBHOOK_SECRET` / `NOTIFY_DISPATCH_SECRET` in `.env.local`; consents to apply this phase's migration.

Token-gated checks — runnable from localhost the moment BotFather is done, NO deploy needed (05-RESEARCH-telegram-api verifiability tiers):
1. **Token valid:** a one-off `getMe` (or the app's getTelegramMe) returns the bot's username — cheapest token-valid probe.
2. **Apply the migration (with consent):** apply `<timestamp>_alerts_telegram.sql` (selectively — do NOT also push the held-back cron). Confirm `price_alerts`, `telegram_links`, `notifications_outbox`, and `claim_due_notifications` exist (Supabase Studio / `psql \d`), and that the legacy `alerts` table is gone.
3. **RLS test green:** `npm run test:rls` → PASS, now covering the three new tables (05-01 Task 3), including the telegram_links UPDATE-affects-zero-rows allowlist-closure proof.
4. **Handshake (ALRT-01):** `npm run dev`, sign in, go to /alerts → TelegramLinkCard → "Link Telegram" → open the `t.me/<username>?start=<token>` link → tap START in Telegram → "I've sent /start". Confirm the card flips to "linked" and `telegram_links` has your real `chat_id` with `status='linked'` (written only via the admin path). Confirm an expired/reused token does not re-link.
5. **Create alert (ALRT-02):** create a price alert on a held/priced ticker with a threshold just BELOW the current price for an 'above' alert (so it will trigger). Confirm it persists and the row shows current price vs threshold; confirm the duplicate (same ticker+direction) is rejected with the friendly message.
6. **Trigger + cooldown (ALRT-03):** click "Refresh now" (or curl the secret-guarded refresh route). Confirm the alert's price is beyond threshold → the sweep enqueues ONE notifications_outbox row (dedupe_key set) → the piggybacked dispatch delivers ONE Telegram message to your chat within seconds. Click "Refresh now" AGAIN inside the cooldown window → confirm NO second message (last_triggered_at + dedupe suppress it).
7. **Outbox retry, not lost (ALRT-05):** (a) Block the bot in Telegram, trigger an alert → confirm the row goes `status='failed'` and the telegram_links row flips to `revoked` (permanent 403 handling). (b) Unblock + re-link; enqueue a message while offline/misconfigured to force a retryable failure → confirm the row stays `pending` with `attempts` bumped and `next_attempt_at` in the future, and that a later `POST /api/notifications/dispatch` (with `Authorization: Bearer <NOTIFY_DISPATCH_SECRET>`) delivers it — proving separate dispatch + retry-on-next-run.
8. **Dispatch lever guard:** confirm `POST /api/notifications/dispatch` WITHOUT the correct secret returns 401 (guard-before-Supabase), and an unset secret also denies.

Deploy-gated checks — DEFERRED until a public HTTPS deploy exists (same treatment as PRICE-02's cron):
9. **Webhook:** after deploying, `setWebhook` with `url=<public>/api/telegram/webhook` + `secret_token=TELEGRAM_WEBHOOK_SECRET`; confirm `getWebhookInfo` shows it registered with zero errors and that a fresh `/start` binds via the webhook (remember: setting a webhook makes local getUpdates 409 — only do this on the deployed instance). Optionally schedule a dispatch cron pointing at `/api/notifications/dispatch` THEN (not needed for correctness — dispatch already rides the refresh cadence).
  </how-to-verify>
  <verify>All token-gated steps (1-8) pass against a real bot + a live Supabase, and the deploy-gated step (9) passes on a deployment — OR the checkpoint is recorded DEFERRED with the specific reason (no bot token / no migration consent / no deploy).</verify>
  <done>The live handshake captures a real chat_id, a per-ticker alert fires exactly once with a working cooldown, the outbox retries failures rather than losing them (and dead-letters/revokes permanent ones), and the RLS test is green for the new tables — OR the checkpoint is explicitly and honestly recorded DEFERRED (matching 03-06/04-07). Phase 5 is not claimed verified on static checks alone.</done>
  <resume-signal>Type "approved" once the token-gated steps pass (note step 9 as deploy-deferred if not yet deployed), or describe which step failed (observed vs. expected) so it can be fixed before Phase 5 is marked complete.</resume-signal>
</task>

</tasks>

<verification>
**DEFERRED / human checkpoint (Task 1):** everything requiring the user's bot token, the applied migration (consent-gated), a live database, a delivered Telegram message, and (for the webhook) a public deploy. If those are unavailable when this plan executes, record the checkpoint DEFERRED in the SUMMARY rather than fabricating a pass — this is the plan's entire purpose and its only task. Token-gated steps (1-8) are runnable from localhost once BotFather + migration consent exist; the webhook (9) is deploy-gated.
</verification>

<success_criteria>
The live verification either passes in full (token-gated steps 1-8 against a real bot + live Supabase; deploy-gated step 9 on a deployment) or is explicitly and honestly recorded DEFERRED pending the bot token, migration consent, and/or a public deploy. Phase 5 is not marked verified on the basis of static/unit checks alone.
</success_criteria>

<output>
After completion, create `.planning/phases/05-alerts-telegram/05-09-SUMMARY.md`
</output>
