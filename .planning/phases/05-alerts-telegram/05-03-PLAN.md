---
phase: 05-alerts-telegram
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/alerts/types.ts
  - src/lib/alerts/evaluate.ts
  - scripts/alerts-eval-test.ts
autonomous: true
requirements: [ALRT-02, ALRT-03]

must_haves:
  truths:
    - "evaluateAlerts fires an alert iff it is active AND the current price is beyond the threshold in its direction AND its cooldown has elapsed (last_triggered_at is null OR now − last_triggered_at > cooldown_minutes) — level+cooldown, NOT edge detection, so a triggered alert never repeats on every refresh (ALRT-03)."
    - "A null price NEVER triggers, and a price whose latest fetch failed (fetch_error set) NEVER triggers — the evaluator refuses to alert on a fabricated or knowingly-stale value (house never-fabricate rule)."
    - "computeAlertDedupeKey returns `price_alert:{alert_id}:{floor(epoch_seconds/(cooldown_minutes*60))}` — identical across re-runs inside one cooldown window (so a crash-recovery re-enqueue is DB-deduped) and different in the next window (so the next legitimate fire is allowed) (ALRT-05 idempotency backstop)."
    - "'above' fires only when price > threshold and 'below' only when price < threshold; exactly-equal does not fire (documented boundary), keeping direction semantics unambiguous (ALRT-02)."
    - "Every function is pure (no I/O, no DB, no clock beyond the injected `now`) and proven by `scripts/alerts-eval-test.ts` under node:assert/strict — verifiable NOW with no live DB."
  artifacts:
    - path: "src/lib/alerts/evaluate.ts"
      provides: "evaluateAlerts(alerts, pricesByInstrument, now) + computeAlertDedupeKey + isCooldownElapsed"
      exports: ["evaluateAlerts", "computeAlertDedupeKey"]
    - path: "src/lib/alerts/types.ts"
      provides: "shared alert-domain vocabulary (AlertDirection, AlertEvalRow, PriceSnapshot, TriggeredAlert)"
      contains: "AlertDirection"
    - path: "scripts/alerts-eval-test.ts"
      provides: "node:assert/strict unit coverage for triggering, cooldown, null/failed-price exclusion, dedupe keys"
      contains: "evaluateAlerts"
  key_links:
    - from: "src/lib/alerts/evaluate.ts"
      to: "src/lib/alerts/types.ts"
      via: "imports AlertEvalRow / PriceSnapshot / TriggeredAlert"
      pattern: "from '@/lib/alerts/types'"
---

<objective>
TDD the pure alert-evaluation core — the token-free, DB-free logic every later plan composes: `evaluateAlerts(alerts, pricesByInstrument, now)` (the level+cooldown trigger rule that excludes null and failed prices), `computeAlertDedupeKey(alert, now)` (the cooldown-window bucket key that backs idempotent enqueue), and the `src/lib/alerts/types.ts` shared vocabulary. Mirror `src/lib/prices/` layout: pure logic here, Supabase orchestration (the sweep that reads price_cache and enqueues) is plan 05-05's scope.

Purpose: the honesty-critical rules — never alert on a fabricated/pending/failed price, cooldown so a standing breach re-notifies at most once per window — live here as tested pure functions, exactly like `pnl-calculator.ts`/`ingest.ts`. This plan does NOT touch package.json (its `test:alerts` npm script is registered by plan 05-02); it verifies via a direct `npx tsx` invocation to stay disjoint within wave 1.
Output: `src/lib/alerts/{types,evaluate}.ts` and `scripts/alerts-eval-test.ts`. Proven by `npx tsx scripts/alerts-eval-test.ts` (and, once 05-02 lands, `npm run test:alerts`).
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-alerts-telegram/05-RESEARCH-schema-outbox.md
@src/lib/prices/ingest.ts
@scripts/price-pnl-test.ts
@src/lib/types.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED+GREEN — types + evaluateAlerts (level+cooldown, null/failed exclusion)</name>
  <files>src/lib/alerts/types.ts, src/lib/alerts/evaluate.ts, scripts/alerts-eval-test.ts</files>
  <action>
Follow `scripts/price-pnl-test.ts`'s pure-test style (`node:assert/strict`, `console.log('PASS')` + `process.exit(0)`, throw on failure, "Do NOT weaken these assertions" header).

RED — write `scripts/alerts-eval-test.ts` with assertions before the implementation exists. Use a small helper to build alert rows and a `Map<instrumentId, PriceSnapshot>`. Cases (05-RESEARCH-schema-outbox "Alert Evaluation" + Pitfall 3):
- above-alert, price > threshold, never triggered (last_triggered_at null) → fires.
- above-alert, price === threshold → does NOT fire (documented boundary).
- above-alert, price < threshold → does NOT fire.
- below-alert, price < threshold, cooldown elapsed → fires; price > threshold → not.
- is_active false, price way beyond threshold → does NOT fire.
- cooldown NOT elapsed (last_triggered_at = now − 10 min, cooldown_minutes = 1440) → does NOT fire; cooldown elapsed (last_triggered_at = now − 2 days) → fires.
- price snapshot is `{ price: null }` (never fetched) → does NOT fire even though nominally beyond threshold.
- price snapshot has `{ price: 100, fetchError: 'HTTP 500' }` (last fetch failed) → does NOT fire (never alert on a knowingly-stale value).
- an instrument with NO entry in the price map → does NOT fire.
- multiple alerts across instruments → returns exactly the ones that should fire, each as a TriggeredAlert carrying the alert id + the observed price.

GREEN — implement:
1. `src/lib/alerts/types.ts` (declarations-only): `export type AlertDirection = 'above' | 'below';` `export interface AlertEvalRow { id: string; accountId: string; userId: string; instrumentId: string; direction: AlertDirection; threshold: number; isActive: boolean; cooldownMinutes: number; lastTriggeredAt: string | null; }` `export interface PriceSnapshot { price: number | null; fetchError: string | null; }` `export interface TriggeredAlert { alert: AlertEvalRow; price: number; }`.
2. `src/lib/alerts/evaluate.ts`:
   - `isCooldownElapsed(lastTriggeredAt: string | null, cooldownMinutes: number, now: Date): boolean` → true if lastTriggeredAt null OR `now − lastTriggeredAt > cooldownMinutes*60_000`.
   - `evaluateAlerts(alerts: AlertEvalRow[], pricesByInstrument: Map<string, PriceSnapshot>, now: Date): TriggeredAlert[]` → for each alert: skip if `!isActive`; look up snapshot; skip if missing, `price === null`, or `fetchError !== null`; check direction (`above`: price > threshold; `below`: price < threshold, strict); check `isCooldownElapsed`; if all pass, push `{ alert, price }`. Pure — reads only the injected `now`, never `Date.now()` internally.

Explicitly NOT edge detection (05-RESEARCH-schema-outbox rationale): there is no previous-observed-price state; level+cooldown fully satisfies ALRT-03's "does not repeat on every refresh".
  </action>
  <verify>
1. `npx tsx scripts/alerts-eval-test.ts` → prints PASS, exit 0 (Task-1 assertions).
2. `npx tsc --noEmit` passes.
3. Eyeball: null-price and fetch_error snapshots are excluded BEFORE the threshold comparison; comparisons are strict (`>`/`<`), equal does not fire; `now` is a parameter, no internal `Date.now()`.
  </verify>
  <done>evaluateAlerts implements the level+cooldown trigger rule with null/failed-price exclusion and strict direction semantics, proven pure under node:assert/strict.</done>
</task>

<task type="tdd">
  <name>Task 2: RED+GREEN — computeAlertDedupeKey (cooldown-window bucket)</name>
  <files>src/lib/alerts/evaluate.ts, scripts/alerts-eval-test.ts</files>
  <action>
Extend the SAME test script, then implement, per 05-RESEARCH-schema-outbox "Deterministic dedupe_key".

RED — add assertions:
- `computeAlertDedupeKey(alert, now)` returns `price_alert:{alert.id}:{bucket}` where `bucket = Math.floor((now.getTime()/1000) / (cooldownMinutes*60))`.
- Two calls with the SAME alert and two `now`s inside the same cooldown window → identical key (this is what makes a crash-recovery re-enqueue DB-deduped).
- A `now` in the NEXT window (advance by cooldown_minutes) → a different key (the next legitimate fire is allowed).
- Different alert ids → different keys at the same instant.

GREEN — implement `computeAlertDedupeKey(alert: AlertEvalRow, now: Date): string`:
`return \`price_alert:${alert.id}:${Math.floor((now.getTime() / 1000) / (alert.cooldownMinutes * 60))}\`;`
Document the known imperfection in a comment: a crossing that straddles a bucket boundary can fire in two adjacent windows ~back-to-back once — acceptable per research. Note that Phase 6/7 will use `news_alert:{userId}:{urlHash}` / `daily_digest:{userId}:{YYYY-MM-DD}` with this same column.
  </action>
  <verify>
1. `npx tsx scripts/alerts-eval-test.ts` → PASS (Task 1 + Task 2 assertions).
2. `npx tsc --noEmit` passes.
3. Eyeball: the key format string is exactly `price_alert:{id}:{bucket}` and the bucket divides by `cooldownMinutes * 60`.
  </verify>
  <done>computeAlertDedupeKey produces the deterministic cooldown-window bucket key (stable within a window, distinct across windows), the idempotency backstop 05-05's enqueue-first write relies on, green under the same test script.</done>
</task>

</tasks>

<verification>
**Static (run now):** `npx tsx scripts/alerts-eval-test.ts` green; `npx tsc --noEmit` clean; greps confirm null/failed-price exclusion and the exact dedupe-key format. Pure logic only — no DB, network, or token — so nothing is deferred from THIS plan. (Note: `npm run test:alerts` becomes runnable once 05-02 registers it in package.json; this plan does not depend on that for verification.)
</verification>

<success_criteria>
`src/lib/alerts/{types,evaluate}.ts` exist as pure zero-I/O modules; `scripts/alerts-eval-test.ts` proves triggering, cooldown, null/failed-price exclusion, strict direction boundaries, and the dedupe-window key under node:assert/strict; the script passes and tsc is clean. package.json is untouched by this plan (disjoint within wave 1).
</success_criteria>

<output>
After completion, create `.planning/phases/05-alerts-telegram/05-03-SUMMARY.md`
</output>
