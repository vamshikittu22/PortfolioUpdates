---
phase: 05-alerts-telegram
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/<timestamp>_alerts_telegram.sql
  - scripts/rls-isolation-test.ts
autonomous: true
requirements: [ALRT-01, ALRT-02, ALRT-03, ALRT-05]

must_haves:
  truths:
    - "A user can own per-instrument price_alerts (direction above/below, threshold, cooldown state) scoped by account-ownership RLS — a second user can neither read nor write them (ALRT-02, AUTH-04 discipline)."
    - "The legacy Phase-1 `alerts` table (symbol-keyed, no cooldown, never written — the /alerts page renders alerts={[]}) is dropped and replaced, so no dead superseded table lingers (governing rule: a feature is not done until its mock module is deleted)."
    - "telegram_links is user-keyed with a closed UPDATE posture: an authenticated user can create a pending link and read/delete their own, but ONLY the service role can set chat_id/status='linked' — that closure IS the allowlist (ALRT-01)."
    - "notifications_outbox is payload-generic (kind + payload jsonb + dedupe_key) with a partial unique index on dedupe_key and NO authenticated write policy, so Phases 6/7 enqueue without schema changes and duplicate sends are DB-suppressed (ALRT-05)."
    - "claim_due_notifications is a SECURITY DEFINER function using FOR UPDATE SKIP LOCKED, revoked from anon/authenticated, so concurrent dispatchers (pg_cron refresh + a user's Refresh-now) never double-claim a row (ALRT-05)."
  artifacts:
    - path: "supabase/migrations/<timestamp>_alerts_telegram.sql"
      provides: "price_alerts (replacing legacy alerts) + telegram_links + notifications_outbox tables with RLS, plus the claim_due_notifications SECURITY DEFINER claim function"
      contains: "CREATE TABLE IF NOT EXISTS public.price_alerts"
    - path: "scripts/rls-isolation-test.ts"
      provides: "extended two-user isolation coverage for price_alerts, telegram_links, notifications_outbox"
      contains: "price_alerts"
  key_links:
    - from: "public.price_alerts.account_id"
      to: "public.investment_accounts.id"
      via: "foreign key + four account-ownership EXISTS-subquery RLS policies (verbatim shape from transactions/import_batches)"
      pattern: "REFERENCES public\\.investment_accounts\\(id\\)"
    - from: "public.telegram_links (UPDATE)"
      to: "service-role only (NO authenticated UPDATE policy)"
      via: "deliberately-closed posture = the allowlist boundary"
      pattern: "ENABLE ROW LEVEL SECURITY"
    - from: "public.claim_due_notifications"
      to: "public.notifications_outbox"
      via: "SECURITY DEFINER UPDATE ... WHERE id IN (SELECT ... FOR UPDATE SKIP LOCKED)"
      pattern: "FOR UPDATE SKIP LOCKED"
---

<objective>
Author the single new migration that underpins all of Phase 5: three new tables in ONE timestamped file — `price_alerts` (dropping and replacing the empty legacy Phase-1 `alerts` table), `telegram_links` (user-keyed, closed-UPDATE allowlist posture), and `notifications_outbox` (payload-generic, service-role-write-only, dedupe-backed) — plus the `claim_due_notifications` SECURITY DEFINER function that lets concurrent dispatchers claim outbox rows atomically. Then extend the existing two-user RLS isolation test to cover all three new tables.

Purpose: lock in the schema and RLS write-ownership boundaries before any evaluator, dispatcher, Server Action, or UI is written against them — every later Phase-5 plan builds on these shapes. This is the ONLY plan in the phase that touches database shape or RLS posture.
Output: one new migration file (static SQL review only — no local Docker; live apply is DEFERRED to the phase checkpoint 05-09 with explicit user consent, matching the 03-01→03-06 / 04-01→04-07 precedent) and an extended `scripts/rls-isolation-test.ts` (authored + typechecked now; live run DEFERRED to 05-09).
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-alerts-telegram/05-RESEARCH-schema-outbox.md
@supabase/migrations/20260714160720_instruments_transactions.sql
@supabase/migrations/20260715230011_csv_import.sql
@supabase/migrations/20260714032952_initial_schema.sql
@scripts/rls-isolation-test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop legacy alerts, create price_alerts (account-ownership RLS + cooldown state)</name>
  <files>supabase/migrations/&lt;timestamp&gt;_alerts_telegram.sql</files>
  <action>
Scaffold the file with `npx supabase migration new alerts_telegram` (pure filesystem scaffold, no Docker — same as every prior migration). Do NOT hand-pick a timestamp. Write the price_alerts section, copying the DDL VERBATIM from `.planning/phases/05-alerts-telegram/05-RESEARCH-schema-outbox.md` section "1. Drop the legacy `alerts` table, create `price_alerts`":

```sql
-- Phase 5: Alerts + Telegram — price alerts, Telegram links, notifications outbox.
-- NEW migration; edits NO existing migration (house rule: 20260714220333 header).
-- Assumes Phase 1/2/3 migrations are applied (they sort earlier by timestamp).

-- Legacy Phase-1 alerts table: symbol-keyed, no cooldown, never written (the
-- /alerts page renders alerts={[]}, no write path exists). Superseded by the
-- instrument-identity price_alerts below. Same drop-and-replace precedent as
-- 20260714160803_watchlist_instrument_identity.sql dropping holdings.
DROP TABLE IF EXISTS public.alerts;

CREATE TABLE IF NOT EXISTS public.price_alerts (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    account_id UUID REFERENCES public.investment_accounts(id) ON DELETE CASCADE NOT NULL,
    instrument_id UUID REFERENCES public.instruments(id) ON DELETE CASCADE NOT NULL,
    direction TEXT NOT NULL CHECK (direction IN ('above', 'below')),
    threshold NUMERIC NOT NULL CHECK (threshold > 0),
    is_active BOOLEAN NOT NULL DEFAULT TRUE,
    cooldown_minutes INT NOT NULL DEFAULT 1440 CHECK (cooldown_minutes >= 60),
    last_triggered_at TIMESTAMPTZ,          -- cooldown anchor; written ONLY by the evaluator (service role)
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE (account_id, instrument_id, direction)
);

ALTER TABLE public.price_alerts ENABLE ROW LEVEL SECURITY;
```

Then add the FOUR account-ownership RLS policies (SELECT/INSERT/UPDATE/DELETE) using the EXACT EXISTS-subquery shape from the transactions/import_batches migrations (05-RESEARCH-schema-outbox Pitfall 7: a subtly different subquery fails only at runtime — copy it character-for-character, only the table name differs). Then the three indexes (`idx_price_alerts_account_id`, `idx_price_alerts_instrument_id`, and the partial `idx_price_alerts_active ON price_alerts(instrument_id) WHERE is_active`).

Locked decisions to encode (research Open Questions, resolved here): ONE alert per (account, instrument, direction) — keep `UNIQUE (account_id, instrument_id, direction)` (do NOT relax to include threshold). Default cooldown = 1440 minutes (24h), floor 60. Do NOT add a column-level restriction on `last_triggered_at` (a user updating their own row is self-harm only; the evaluator writes it via the admin client anyway).
  </action>
  <verify>
1. `grep -c "CREATE TABLE IF NOT EXISTS public.price_alerts" supabase/migrations/*_alerts_telegram.sql` → 1.
2. `grep -c "DROP TABLE IF EXISTS public.alerts" supabase/migrations/*_alerts_telegram.sql` → 1.
3. `grep -c "CREATE POLICY" supabase/migrations/*_alerts_telegram.sql` → 4 so far (four on price_alerts; more added in Task 2).
4. `grep "UNIQUE (account_id, instrument_id, direction)" supabase/migrations/*_alerts_telegram.sql` → matches.
5. Eyeball: every price_alerts policy references `investment_accounts ... user_id = auth.uid()` identically to the transactions migration; `cooldown_minutes ... DEFAULT 1440 CHECK (cooldown_minutes >= 60)` present.
  </verify>
  <done>The migration drops the empty legacy alerts table and creates price_alerts with account-ownership RLS (four EXISTS-subquery policies), the one-per-direction unique constraint, cooldown state columns, and the active-alert partial index.</done>
</task>

<task type="auto">
  <name>Task 2: telegram_links + notifications_outbox + claim_due_notifications RPC</name>
  <files>supabase/migrations/&lt;timestamp&gt;_alerts_telegram.sql</files>
  <action>
APPEND to the SAME migration file (do not create a second file). Copy the DDL VERBATIM from `.planning/phases/05-alerts-telegram/05-RESEARCH-schema-outbox.md` sections 2, 3, and 4:

1. **telegram_links** (research section 2): `user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE PRIMARY KEY`, `chat_id BIGINT` (nullable until handshake — NEVER INT, Telegram ids exceed 32 bits), `link_token TEXT UNIQUE NOT NULL`, `token_expires_at TIMESTAMPTZ NOT NULL`, `status TEXT CHECK (status IN ('pending','linked','revoked')) DEFAULT 'pending'`, `linked_at`, timestamps, and `CONSTRAINT telegram_links_chat_unique UNIQUE (chat_id)`. Enable RLS. Add EXACTLY THREE policies: SELECT own (`auth.uid() = user_id`), INSERT own (`WITH CHECK auth.uid() = user_id`), DELETE own. Add NO UPDATE policy — include the research's comment block explaining that the closed UPDATE posture IS the allowlist (chat_id/status='linked' written only by the service role when a valid /start token arrives; re-link = DELETE + INSERT via Server Actions).

2. **notifications_outbox** (research section 3): `id`, `user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL`, `kind TEXT CHECK (kind IN ('price_alert','news_alert','daily_digest'))` (all three roadmapped kinds enumerated NOW so Phase 6/7 need no migration), `payload JSONB NOT NULL`, `dedupe_key TEXT`, `status TEXT CHECK (status IN ('pending','sent','failed')) DEFAULT 'pending'`, `attempts INT DEFAULT 0`, `next_attempt_at TIMESTAMPTZ DEFAULT NOW()`, `last_error TEXT`, `sent_at`, `created_at`. Enable RLS. Add ONE policy only: SELECT own (`auth.uid() = user_id`) — include the research comment that there are deliberately NO authenticated write policies (writes are service-role only, the exact closed posture of price_cache/fx_cache). Add the partial unique index `uniq_notifications_outbox_dedupe ON notifications_outbox(dedupe_key) WHERE dedupe_key IS NOT NULL` and the due index `idx_notifications_outbox_due ON notifications_outbox(next_attempt_at) WHERE status = 'pending'`.

3. **claim_due_notifications** (research section 4): copy the SECURITY DEFINER plpgsql function VERBATIM — it dead-letters rows with `attempts >= 8` to status='failed', then `RETURN QUERY UPDATE ... SET attempts = attempts + 1, next_attempt_at = NOW() + LEAST(POWER(2, attempts) * INTERVAL '1 minute', INTERVAL '6 hours') WHERE id IN (SELECT id ... WHERE status='pending' AND next_attempt_at <= NOW() AND attempts < 8 ORDER BY created_at LIMIT p_limit FOR UPDATE SKIP LOCKED) RETURNING o.*`. Include `SET search_path = public`. Follow with `REVOKE ALL ON FUNCTION public.claim_due_notifications(INT) FROM PUBLIC;` and `REVOKE ALL ... FROM anon, authenticated;` (system/service-role only — stricter than find_or_create_instrument, which authenticated users may call).
  </action>
  <verify>
1. `grep -c "CREATE TABLE IF NOT EXISTS public.telegram_links" supabase/migrations/*_alerts_telegram.sql` → 1.
2. `grep -c "CREATE TABLE IF NOT EXISTS public.notifications_outbox" supabase/migrations/*_alerts_telegram.sql` → 1.
3. `grep "chat_id BIGINT" supabase/migrations/*_alerts_telegram.sql` → matches (never INT).
4. `grep "FOR UPDATE SKIP LOCKED" supabase/migrations/*_alerts_telegram.sql` → matches.
5. `grep -c "SECURITY DEFINER" supabase/migrations/*_alerts_telegram.sql` → 1 (the claim function).
6. `grep "REVOKE ALL ON FUNCTION public.claim_due_notifications" supabase/migrations/*_alerts_telegram.sql` → matches (anon AND authenticated revoked).
7. `grep -c "CREATE POLICY" supabase/migrations/*_alerts_telegram.sql` → 8 total (4 price_alerts + 3 telegram_links + 1 notifications_outbox). Eyeball: NO UPDATE policy on telegram_links; NO write policy on notifications_outbox.
8. `grep "uniq_notifications_outbox_dedupe" supabase/migrations/*_alerts_telegram.sql` → matches (partial dedupe index).
  </verify>
  <done>The same migration now defines telegram_links (closed-UPDATE allowlist posture, BIGINT chat_id), notifications_outbox (generic kind/payload/dedupe, service-role-write-only, partial dedupe index), and the claim_due_notifications SKIP-LOCKED claim function revoked from all non-service roles.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the two-user RLS isolation test to the three new tables</name>
  <files>scripts/rls-isolation-test.ts</files>
  <action>
Extend the existing `scripts/rls-isolation-test.ts` (do NOT rewrite it) to prove the three new user-owned/user-readable tables enforce isolation, matching AUTH-04 discipline and the 04-01 Task 3 precedent. Follow the file's established structure exactly (two authenticated sessions User A / User B via the anon key, `node:assert/strict`, throw-on-failure, no new dependency):

1. **price_alerts** (account-owned, like transactions): User A inserts a price_alert for A's account (any seed instrument_id — resolve one the way the file already resolves fixtures; direction 'above', threshold 1, cooldown 60) — MUST succeed. User B `select()` A's alert by id → assert zero rows. User B `insert()` a price_alert carrying A's account_id → assert rejected.
2. **telegram_links** (user-keyed): User A inserts a pending link for A (`user_id` = A, a random `link_token`, `token_expires_at` in the future, status default 'pending') — MUST succeed. User B `select()` A's link → assert zero rows. User B `update()` A's link to set `chat_id`/status='linked' → assert it affects ZERO rows (there is NO authenticated UPDATE policy — this is the allowlist-closure proof; a nonzero effect is a hard failure). User A `update()` of their OWN link's chat_id is ALSO expected to affect zero rows (no UPDATE policy for anyone) — assert that too, documenting that only the service role can link.
3. **notifications_outbox** (service-role-write-only): User B (or A) attempting to `insert()` an outbox row → assert rejected (no authenticated INSERT policy). A user reading their own rows is allowed by the SELECT policy but there is nothing to read via the anon path; do not fabricate a row through it.
4. Reuse the file's existing PASS/exit-0 on success and throw/non-zero on failure; add the three tables to the summary line. Do NOT weaken any existing assertion.

This test needs a live Supabase to actually run (two real JWTs through PostgREST); authoring + typechecking happens now, the live run is DEFERRED to 05-09 alongside the migration apply — exactly what the file's header already documents.
  </action>
  <verify>
1. `npx tsc --noEmit` passes (new code typechecks against `@supabase/supabase-js`).
2. `grep -c "price_alerts" scripts/rls-isolation-test.ts` → ≥ 2; `grep -c "telegram_links" scripts/rls-isolation-test.ts` → ≥ 2; `grep -c "notifications_outbox" scripts/rls-isolation-test.ts` → ≥ 1.
3. Eyeball: the telegram_links UPDATE-affects-zero-rows assertion is present (the allowlist-closure proof); no existing assertion deleted or loosened.
4. `npm run test:rls` is NOT expected to pass here without a live DB — record DEFERRED (the script already exits clearly when `.env.local` lacks real credentials / the migration is unapplied).
  </verify>
  <done>scripts/rls-isolation-test.ts now covers two-user isolation for price_alerts (account-owned), telegram_links (owner read/insert/delete but NO update by anyone — allowlist closure), and notifications_outbox (no authenticated write), typechecks clean, and its live run is honestly deferred to 05-09.</done>
</task>

</tasks>

<verification>
**Static (run now):** grep + `git diff --stat` prove the migration is ONE new file and no existing migration was modified; `npx tsc --noEmit` proves the extended RLS test typechecks; SQL reviewed by eye for balanced statements, the verbatim account-ownership RLS subquery shape, the closed telegram_links/notifications_outbox write posture, and `FOR UPDATE SKIP LOCKED` in the claim function.

**DEFERRED (blocked on live Supabase — no Docker locally; live apply needs explicit user consent per .planning/STATE.md):**
- `npx supabase db push` of this migration against the hosted project. CAUTION carried from STATE.md: a blanket `supabase db push` would ALSO apply the deliberately-held-back `price_refresh_cron.sql` — push SELECTIVELY / with consent.
- Confirming the RLS/allowlist posture behaves as designed and that `claim_due_notifications` SKIP-LOCKED behavior is correct under concurrency.
- Running `npm run test:rls` green against the live DB.
All exercised in plan 05-09.
</verification>

<success_criteria>
One new migration under `supabase/migrations/` drops the legacy alerts table and adds price_alerts (account-ownership RLS, one-per-direction), telegram_links (closed-UPDATE allowlist, BIGINT chat_id), notifications_outbox (generic payload, service-role-write-only, dedupe index), and the claim_due_notifications SKIP-LOCKED SECURITY DEFINER function (revoked from anon/authenticated). No existing migration is modified. `scripts/rls-isolation-test.ts` is extended for all three tables and typechecks clean. Live apply and live RLS run are explicitly DEFERRED, never fabricated as passing.
</success_criteria>

<output>
After completion, create `.planning/phases/05-alerts-telegram/05-01-SUMMARY.md`
</output>
