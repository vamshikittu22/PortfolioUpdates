---
phase: 05-alerts-telegram
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/telegram/types.ts
  - src/lib/telegram/parse-start-payload.ts
  - src/lib/telegram/build-message.ts
  - src/lib/telegram/link-token.ts
  - src/lib/telegram/classify-send-error.ts
  - scripts/telegram-logic-test.ts
  - package.json
autonomous: true
requirements: [ALRT-01, ALRT-03]

must_haves:
  truths:
    - "A `/start <token>` message text parses to exactly its 43-char base64url token, a bare `/start` (or any non-start text) parses to null, and a payload with characters outside [A-Za-z0-9_-] is rejected — so the handshake never binds on a malformed payload (ALRT-01)."
    - "generateLinkToken() produces a 43-char base64url string whose charset exactly matches Telegram's deep-link payload rules ([A-Za-z0-9_-], ≤64 chars), and isValidLinkTokenShape rejects anything else (ALRT-01)."
    - "buildPriceAlertMessage HTML-escapes only &, <, > in interpolated values (never MarkdownV2) and truncates to ≤4096 chars, so a price like `1082.40` or `-2.3%` can never 400 the send with a parse-entities error (ALRT-03)."
    - "classifySendError maps 429→retryable(with retry_after seconds), 403 blocked / 400 chat-not-found / 400 parse-or-too-long → permanent, and 5xx/network → retryable, keying on error_code first and matching description by case-insensitive substring (never exact equality) — the exact contract the outbox dispatcher needs (ALRT-03/05 delivery taxonomy)."
    - "Every function is pure (no I/O, no env, no network) and proven by `scripts/telegram-logic-test.ts` under node:assert/strict — verifiable NOW with no bot token."
  artifacts:
    - path: "src/lib/telegram/parse-start-payload.ts"
      provides: "parseStartPayload(text) → token | null"
      exports: ["parseStartPayload"]
    - path: "src/lib/telegram/build-message.ts"
      provides: "escapeHtml + buildPriceAlertMessage (HTML parse_mode, ≤4096 truncation)"
      exports: ["escapeHtml", "buildPriceAlertMessage"]
    - path: "src/lib/telegram/link-token.ts"
      provides: "generateLinkToken + isValidLinkTokenShape"
      exports: ["generateLinkToken", "isValidLinkTokenShape"]
    - path: "src/lib/telegram/classify-send-error.ts"
      provides: "classifySendError(errorCode, description, retryAfter?) → { kind, retryAfterSeconds? }"
      exports: ["classifySendError"]
    - path: "scripts/telegram-logic-test.ts"
      provides: "node:assert/strict unit coverage for all pure telegram logic"
      contains: "assert"
  key_links:
    - from: "package.json scripts"
      to: "scripts/telegram-logic-test.ts + scripts/alerts-eval-test.ts"
      via: "test:telegram and test:alerts npm scripts (this plan is the SOLE package.json owner in wave 1)"
      pattern: "test:telegram"
    - from: "src/lib/telegram/build-message.ts"
      to: "Telegram HTML parse_mode contract"
      via: "3-entity escape, never MarkdownV2"
      pattern: "&amp;|&lt;|&gt;"
---

<objective>
TDD the entire pure Telegram logic layer — the functions that are verifiable NOW with no bot token: `parseStartPayload` (redeem the `/start <token>` handshake), `generateLinkToken`/`isValidLinkTokenShape` (single-use high-entropy deep-link token), `escapeHtml`/`buildPriceAlertMessage` (HTML parse_mode message builder that sidesteps the MarkdownV2 escaping footgun and truncates to 4096), and `classifySendError` (the retryable-vs-permanent taxonomy the outbox dispatcher depends on). Split pure-vs-network exactly like `src/lib/prices/` (pure `ingest.ts` vs network `fetch-prices.ts`); the network wrapper `api.ts` is plan 05-04's scope. This plan also OWNS package.json for the phase's new test-script registrations.

Purpose: these are the token-free, TDD-able primitives (Tier "Verifiable NOW" from 05-RESEARCH-telegram-api). Getting the error taxonomy and HTML escaping right here is what makes the dispatcher (05-04) and handshake (05-06) thin, correct, and never-fabricating.
Output: `src/lib/telegram/{types,parse-start-payload,build-message,link-token,classify-send-error}.ts`, `scripts/telegram-logic-test.ts`, and `package.json` with `test:telegram` + `test:alerts` registered. Proven by `npm run test:telegram`.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-alerts-telegram/05-RESEARCH-telegram-api.md
@src/lib/prices/ingest.ts
@scripts/price-pnl-test.ts
@package.json
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED+GREEN — parse-start-payload, link-token, build-message</name>
  <files>src/lib/telegram/types.ts, src/lib/telegram/parse-start-payload.ts, src/lib/telegram/link-token.ts, src/lib/telegram/build-message.ts, scripts/telegram-logic-test.ts, package.json</files>
  <action>
Follow the RED→GREEN cycle from `scripts/price-pnl-test.ts`'s style (pure, `node:assert/strict`, `console.log('PASS')` + `process.exit(0)` on success, throw on failure, "Do NOT weaken these assertions" header).

RED — first register the npm scripts and write failing tests:
1. `package.json`: add BOTH `"test:telegram": "tsx scripts/telegram-logic-test.ts"` AND `"test:alerts": "tsx scripts/alerts-eval-test.ts"` to the scripts block (this plan is the sole wave-1 package.json owner; `test:alerts` is registered here for plan 05-03 which does NOT touch package.json — 05-03 verifies via `npx tsx` directly). Do not add any dependency — the Telegram layer is raw-fetch/zero-dep (05-RESEARCH-telegram-api Q5).
2. `scripts/telegram-logic-test.ts`: write assertions BEFORE the implementations exist:
   - `parseStartPayload('/start AbC-_123...')` → that exact token; `parseStartPayload('/start')` → null; `parseStartPayload('hello')` → null; `parseStartPayload('/start tok with space')` → null (payload must be a single [A-Za-z0-9_-] run); a payload with a `.` or `!` → null.
   - `generateLinkToken()` → matches `/^[A-Za-z0-9_-]{43}$/` and `isValidLinkTokenShape(it)` true; `isValidLinkTokenShape('bad token!')` false; `isValidLinkTokenShape('')` false. Call generateLinkToken twice → the two differ (entropy).
   - `escapeHtml('a & b < c > d')` → `'a &amp; b &lt; c &gt; d'` (ONLY those three entities; a quote/apostrophe is left as-is — HTML text-node escaping, not attribute). `buildPriceAlertMessage(...)` for an above-alert renders a message CONTAINING the display symbol (escaped), the threshold, and the current price, uses `<b>`/`<code>` tags (HTML mode), and a 5000-char forced input truncates the RESULT to ≤4096.

GREEN — implement to pass:
1. `src/lib/telegram/types.ts` — declarations-only shared vocabulary (import `Currency`/`Exchange` from `@/lib/types` if useful): e.g. `SendErrorKind = 'retryable' | 'permanent'`, `SendErrorClassification = { kind: SendErrorKind; retryAfterSeconds?: number }`, `PriceAlertMessageInput = { displaySymbol: string; direction: 'above'|'below'; threshold: number; price: number; currency: string }`.
2. `parse-start-payload.ts` — `parseStartPayload(text: string): string | null`: return null unless text starts with `/start ` and the remainder is a single non-empty `[A-Za-z0-9_-]+` run (no whitespace); return that run. Reference the 64-char limit from 05-RESEARCH-telegram-api Q2.
3. `link-token.ts` — `generateLinkToken(): string` = `crypto.randomBytes(32).toString('base64url')` (`node:crypto`); `isValidLinkTokenShape(t: string): boolean` = `/^[A-Za-z0-9_-]{1,64}$/.test(t)` (accept the general deep-link charset/length, but generateLinkToken always yields 43).
4. `build-message.ts` — `escapeHtml(s: string): string` replacing `&`→`&amp;`, `<`→`&lt;`, `>`→`&gt;` (in that order, & first); `buildPriceAlertMessage(input): string` composing an HTML-parse-mode string (e.g. `<b>${escapeHtml(displaySymbol)}</b> crossed ${direction} ${threshold} — now <code>${price}</code>`), then `.slice(0, 4096)`. HTML mode ONLY — never MarkdownV2 (05-RESEARCH-telegram-api Q4 / Pitfall 1).
  </action>
  <verify>
1. `npm run test:telegram` → PASS (all Task-1 assertions).
2. `npx tsc --noEmit` passes.
3. `grep -c "test:telegram\|test:alerts" package.json` → both present.
4. `grep "MarkdownV2" src/lib/telegram/build-message.ts` → NO match (HTML mode only); `grep "randomBytes(32)" src/lib/telegram/link-token.ts` → matches.
  </verify>
  <done>parseStartPayload, generateLinkToken/isValidLinkTokenShape, and escapeHtml/buildPriceAlertMessage are implemented, pure, and green under node:assert/strict; both phase test scripts are registered in package.json.</done>
</task>

<task type="tdd">
  <name>Task 2: RED+GREEN — classify-send-error (delivery taxonomy)</name>
  <files>src/lib/telegram/classify-send-error.ts, scripts/telegram-logic-test.ts</files>
  <action>
Extend the SAME test script, then implement, encoding the error taxonomy table from 05-RESEARCH-telegram-api Q4 EXACTLY (key on error_code first; match description by case-insensitive substring, never exact equality — Telegram says codes/descriptions are "subject to change").

RED — add assertions:
- `classifySendError(429, 'Too Many Requests: retry after 23', 23)` → `{ kind: 'retryable', retryAfterSeconds: 23 }`.
- `classifySendError(429, 'Too Many Requests: retry after 5')` with retryAfter arg omitted → retryable (retryAfterSeconds may be undefined; kind must be 'retryable').
- `classifySendError(403, 'Forbidden: bot was blocked by the user')` → `{ kind: 'permanent' }`.
- `classifySendError(400, 'Bad Request: chat not found')` → permanent.
- `classifySendError(400, "Bad Request: can't parse entities ...")` → permanent; `classifySendError(400, 'Bad Request: message is too long')` → permanent.
- `classifySendError(500, 'Internal Server Error')` → retryable; `classifySendError(null, 'network timeout')` (no HTTP code — transit failure) → retryable.
- Case-insensitivity: `classifySendError(403, 'FORBIDDEN: BOT WAS BLOCKED BY THE USER')` → permanent.

GREEN — implement `classifySendError(errorCode: number | null, description: string, retryAfterSeconds?: number): SendErrorClassification`:
- 429 → `{ kind: 'retryable', retryAfterSeconds }`.
- 403 → permanent (blocked / can't-initiate — should not occur with a handshake-bound allowlist, still permanent).
- 400 with description substring `chat not found` / `parse entities` / `too long` → permanent (bad allowlist entry or a builder bug — do not retry the same bytes).
- Any other 400 → permanent (unknown client error; don't hammer).
- 5xx OR errorCode === null (network/transit) → retryable.
- Default (unclassified) → retryable with backoff (safer to retry-then-dead-letter via attempts cap than to lose silently). Do substring matching with `description.toLowerCase().includes(...)`.
  </action>
  <verify>
1. `npm run test:telegram` → PASS (Task 1 + Task 2 assertions).
2. `npx tsc --noEmit` passes.
3. Eyeball: matching is on `error_code` first, descriptions via `.toLowerCase().includes(...)` (never `===`).
  </verify>
  <done>classifySendError returns the retryable/permanent classification (with retry_after for 429) keyed on error_code + case-insensitive description substring, green under the same test script — the delivery contract the outbox dispatcher (05-04) consumes.</done>
</task>

</tasks>

<verification>
**Static (run now):** `npm run test:telegram` green; `npx tsc --noEmit` clean; greps confirm HTML-only messaging, randomBytes token, error_code-first classification, and both npm scripts registered. Everything here is pure — no bot token, DB, or network is touched, so nothing is deferred from THIS plan.
</verification>

<success_criteria>
`src/lib/telegram/{types,parse-start-payload,build-message,link-token,classify-send-error}.ts` exist as pure, zero-I/O modules; `scripts/telegram-logic-test.ts` proves parsing, token shape, HTML escaping/truncation, and the full error taxonomy under node:assert/strict; `npm run test:telegram` passes and tsc is clean. package.json registers `test:telegram` and `test:alerts`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-alerts-telegram/05-02-SUMMARY.md`
</output>
