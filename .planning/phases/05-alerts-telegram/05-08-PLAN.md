---
phase: 05-alerts-telegram
plan: 08
type: execute
wave: 4
depends_on: ["05-06", "05-07"]
files_modified:
  - src/lib/types.ts
  - src/components/dashboard/AlertsTable.tsx
  - src/components/dashboard/AlertFormDialog.tsx
  - src/components/dashboard/TelegramLinkCard.tsx
  - src/app/(dashboard)/alerts/page.tsx
autonomous: true
requirements: [ALRT-01, ALRT-02]

must_haves:
  truths:
    - "/alerts is rewritten as an auth-guarded async Server Component (createClient → getUser → if(!user) return null → getAccountId) that reads real data: getPriceAlerts (05-07) and getTelegramLink (05-06) — no mock, no fabricated alerts, real empty states (governing rule: mock module deleted)."
    - "The mock-era AlertItem type (sentiment_change/news_spike, threshold:string, Email/Push/In-App delivery, no instrumentId) is replaced by a real price-alert shape (direction above/below, numeric threshold, instrument identity, cooldown, current price, Telegram delivery); AlertsTable is rewritten to render it (ALRT-02)."
    - "A user can create a price alert from the page via AlertFormDialog — searching the real instrument master (searchInstrumentsAction), choosing direction/threshold/cooldown — and edit/toggle/delete existing alerts, each calling the 05-07 Server Actions with inline errors surfaced (never swallowed) (ALRT-02)."
    - "A TelegramLinkCard shows the link status and drives the handshake: Link → generateTelegramLink renders the t.me deep link → 'I've sent /start' → checkTelegramLink polls+binds → linked state with an Unlink button (ALRT-01)."
    - "Each alert row shows its current cached price vs threshold with an honest em-dash when unpriced, and any rendered timestamp uses pinned en-IN / Asia/Kolkata formatting to avoid the hydration bug StalenessBadge fixed."
  artifacts:
    - path: "src/app/(dashboard)/alerts/page.tsx"
      provides: "auth-guarded Server Component reading getPriceAlerts + getTelegramLink, rendering TelegramLinkCard + AlertsTable + AlertFormDialog"
      contains: "auth.getUser"
    - path: "src/components/dashboard/AlertsTable.tsx"
      provides: "rewritten table for price alerts (direction/threshold/current price/status, edit/toggle/delete)"
      contains: "PriceAlertView"
    - path: "src/components/dashboard/AlertFormDialog.tsx"
      provides: "create/edit alert dialog with real-instrument search"
      contains: "searchInstrumentsAction"
    - path: "src/components/dashboard/TelegramLinkCard.tsx"
      provides: "Telegram link/unlink handshake UI"
      contains: "generateTelegramLink"
    - path: "src/lib/types.ts"
      provides: "AlertItem replaced/evolved to the real price-alert display shape"
      contains: "direction"
  key_links:
    - from: "src/app/(dashboard)/alerts/page.tsx"
      to: "getPriceAlerts (05-07) + getTelegramLink (05-06)"
      via: "server-side reads in the RSC, same pattern as holdings/page.tsx"
      pattern: "getPriceAlerts"
    - from: "src/components/dashboard/AlertFormDialog.tsx"
      to: "createPriceAlert / updatePriceAlert + searchInstrumentsAction (05-07)"
      via: "useTransition + inline error, calls the Server Actions directly"
      pattern: "createPriceAlert"
    - from: "src/components/dashboard/TelegramLinkCard.tsx"
      to: "generateTelegramLink / checkTelegramLink / unlinkTelegram (05-06)"
      via: "useTransition state machine (unlinked → link shown → linked)"
      pattern: "checkTelegramLink"
---

<objective>
Rewrite the mock-era `/alerts` surface into the real Phase-5 feature: replace the `AlertItem` type with a real price-alert display shape, rewrite `AlertsTable` to render it (direction/threshold/current-price/status + edit/toggle/delete), add an `AlertFormDialog` (create/edit with real-instrument search) and a `TelegramLinkCard` (the linking handshake UI), and rewrite `src/app/(dashboard)/alerts/page.tsx` as an auth-guarded async Server Component reading `getPriceAlerts` (05-07) and `getTelegramLink` (05-06). The dead "Delivery Settings" button becomes the Telegram linking anchor; the Phase-6 marketing cards (Sentiment/Volume) are removed or clearly labeled "coming later" (no fabricated data).

Purpose: this is where ALRT-01 + ALRT-02 become user-visible. Every collision from 05-RESEARCH-codebase (route/component/type/nav all exist as mock scaffolding) is handled by REWRITING in place, honoring "a feature is not done until its mock module is deleted."
Output: edited `src/lib/types.ts`, `AlertsTable.tsx`, `alerts/page.tsx`; new `AlertFormDialog.tsx`, `TelegramLinkCard.tsx`. Verified by tsc + build; full click-through E2E is 05-09.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-alerts-telegram/05-RESEARCH-codebase.md
@.planning/phases/05-alerts-telegram/05-06-PLAN.md
@.planning/phases/05-alerts-telegram/05-07-PLAN.md
@src/app/(dashboard)/holdings/page.tsx
@src/app/(dashboard)/alerts/page.tsx
@src/components/dashboard/AlertsTable.tsx
@src/components/dashboard/HoldingFormDialog.tsx
@src/components/dashboard/RefreshPricesButton.tsx
@src/components/dashboard/StalenessBadge.tsx
@src/lib/types.ts
@src/lib/alerts/read.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace AlertItem type + rewrite AlertsTable</name>
  <files>src/lib/types.ts, src/components/dashboard/AlertsTable.tsx</files>
  <action>
1. `src/lib/types.ts`: REPLACE the mock-era `AlertItem` (lines ~96-103: `sentiment_change`/`news_spike`, `threshold: string`, `delivery: 'Email'|'Push'|'In-App'`, no instrumentId) with the real display shape. Simplest and cleanest: re-export or align to `PriceAlertView` from `@/lib/alerts/read` (05-07) so there is ONE alert vocabulary. Either `export type { PriceAlertView } from '@/lib/alerts/read';` here, or delete `AlertItem` outright and have the table import `PriceAlertView` directly. Do NOT leave the Phase-6 `sentiment_change`/`news_spike` branches anywhere in Phase-5 code (those are ALRT-04/NEWS, Phase 6). Confirm the only consumer is AlertsTable (05-RESEARCH-codebase: `grep` shows AlertItem consumed only there).
2. `src/components/dashboard/AlertsTable.tsx`: rewrite (`'use client'`) to consume `PriceAlertView[]`. Columns: Symbol (ticker + name), Trigger (direction: TrendingUp for 'above' / TrendingDown for 'below' + the numeric threshold), Current (currentPrice formatted, or an em-dash `—` with a title when null — the honest-pending pattern from holdings/page.tsx:136), Status (Active/Paused from isActive), Delivery (a single "Telegram" indicator — the ONLY delivery channel this phase supports; remove the Push/Email/In-App icons), and an Actions cell (edit → opens AlertFormDialog in edit mode; toggle active → togglePriceAlert; delete → deletePriceAlert), each a client interaction with `useTransition` + inline error (RefreshPricesButton idiom, never swallow). Keep the existing empty-state ("No active alerts") and the glass-card styling. If any timestamp (e.g. last_triggered_at) is shown, format with pinned `en-IN` / `Asia/Kolkata` (StalenessBadge lesson — a real hydration bug). The "Create Alert" button triggers AlertFormDialog in add mode (wire in Task 2/3).
  </action>
  <verify>
1. `grep -c "sentiment_change\|news_spike" src/lib/types.ts src/components/dashboard/AlertsTable.tsx` → 0 (Phase-6 branches gone).
2. `grep "PriceAlertView" src/components/dashboard/AlertsTable.tsx` → matches (real shape consumed).
3. `grep -c "'Email'\|'Push'\|'In-App'" src/components/dashboard/AlertsTable.tsx` → 0 (Telegram-only delivery).
4. `grep "togglePriceAlert\|deletePriceAlert" src/components/dashboard/AlertsTable.tsx` → matches (row actions wired).
5. `npx tsc --noEmit` passes.
  </verify>
  <done>AlertItem is replaced by the real price-alert display shape and AlertsTable renders it (direction/threshold/current-price/status, Telegram-only delivery, edit/toggle/delete row actions) with honest em-dash pricing and no Phase-6 branches.</done>
</task>

<task type="auto">
  <name>Task 2: AlertFormDialog (create/edit with real-instrument search)</name>
  <files>src/components/dashboard/AlertFormDialog.tsx</files>
  <action>
Create `src/components/dashboard/AlertFormDialog.tsx` (`'use client'`) modeled on `HoldingFormDialog.tsx` (same Radix dialog + debounced instrument search pattern). Props: `mode: 'add' | 'edit'`, `trigger: ReactNode`, and for edit an existing `PriceAlertView`. Fields:
- Instrument: in add mode, a debounced search input calling `searchInstrumentsAction` (05-07 re-export / portfolio.ts) — the SAME real-master search as HoldingFormDialog (PORT-06: never a free-text ticker); in edit mode, the instrument is fixed (show it read-only).
- Direction: a select/toggle `above` / `below`.
- Threshold: a numeric input (> 0).
- Cooldown minutes: optional numeric input (default 1440, min 60) — a small "how often it can re-notify" helper text.
On submit inside `useTransition`: add → `createPriceAlert({ instrumentId, direction, threshold, cooldownMinutes })`; edit → `updatePriceAlert({ id, direction, threshold, cooldownMinutes })`. Surface `{ success:false, error }` inline (e.g. the friendly duplicate-direction message) — never swallow; on success close the dialog (the Server Action already revalidates /alerts). Follow HoldingFormDialog's isPending/error idioms exactly.
  </action>
  <verify>
1. `grep "searchInstrumentsAction" src/components/dashboard/AlertFormDialog.tsx` → matches (real-master search).
2. `grep "createPriceAlert\|updatePriceAlert" src/components/dashboard/AlertFormDialog.tsx` → matches.
3. Eyeball: threshold validated > 0 client-side; `{ success:false, error }` rendered inline; `useTransition` used; edit mode fixes the instrument.
4. `npx tsc --noEmit` passes.
  </verify>
  <done>AlertFormDialog creates/edits alerts against real instruments (debounced master search, direction/threshold/cooldown), calling the 05-07 Server Actions with inline error surfacing — reusing the HoldingFormDialog interaction pattern.</done>
</task>

<task type="auto">
  <name>Task 3: TelegramLinkCard + rewrite the /alerts page (auth-guarded Server Component)</name>
  <files>src/components/dashboard/TelegramLinkCard.tsx, src/app/(dashboard)/alerts/page.tsx</files>
  <action>
1. `src/components/dashboard/TelegramLinkCard.tsx` (`'use client'`): props `{ status: 'pending'|'linked'|'revoked'|'none'; linkedAt: string | null }`. A `useTransition` state machine:
   - not linked → a "Link Telegram" button → `generateTelegramLink()` → on `{ok:true,url}` render the `t.me` link (open in new tab) + instructions ("Open the link, tap START, then click the button below") + an "I've sent /start" button → `checkTelegramLink()` → on `{ok:true, linked:true}` flip to the linked view; if still not linked, show an honest "no /start received yet — try again" (and surface any 409/webhook error the action returns).
   - linked → a linked badge (+ linkedAt formatted `en-IN`/`Asia/Kolkata`) and an "Unlink" button → `unlinkTelegram()`.
   Inline errors, never swallowed. This card is the anchor that REPLACES the dead "Delivery Settings" button.
2. `src/app/(dashboard)/alerts/page.tsx`: REWRITE from the static mock page into an async auth-guarded Server Component, mirroring holdings/page.tsx: `const supabase = await createClient(); const { data:{ user } } = await supabase.auth.getUser(); if (!user) return null; const accountId = await getAccountId(supabase, user.id);` then `const alerts = await getPriceAlerts(supabase, accountId);` and `const link = await getTelegramLink(supabase, user.id);`. Render: the header (keep the Bell title; the old "Delivery Settings" button is removed — its role moves into TelegramLinkCard), `<TelegramLinkCard status={link.status} linkedAt={link.linkedAt} />`, an `<AlertFormDialog mode="add" trigger={<button>Create Alert</button>} />` (or route the table's Create button), and `<AlertsTable alerts={alerts} />`. REMOVE the three Phase-6 marketing cards (Price/Sentiment/Volume) OR keep only a single honest "Price alerts" note — do NOT render Sentiment/Volume as if they exist (Phase 6). No fabricated data anywhere.
  </action>
  <verify>
1. `grep "auth.getUser" src/app/(dashboard)/alerts/page.tsx` → matches; `grep "if (!user) return null" src/app/(dashboard)/alerts/page.tsx` → matches (auth guard, was previously a static sync component).
2. `grep "getPriceAlerts" src/app/(dashboard)/alerts/page.tsx` → matches; `grep "getTelegramLink" src/app/(dashboard)/alerts/page.tsx` → matches (real reads).
3. `grep "generateTelegramLink\|checkTelegramLink\|unlinkTelegram" src/components/dashboard/TelegramLinkCard.tsx` → all present.
4. `grep -c "Sentiment Shifts\|Volume Spikes" src/app/(dashboard)/alerts/page.tsx` → 0 (Phase-6 marketing removed).
5. `npx tsc --noEmit` passes; `npm run build` compiles and lists `/alerts`.
  </verify>
  <done>/alerts is an auth-guarded Server Component reading real alerts + link status, rendering the TelegramLinkCard handshake UI, the create/edit dialog, and the rewritten AlertsTable — with the mock marketing cards and dead delivery button gone.</done>
</task>

</tasks>

<verification>
**Static (run now):** grep checks above; `npx tsc --noEmit` clean; `npm run build` succeeds and lists `/alerts`; review confirms no mock/fabricated alert data, Telegram-only delivery, real-master instrument search, auth-guarded RSC, and pinned timestamp formatting.

**DEFERRED (functional/E2E — needs the running app + live DB with the 05-01 migration applied + a real bot token):** the actual click-through (link Telegram via the real handshake, create an alert, toggle/edit/delete, see current-price vs threshold render). Exercised in 05-09.
</verification>

<success_criteria>
The mock `/alerts` surface is fully rewritten: `AlertItem` replaced by the real price-alert shape, `AlertsTable` rendering it (Telegram-only delivery, row edit/toggle/delete), `AlertFormDialog` creating/editing against the real instrument master, `TelegramLinkCard` driving the link/unlink handshake, and the page an auth-guarded Server Component reading getPriceAlerts + getTelegramLink with real empty states and no Phase-6 marketing. tsc + build clean; E2E deferred to 05-09.
</success_criteria>

<output>
After completion, create `.planning/phases/05-alerts-telegram/05-08-SUMMARY.md`
</output>
