---
phase: 05-alerts-telegram
plan: 07
type: execute
wave: 2
depends_on: ["05-01", "05-03"]
files_modified:
  - src/server-actions/alerts.ts
  - src/lib/alerts/read.ts
autonomous: true
requirements: [ALRT-02]

must_haves:
  truths:
    - "A signed-in user can create a per-ticker price alert (instrument chosen from the real ISIN+exchange master via searchInstruments, direction above/below, threshold, optional cooldown) through a cookie-bound Server Action that resolves accountId and lets RLS authorize the write — never the admin client (ALRT-02)."
    - "A user can update (threshold/cooldown/direction), toggle active, and delete their own alerts; each mutation revalidates /alerts before returning, and a duplicate (account,instrument,direction) is caught as a friendly error (Postgres 23505), not a raw crash (ALRT-02)."
    - "getPriceAlerts reads the account's alerts joined to instrument display data AND the current cached price (price_cache), returning UI-ready rows with an honest null price when never fetched — the same read-only cookie-bound, never-fabricate discipline as get-portfolio-pnl.ts."
    - "The instrument for an alert is ALWAYS resolved against the symbol master (searchInstruments), never accepted as a free-text ticker (PORT-06 rule extended to alerts)."
  artifacts:
    - path: "src/server-actions/alerts.ts"
      provides: "createPriceAlert / updatePriceAlert / togglePriceAlert / deletePriceAlert Server Actions (+ re-exported searchInstrumentsAction for ticker selection)"
      exports: ["createPriceAlert", "updatePriceAlert", "togglePriceAlert", "deletePriceAlert"]
    - path: "src/lib/alerts/read.ts"
      provides: "getPriceAlerts(supabase, accountId) → PriceAlertView[] (alerts + instrument display + cached price)"
      exports: ["getPriceAlerts"]
  key_links:
    - from: "src/server-actions/alerts.ts"
      to: "requireAuthedContext (getUser + getAccountId, cookie-bound client) + public.price_alerts"
      via: "copied from portfolio.ts — never the admin client; RLS authorizes"
      pattern: "auth.getUser"
    - from: "src/server-actions/alerts.ts"
      to: "revalidatePath('/alerts')"
      via: "every mutation revalidates the alerts page before returning success"
      pattern: "revalidatePath\\('/alerts'\\)"
    - from: "src/lib/alerts/read.ts"
      to: "public.price_alerts + public.instruments + public.price_cache"
      via: "cookie-bound RLS-scoped reads joined by instrument_id"
      pattern: "from\\('price_alerts'\\)"
---

<objective>
Write the user-facing price-alert data layer: `src/server-actions/alerts.ts` (create/update/toggle/delete Server Actions over `price_alerts`, cookie-bound and RLS-authorized, copying Phase 2's `requireAuthedContext` pattern) and `src/lib/alerts/read.ts` (`getPriceAlerts` — the read that joins alerts to instrument display data and their current cached price for the RSC). Ticker selection reuses the existing `searchInstrumentsAction` (PORT-06's "search the real master, never free-text" rule), so alerts can only target real instruments.

Purpose: this is the ALRT-02 CRUD surface the UI (05-08) drives. It is deliberately independent of the Telegram engine — it only needs the schema (05-01) and the alert domain types (05-03) — so it runs in wave 2 parallel to the delivery subsystem.
Output: `src/server-actions/alerts.ts` and `src/lib/alerts/read.ts`. Static-verified (tsc + review + greps); live CRUD against the DB is exercised in 05-09.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-alerts-telegram/05-RESEARCH-codebase.md
@.planning/phases/05-alerts-telegram/05-01-PLAN.md
@src/server-actions/portfolio.ts
@src/lib/supabase/portfolio.ts
@src/lib/prices/get-portfolio-pnl.ts
@src/lib/alerts/types.ts
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Price-alert CRUD Server Actions</name>
  <files>src/server-actions/alerts.ts</files>
  <action>
Create `src/server-actions/alerts.ts` with `'use server';` and a file-level comment naming ALRT-02. Copy the `requireAuthedContext` helper VERBATIM from `src/server-actions/portfolio.ts` (cookie-bound `createClient()` + `auth.getUser()` throw-if-null + `getAccountId`) — NEVER import `@/utils/supabase/admin` (portfolio.ts:6-7 rule; alert rows are user-owned so RLS does the authorization). Use the `ActionResult = { success: true } | { success: false; error: string }` shape. Implement:

- `createPriceAlert(input: { instrumentId: string; direction: 'above'|'below'; threshold: number; cooldownMinutes?: number }): Promise<ActionResult>` — validate `threshold > 0` (throw on programmatic-invalid, matching assertValidBuySell style) and `cooldownMinutes` (if provided) `>= 60`; `insert` into `price_alerts` `{ account_id: accountId, instrument_id, direction, threshold, cooldown_minutes: input.cooldownMinutes ?? 1440, is_active: true }`. Catch Postgres `23505` (the UNIQUE(account_id,instrument_id,direction)) → `{ success:false, error:'An alert for this ticker and direction already exists' }` (the portfolio.ts:288 friendly-mapping precedent).
- `updatePriceAlert(input: { id: string; threshold: number; cooldownMinutes: number; direction: 'above'|'below' }): Promise<ActionResult>` — `update` the row by id (RLS scopes to the owner; a foreign id simply updates zero rows). Re-validate threshold/cooldown. Also set `updated_at: new Date().toISOString()`. Map 23505 the same way (changing direction could collide).
- `togglePriceAlert(input: { id: string; isActive: boolean }): Promise<ActionResult>` — `update({ is_active: input.isActive, updated_at })` by id.
- `deletePriceAlert(input: { id: string }): Promise<ActionResult>` — `delete().eq('id', input.id)` (RLS scopes to owner).
- Re-export `searchInstrumentsAction` from `@/server-actions/portfolio` (or instruct the UI to import it directly) so the alert form uses the SAME real-master search as the holdings dialog (PORT-06). Prefer `export { searchInstrumentsAction } from './portfolio';` to keep one implementation.

Every mutation calls `revalidatePath('/alerts')` before returning `{ success: true }` (and `revalidatePath('/')` only if a nav count ever renders alert data — not needed yet, so /alerts alone). Do NOT write `last_triggered_at` here — that column is the evaluator's (05-05).
  </action>
  <verify>
1. `grep -c "from '@/utils/supabase/admin'" src/server-actions/alerts.ts` → 0 (never the admin client).
2. `grep "auth.getUser" src/server-actions/alerts.ts` → matches (auth gate present).
3. `grep -c "revalidatePath('/alerts')" src/server-actions/alerts.ts` → ≥ 4 (every mutation revalidates).
4. `grep "23505" src/server-actions/alerts.ts` → matches (friendly duplicate-direction mapping).
5. `grep "searchInstrumentsAction" src/server-actions/alerts.ts` → matches (real-master ticker selection reused).
6. `npx tsc --noEmit` passes.
  </verify>
  <done>src/server-actions/alerts.ts exposes create/update/toggle/delete over price_alerts via the cookie-bound RLS-authorized client (never admin), maps the duplicate-direction constraint to a friendly error, reuses the real-instrument search, and revalidates /alerts on every mutation.</done>
</task>

<task type="auto">
  <name>Task 2: getPriceAlerts read (alerts + instrument display + cached price)</name>
  <files>src/lib/alerts/read.ts</files>
  <action>
Create `src/lib/alerts/read.ts` following the read-only, cookie-bound, never-fabricate style of `src/lib/prices/get-portfolio-pnl.ts` (accepts an already-constructed cookie-bound `SupabaseClient` — RLS-scoped; NEVER the admin client; see that file's header). Define a UI-facing view type and the reader:

- `export interface PriceAlertView { id: string; instrumentId: string; ticker: string; name: string; exchange: Exchange; currency: Currency; direction: 'above'|'below'; threshold: number; isActive: boolean; cooldownMinutes: number; lastTriggeredAt: string | null; currentPrice: number | null; }`
- `export async function getPriceAlerts(supabase: SupabaseClient, accountId: string): Promise<PriceAlertView[]>`:
  1. Select the account's alerts joined to instrument display columns in ONE query (no N+1 — the getHoldings precedent): `supabase.from('price_alerts').select('id, instrument_id, direction, threshold, is_active, cooldown_minutes, last_triggered_at, instruments ( symbol, display_name, exchange, currency )').eq('account_id', accountId).order('created_at')`. Normalize the nested to-one instrument (it may surface as object or one-element array — reuse the firstInstrument normalization idea from lib/supabase/portfolio.ts).
  2. Load current prices for those instruments from `price_cache` (`select instrument_id, price ... .in('instrument_id', ids)`), keyed by instrument_id. Map each alert's `currentPrice` to the cached `price ?? null` (null when never fetched — an em-dash in the UI, never a fabricated number).
  3. Map snake_case → the camelCase `PriceAlertView` and return.

Import `Exchange`/`Currency` from `@/lib/types`. Keep this a plain lib function (not a Server Action) — the RSC in 05-08 already has `supabase` + `accountId`, exactly like holdings/page.tsx calls getPortfolioPnL.
  </action>
  <verify>
1. `grep "from('price_alerts')" src/lib/alerts/read.ts` → matches; `grep "from('price_cache')" src/lib/alerts/read.ts` → matches (current price joined in).
2. `grep -c "from '@/utils/supabase/admin'" src/lib/alerts/read.ts` → 0 (cookie-bound read only).
3. Eyeball: `currentPrice` is `price ?? null` (never 0/fabricated); instrument join normalized for the object-or-array shape.
4. `npx tsc --noEmit` passes.
  </verify>
  <done>getPriceAlerts returns UI-ready alert rows (instrument display + honest cached current price, null when unpriced) via a single cookie-bound RLS-scoped read, matching the get-portfolio-pnl.ts discipline.</done>
</task>

</tasks>

<verification>
**Static (run now):** grep checks above; `npx tsc --noEmit` clean; review confirms neither file imports the admin client, mutations revalidate /alerts, the duplicate-direction constraint maps to a friendly error, ticker selection reuses searchInstruments, and the read never fabricates a price.

**DEFERRED (needs live Supabase + the 05-01 migration applied — consent-gated):** actually creating/editing/deleting an alert against the DB and confirming RLS admits the owner and rejects a second user (covered by the 05-01 RLS test). Exercised in 05-09.
</verification>

<success_criteria>
`src/server-actions/alerts.ts` exposes cookie-bound, admin-client-free create/update/toggle/delete over price_alerts (friendly duplicate mapping, real-master ticker search, /alerts revalidation) and `src/lib/alerts/read.ts` returns UI-ready alert rows with honest cached prices. tsc clean; live CRUD deferred to 05-09.
</success_criteria>

<output>
After completion, create `.planning/phases/05-alerts-telegram/05-07-SUMMARY.md`
</output>
