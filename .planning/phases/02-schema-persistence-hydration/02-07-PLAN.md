---
phase: 02-schema-persistence-hydration
plan: 07
type: execute
wave: 4
depends_on: ["02-05"]
files_modified:
  - src/components/dashboard/HoldingsTable.tsx
  - src/components/dashboard/WatchlistTable.tsx
  - src/app/(dashboard)/research/page.tsx
autonomous: true
requirements: [WIRE-01]

must_haves:
  truths:
    - "Clicking a 'Research' affordance on a held or watched ticker opens the Research module pre-loaded for that exact ticker, not always defaulting to HDFCBANK."
    - "Navigating to /research directly (no query param) still shows the existing default demo behavior — this is additive, not a regression."
  artifacts:
    - path: "src/app/(dashboard)/research/page.tsx"
      provides: "reads initial ticker from the ?ticker= URL search param via useSearchParams"
  key_links:
    - from: "src/components/dashboard/HoldingsTable.tsx"
      to: "/research?ticker={symbol}"
      via: "next/link Link"
      pattern: "href=\\{`/research\\?ticker="
    - from: "src/app/(dashboard)/research/page.tsx"
      to: "next/navigation:useSearchParams"
      via: "reading the ticker query param on mount"
      pattern: "useSearchParams\\(\\)"
---

<objective>
Deep-link the Research module from real holdings and watchlist rows (WIRE-01): clicking a ticker's "Research" affordance in `HoldingsTable`/`WatchlistTable` opens `/research?ticker=SYMBOL` pre-loaded for that instrument, instead of the user having to re-search manually.

Purpose: WIRE-01 requires the research module to be reachable from real persisted holdings/watchlist data — this plan does not touch the research module's own data source (it stays on its existing mock/Gemini-backed report data; only the ENTRY POINT changes to read from real portfolio rows).
Output: a "Research" link per row in both tables, and `research/page.tsx` initializing its selected ticker from the URL instead of a hardcoded default.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-schema-persistence-hydration/02-05-SUMMARY.md
@src/components/dashboard/HoldingsTable.tsx
@src/components/dashboard/WatchlistTable.tsx
@src/app/(dashboard)/research/page.tsx
@src/components/research/StockSearchBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Research deep-link to HoldingsTable and WatchlistTable rows</name>
  <files>src/components/dashboard/HoldingsTable.tsx, src/components/dashboard/WatchlistTable.tsx</files>
  <action>
Add a small "Research" affordance (icon button or text link, matching each table's existing visual density — e.g. next to the exchange badge in `HoldingsTable`, next to the ticker in `WatchlistTable`) using `next/link`'s `<Link href={`/research?ticker=${h.ticker}`}>`. Use the plain `ticker` symbol (not `instrumentId`) since the research module's existing search/report lookup (`src/lib/research/research-service.ts`) is ticker-keyed, not instrument-id-keyed — do not change that module's lookup key in this plan. Keep both table components in their current 'use client' presentational role; this is an additive UI element, not a data-shape change.
  </action>
  <verify>
`npx tsc --noEmit` passes. `grep "research?ticker=" src/components/dashboard/HoldingsTable.tsx src/components/dashboard/WatchlistTable.tsx` matches in both files.
  </verify>
  <done>Every holding/watchlist row has a working link to /research?ticker={its ticker}.</done>
</task>

<task type="auto">
  <name>Task 2: research/page.tsx reads the initial ticker from the URL</name>
  <files>src/app/(dashboard)/research/page.tsx</files>
  <action>
Import `useSearchParams` from `next/navigation` (client-side hook — this page is already `'use client'`). On mount, initialize `selectedTicker` from `searchParams.get('ticker')` if present (uppercase it, matching the existing `HDFCBANK` default convention), falling back to the existing default-to-first-available-stock behavior when the param is absent — this must be additive, not a regression of the current direct-navigation experience.

Per Next.js docs (`node_modules/next/dist/docs/01-app/01-getting-started/08-caching.md` and the forms guide already reviewed for plan 02-05), reading `useSearchParams()` in a Client Component during static generation can trigger a "missing Suspense boundary" build warning/error depending on the route's rendering mode; this project does NOT have `cacheComponents` enabled in `next.config.ts` (confirmed during Phase 2 research), so this is unlikely to bite, but as a defensive measure wrap the page's return in a `<Suspense>` boundary if `next build` reports the missing-boundary error — check by running the build, don't add Suspense speculatively if it isn't needed (avoids an unnecessary loading flash for a page that's already client-rendered top to bottom).
  </action>
  <verify>
`npx tsc --noEmit` passes. `grep "useSearchParams" src/app/\(dashboard\)/research/page.tsx` matches. `npm run build` completes without a "missing Suspense boundary" error for this route (if it does error, confirm the Suspense wrapper was added and the build passes on retry).
  </verify>
  <done>research/page.tsx opens pre-loaded with the ticker from ?ticker=, and direct navigation to /research with no param still shows the existing default behavior.</done>
</task>

</tasks>

<verification>
**Static (run now):** `npx tsc --noEmit`; `npm run build` (this project has no live Supabase, but `next build` itself doesn't require one — confirm it completes; if it fails for unrelated reasons already present before this plan, note that rather than treating it as this plan's regression); grep checks above.

**DEFERRED:** clicking through from a REAL holding row to the research page requires a live database with at least one holding to click on (plan 02-06's checkpoint). This plan's own scope (the link + the URL-param read) is fully verifiable statically without a DB.
</verification>

<success_criteria>
HoldingsTable and WatchlistTable rows link to `/research?ticker=X`; research/page.tsx honors that param on load while preserving its existing no-param default behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-schema-persistence-hydration/02-07-SUMMARY.md`
</output>
