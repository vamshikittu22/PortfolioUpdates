---
phase: 02-schema-persistence-hydration
plan: 06
type: execute
wave: 4
depends_on: ["02-05"]
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/news/page.tsx
  - src/app/(dashboard)/alerts/page.tsx
  - src/store/usePortfolioStore.ts
  - src/lib/mock-portfolio.ts
autonomous: false
requirements: [PORT-07]

must_haves:
  truths:
    - "No file in the codebase imports usePortfolioStore or mock-portfolio — both are deleted, not just unused."
    - "The sidebar/header shows the real signed-in user's single account, not a 3-account mock switcher."
    - "News and Alerts pages compile and render honest empty states for the features that don't exist yet (Phase 5/6), while News still shows the REAL persisted watchlist (in scope for Phase 2)."
    - "A human, once a live Supabase exists, can confirm: add a holding, refresh the browser, it's still there; partial-sell it, average cost is unchanged; split it, average cost drops and no false loss appears."
  artifacts:
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "single real-account sidebar/header, no mock account switcher"
  key_links:
    - from: "src/app/(dashboard)/news/page.tsx"
      to: "src/lib/supabase/portfolio.ts:getWatchlist"
      via: "real watchlist data feeding WatchlistTable"
      pattern: "getWatchlist\\("
---

<objective>
Finish PORT-07 by removing the last consumers of the mock portfolio store (`layout.tsx`'s account switcher, `news/page.tsx`, `alerts/page.tsx`), delete `src/store/usePortfolioStore.ts` and `src/lib/mock-portfolio.ts` outright, and verify — with a human at a live Supabase instance, since this environment has none — that the whole persistence story actually holds up: refresh survival, partial-sell correctness, and split correctness with no false loss.

Purpose: this is the phase's closing plan. Every other Phase 2 plan produced code that is DEFERRED-verified (no Docker/live Supabase in this environment per .planning/STATE.md); this plan's checkpoint is where that debt is either cleared (if the user now has a live DB) or explicitly re-recorded as deferred, matching the honesty standard Phase 1 already established.
Output: mock-free `layout.tsx`/`news/page.tsx`/`alerts/page.tsx`, `usePortfolioStore.ts` and `mock-portfolio.ts` deleted, and a completed (or explicitly deferred) human verification checkpoint.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-schema-persistence-hydration/02-05-SUMMARY.md
@src/app/(dashboard)/layout.tsx
@src/app/(dashboard)/news/page.tsx
@src/app/(dashboard)/alerts/page.tsx
@src/components/dashboard/NewsFeed.tsx
@src/components/dashboard/AlertsTable.tsx
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify layout.tsx to a single real account</name>
  <files>src/app/(dashboard)/layout.tsx</files>
  <action>
Remove the `usePortfolioStore` import, the `accounts`/`selectedAccountId`/`switchAccount` usage, and the entire account-switcher dropdown UI (the `accountDropdownOpen` state, the button listing `Object.values(accounts).map(...)`) — per the roadmap's Phase 2 scope decision (02-RESEARCH.md Open Question 1), this phase assumes a single account per user, auto-created by the Phase 1 `handle_new_user` trigger. Replace the account name display (currently `selectedAccount.profile.name`) with either a static label ("My Portfolio") or a lightweight client-side fetch of the account's `name` column via the existing browser Supabase client pattern already in this file (`supabase.from('investment_accounts').select('name').single()`, consistent with the existing `auth.getUser()` effect) — pick whichever keeps the diff smaller and document the choice. Keep the existing `userEmail` / logout logic entirely as-is (it does not touch the mock store).
  </action>
  <verify>
`grep "usePortfolioStore" src/app/\(dashboard\)/layout.tsx` returns NO matches. `npx tsc --noEmit` passes.
  </verify>
  <done>Sidebar/header show one real account, no switcher UI, no mock store import.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate News + Alerts pages off the mock store</name>
  <files>src/app/(dashboard)/news/page.tsx, src/app/(dashboard)/alerts/page.tsx</files>
  <action>
`news/page.tsx`: currently reads `{ news, watchlist, newsPrefs, profile }` from `usePortfolioStore`. Convert to a Server Component (drop `'use client'`) that fetches the real watchlist via `getWatchlist` from `@/lib/supabase/portfolio` (same pattern as plan 02-05's dashboard/holdings pages) and passes it to `WatchlistTable`. Pass `news={[]}` to `NewsFeed` (Phase 6 not live yet — it already renders a correct empty state, confirmed in plan 02-05). The "Tracking Panel" / `newsPrefs.trackedSymbols` UI is Phase 6 scope with no backing table yet — either remove that panel entirely for now (preferred, avoids UI that does nothing) or keep it as a disabled/"coming soon" affordance; do not wire it to fake local state that looks persisted but isn't. Extract the interactive open/close tracking-panel bits into a small `'use client'` sub-component if you keep any of that UI, since the page itself is now a Server Component.

`alerts/page.tsx`: currently reads `{ alerts, profile }` from `usePortfolioStore`. Convert to pass `alerts={[]}` to `AlertsTable` (Phase 5 not live yet — it already renders a correct "No active alerts" empty state) and drop the `profile.name` reference (replace with a static heading, no per-account name needed here). This can stay a simple Server Component or plain component — no client interactivity is lost by removing the mock data.
  </action>
  <verify>
`grep "usePortfolioStore" src/app/\(dashboard\)/news/page.tsx src/app/\(dashboard\)/alerts/page.tsx` returns NO matches. `npx tsc --noEmit` passes. Both pages still export a valid default component.
  </verify>
  <done>News page shows the real watchlist and an honest empty news feed; Alerts page shows an honest empty alerts state; neither imports the mock store.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the mock store and verify zero remaining references</name>
  <files>src/store/usePortfolioStore.ts, src/lib/mock-portfolio.ts</files>
  <action>
Delete both files. Run a full-project grep to confirm nothing still imports them (this is the actual completion proof for PORT-07's "mock portfolio store is deleted" clause, not just this plan's own edited files).
  </action>
  <verify>
`grep -rl "usePortfolioStore\|mock-portfolio" src` (from the project root) returns NO files. `npx tsc --noEmit` passes for the whole project (proves nothing else was silently broken).
  </verify>
  <done>src/store/usePortfolioStore.ts and src/lib/mock-portfolio.ts no longer exist on disk, and no file anywhere in src/ references them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live-DB verification of the full persistence + hydration slice</name>
  <files>none (verification-only task, no files modified)</files>
  <what-built>
The complete Phase 2 persistence + hydration slice: instruments/transactions schema (02-01), tested holdings-derivation math (02-02), Supabase-backed YouTube channels (02-03), the data/mutation layer (02-04), and real UI for Dashboard/Holdings/Watchlist (02-05), with the mock store fully removed (this plan).

This environment has no Docker and no live Supabase (see .planning/STATE.md) — everything above was written and statically verified (`npx tsc --noEmit`, grep checks) but NEVER executed against a real database. This checkpoint is where that gap gets closed, once you have a live Supabase (local `npx supabase start` with Docker, or a hosted project).
  </what-built>
  <action>
See how-to-verify below. This is a verification-only checkpoint; no code is written by this task.
  </action>
  <how-to-verify>
Once a live Supabase exists and `.env.local` has real (non-placeholder) `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY`:
1. Apply all 5 migrations (2 from Phase 1 + 3 from plan 02-01): `npx supabase db push` (or `db reset` locally).
2. Sign up a fresh user (or use an existing one from Phase 1's flow), land on the Dashboard.
3. Open Holdings → Add Asset → search for an instrument (e.g. "TCS") → buy 10 @ ₹3850 → confirm it appears in the table with avg cost ₹3850.00 and no crash on the pending-price columns (should show "—", not "NaN" or a blank error).
4. Refresh the browser (hard refresh) → confirm the holding is still there (persistence-survives-refresh, PORT-01/PORT-02).
5. Sell 4 shares @ ₹4000 (partial sell) → confirm quantity drops to 6 and avg cost STAYS ₹3850.00 (PORT-04 correctness — a bug here would show avg cost changing on a partial sell).
6. Record a 2-for-1 split (+10 additional shares) → confirm quantity becomes 20 and avg cost drops to ~₹1925.00, with no "loss" indicator shown anywhere (PORT-05 — the whole point of this requirement is that a split must never look like a loss).
7. Add a symbol to the Watchlist, confirm it appears; remove it, confirm it disappears without a refresh.
8. Open a second browser/incognito session as a different user, confirm they see NONE of the above (RLS — same proof pattern as Phase 1's `test:rls`, now extended to transactions/watchlist_items).
  </how-to-verify>
  <verify>All 8 steps in how-to-verify pass against a live Supabase instance.</verify>
  <done>Persistence-survives-refresh, partial-sell correctness, and split correctness are confirmed live, or explicitly recorded as DEFERRED (with reason: no live Supabase available) in the plan SUMMARY.</done>
  <resume-signal>Type "approved" once all 8 steps pass, or describe which step failed (include the observed vs expected behavior) so it can be fixed before Phase 2 is marked complete.</resume-signal>
</task>

</tasks>

<verification>
**Static (run now):** grep checks in Tasks 1–3; full-project `npx tsc --noEmit`.

**DEFERRED / human checkpoint (Task 4):** everything requiring a live database — this is the plan's explicit, non-autonomous gate. If no live Supabase is available when this plan executes, record the checkpoint as DEFERRED in the SUMMARY (matching Phase 1's honest-deferral pattern in `.planning/phases/01-auth-rls-foundation/01-01-SUMMARY.md`) rather than fabricating a pass.
</verification>

<success_criteria>
Zero references to the mock portfolio store remain anywhere in `src/`; both mock files are deleted; layout/news/alerts compile against real (or honestly-empty) data; the 8-step human verification either passes or is explicitly recorded as deferred pending a live database.
</success_criteria>

<output>
After completion, create `.planning/phases/02-schema-persistence-hydration/02-06-SUMMARY.md`
</output>
