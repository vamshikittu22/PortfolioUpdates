---
phase: 02-schema-persistence-hydration
plan: 05
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/app/(dashboard)/page.tsx
  - src/app/(dashboard)/holdings/page.tsx
  - src/components/dashboard/HoldingsTable.tsx
  - src/components/dashboard/WatchlistTable.tsx
  - src/components/dashboard/AllocationChart.tsx
  - src/components/dashboard/HoldingFormDialog.tsx
  - src/components/dashboard/WatchlistFormDialog.tsx
autonomous: true
requirements: [PORT-01, PORT-02, PORT-03, PORT-04, PORT-05, PORT-07]

must_haves:
  truths:
    - "User can add a holding by searching the real instrument master (not typing a free-text ticker) and see it appear in the holdings table without a manual page refresh."
    - "User can edit an existing holding's quantity/price/date, sell part of a holding, or delete it entirely, and the table reflects the change immediately."
    - "User can record a split or bonus action on a holding and the displayed average cost drops proportionally instead of showing a fabricated loss."
    - "User can add/remove a watchlist entry from the same real instrument master."
    - "With zero holdings/watchlist items, the pages show an honest empty state — never mock data, never a silent blank screen."
    - "Because no live price feed exists yet (Phase 3), every price-dependent field (current price, day change, total return, portfolio value) visibly says so — e.g. an em-dash or 'Pricing arrives in Phase 3' badge — never a fabricated number."
  artifacts:
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Server Component dashboard reading real holdings/watchlist/allocation via src/lib/supabase/portfolio.ts"
    - path: "src/app/(dashboard)/holdings/page.tsx"
      provides: "Server Component holdings page with the same real data"
    - path: "src/components/dashboard/HoldingFormDialog.tsx"
      provides: "add/edit/sell/delete holding UI wired to src/server-actions/portfolio.ts"
  key_links:
    - from: "src/app/(dashboard)/page.tsx"
      to: "src/lib/supabase/portfolio.ts:getHoldings"
      via: "await inside an async Server Component"
      pattern: "getHoldings\\("
    - from: "src/components/dashboard/HoldingFormDialog.tsx"
      to: "src/server-actions/portfolio.ts:addHolding"
      via: "form action / onClick calling the Server Action"
      pattern: "addHolding\\(|sellHolding\\(|editHolding\\(|deleteHolding\\(|recordSplit\\(|recordBonus\\("
    - from: "src/components/dashboard/WatchlistFormDialog.tsx"
      to: "src/server-actions/portfolio.ts:addToWatchlist"
      via: "onClick calling the Server Action"
      pattern: "addToWatchlist\\("
---

<objective>
Hydrate the Dashboard and Holdings pages (and the Watchlist/Allocation views embedded in them) from real persisted data via the wave-2 data layer, and build the add/edit/sell/delete/split/bonus/watchlist UI that PORT-01 through PORT-05 actually require (today these buttons are inert placeholders — "Add Asset" and "Manage" have no handlers).

Purpose: this is the phase's core "the UI reads live user data instead of mock" deliverable for the two pages that own it most directly. `src/store/usePortfolioStore.ts` and `src/lib/mock-portfolio.ts` are NOT deleted in this plan — `news/page.tsx`, `alerts/page.tsx`, and `layout.tsx` still import from them and are migrated in plan 02-06, which also performs the actual deletion once every consumer is gone.
Output: Server Component pages for Dashboard + Holdings, updated presentational table/chart components with honest pending-price states, and two new dialog components wired to the wave-2 Server Actions.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-schema-persistence-hydration/02-RESEARCH.md
@.planning/phases/02-schema-persistence-hydration/02-04-SUMMARY.md
@src/lib/types.ts
@src/lib/supabase/portfolio.ts
@src/server-actions/portfolio.ts
@src/app/(dashboard)/page.tsx
@src/app/(dashboard)/holdings/page.tsx
@src/components/dashboard/HoldingsTable.tsx
@src/components/dashboard/WatchlistTable.tsx
@src/components/dashboard/AllocationChart.tsx
@src/components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Dashboard + Holdings pages to Server Components reading real data</name>
  <files>src/app/(dashboard)/page.tsx, src/app/(dashboard)/holdings/page.tsx</files>
  <action>
Drop the `'use client'` directive from both files; make the default export an `async function` Server Component. Per 02-RESEARCH.md Pattern 1: use `@/utils/supabase/server`'s `createClient()`, call `supabase.auth.getUser()`, then `getAccountId`, `getHoldings`, `getWatchlist` from `@/lib/supabase/portfolio`.

Compute KPIs HONESTLY from what's actually knowable pre-Phase-3 (no price feed exists yet):
- "Total Invested" = `sum(holding.quantity * holding.avgCost)` (cost basis, NOT market value — do not label this "Portfolio Value", that would imply live pricing).
- "Holdings" = `holdings.length`.
- "Watchlist" = `watchlist.length`.
- Replace the "Today's P&L" / "Daily Sentiment" KPI cards' trend data with a neutral "Pricing arrives in Phase 3" badge instead of any percentage — do NOT compute a fake day-change from cost basis.

Allocation data: group holdings by `exchange` (or `currency`) instead of the old mock `sector` field (sector doesn't exist on the new schema) — pass `{ name: exchange, value: costBasisForThatExchange }` to `AllocationChart`.

`page.tsx` also renders `WatchlistTable` and `NewsFeed` today — pass the real `watchlist` array to `WatchlistTable`, and pass `news={[]}` to `NewsFeed` (it already renders a correct empty state for zero items — verify this by reading `src/components/dashboard/NewsFeed.tsx`, no component change needed there). Do NOT import from `usePortfolioStore` or `mock-portfolio` in either file being edited here.

Both pages need client-side interactivity (opening the add/edit dialogs) — extract that into small `'use client'` sub-components (`HoldingFormDialog`, `WatchlistFormDialog` from Task 3) that the Server Component page renders and passes server-fetched data into as props; the dialogs themselves call the Server Actions directly (Server Actions are callable from Client Components per `node_modules/next/dist/docs/01-app/02-guides/forms.md`).
  </action>
  <verify>
`npx tsc --noEmit` passes. `grep "'use client'" src/app/\(dashboard\)/page.tsx src/app/\(dashboard\)/holdings/page.tsx` returns NO matches (both are now Server Components). `grep "usePortfolioStore\|mock-portfolio" src/app/\(dashboard\)/page.tsx src/app/\(dashboard\)/holdings/page.tsx` returns NO matches.
  </verify>
  <done>Both pages fetch real data server-side, compute only honestly-derivable KPIs, and no longer reference the mock store.</done>
</task>

<task type="auto">
  <name>Task 2: Update presentational components for real data + honest pending-price states</name>
  <files>src/components/dashboard/HoldingsTable.tsx, src/components/dashboard/WatchlistTable.tsx, src/components/dashboard/AllocationChart.tsx</files>
  <action>
Update prop types to import `Holding`/`WatchlistItem` from `@/lib/types` instead of `@/lib/mock-portfolio`.

`HoldingsTable.tsx`: replace the `Current Price` / `Total Return` columns' values — since `currentPrice`/`totalChangePercent` are optional/undefined pre-Phase-3, render an em-dash (`—`) with a muted "pending" tooltip/label instead of `formatCurrency(h.currentPrice)` (which would crash or show `NaN` today). Replace the `broker` badge column (field no longer exists) with an `exchange` badge (NSE/BSE/NASDAQ/NYSE) using the same visual treatment. Add a per-row actions affordance (icon buttons or a small menu) for Edit / Sell / Split / Bonus / Delete that open `HoldingFormDialog` in the corresponding mode — wire these to the dialog component from Task 3, do not implement the mutation logic inline here (keep this component presentational).

`WatchlistTable.tsx`: replace `sentiment`/`newsCount`/`insight` rendering with a conditional — if present (they won't be until Phase 6), render as today; if absent, render a "Sentiment available after Phase 6" placeholder instead of blank cells. Wire the existing "Manage" button (currently inert) to open `WatchlistFormDialog`. Add a small remove (X) affordance per row calling `removeFromWatchlist` (this can call the Server Action directly since the table itself can be `'use client'`, or delegate to a passed-in callback — pick whichever keeps the component simplest, document the choice).

`AllocationChart.tsx`: no shape change needed (still `{name, value}[]`) — just confirm it renders a sensible empty state when `data` is `[]` (zero holdings); add one if missing (e.g. "No holdings yet" centered message instead of an empty pie).
  </action>
  <verify>
`npx tsc --noEmit` passes. `grep "@/lib/mock-portfolio" src/components/dashboard/HoldingsTable.tsx src/components/dashboard/WatchlistTable.tsx` returns NO matches. Manually confirm no `h.currentPrice.toFixed` or similar unguarded access to an optional pricing field remains (would throw at runtime on real data with no price yet).
  </verify>
  <done>All three components compile against real types, never crash on absent pricing/sentiment fields, and show honest placeholders instead.</done>
</task>

<task type="auto">
  <name>Task 3: Add/Edit/Sell/Delete Holding dialog + Watchlist add/remove dialog</name>
  <files>src/components/dashboard/HoldingFormDialog.tsx, src/components/dashboard/WatchlistFormDialog.tsx</files>
  <action>
Both are `'use client'` components built on the existing `src/components/ui/dialog.tsx` primitive (already in the codebase — reuse it, do not hand-roll a new modal).

`HoldingFormDialog`: accepts a `mode: 'add' | 'edit' | 'sell' | 'split' | 'bonus'` and (for edit/sell/split/bonus) the target `instrumentId`. For `add`/`edit`, include an instrument search field that calls a small client-fetch to a thin wrapper around `searchInstruments` (either expose it via a tiny API route `src/app/api/instruments/search/route.ts` since `searchInstruments` currently takes a server Supabase client, OR call it through a dedicated Server Action wrapper — pick the Server Action wrapper to stay consistent with this phase's chosen pattern; add a one-line `searchInstrumentsAction` to `src/server-actions/portfolio.ts` if not already exposed there, note this as a small addition to that file beyond its original task list). For `sell`/`split`/`bonus`, skip instrument search (target is already known) and show only the relevant fields (quantity + price + date for sell; additionalQuantity + date for split/bonus). On submit, call the matching Server Action (`addHolding`/`editHolding`/`sellHolding`/`recordSplit`/`recordBonus`), show a loading state, close on success, surface the error message on failure (reuse the toast/inline-error pattern already used elsewhere in this codebase, e.g. `src/app/(dashboard)/youtube/page.tsx`'s toast approach, or a simple inline `<p className="text-danger">` if a toast system isn't trivially reusable here — do not silently swallow errors).

`WatchlistFormDialog`: same instrument-search pattern, calls `addToWatchlist` on submit.

Wire the previously-inert "Add Asset" button in `holdings/page.tsx` and the "Manage" button in `WatchlistTable.tsx` to open these dialogs (via client state in a small wrapper, since the pages themselves are now Server Components — the button that opens the dialog must live in a `'use client'` boundary, e.g. render `<HoldingFormDialog trigger={<button>Add Asset</button>} mode="add" />` as a client component embedded in the Server Component page).
  </action>
  <verify>
`npx tsc --noEmit` passes. `grep "addHolding\|editHolding\|sellHolding\|recordSplit\|recordBonus" src/components/dashboard/HoldingFormDialog.tsx` matches all five. `grep "addToWatchlist" src/components/dashboard/WatchlistFormDialog.tsx` matches. Confirm both files start with `'use client'`.
  </verify>
  <done>Add/Edit/Sell/Split/Bonus holding flows and Add-to-watchlist flow all exist as real UI wired to the Server Actions, replacing the previously-inert buttons.</done>
</task>

</tasks>

<verification>
**Static (run now):** `npx tsc --noEmit` across the whole project; grep checks above; visual code review confirming no unguarded access to Phase-3-only fields (currentPrice, dayChangePercent) and no remaining mock-store imports in the files this plan owns.

**DEFERRED (blocked on a live Supabase — no Docker in this environment):** actually adding a holding through the dialog and seeing it appear in the table without refresh; editing/selling/deleting and confirming the derived quantity/avgCost match hand-computed expectations; recording a split and confirming the UI shows a lower avg cost, not a fabricated loss; confirming a page refresh preserves everything. These are the exact scenarios plan 02-06's checkpoint exercises once a live DB exists — this plan's job is to make the code correct and ready, not to fabricate a "verified" runtime result it cannot produce in this environment.
</verification>

<success_criteria>
Dashboard and Holdings pages are Server Components reading real per-user data through the wave-2 layer; presentational components handle absent pricing/sentiment data honestly; working Add/Edit/Sell/Split/Bonus/Delete holding and Add/Remove watchlist UI exists and is wired to the Server Actions. No file in this plan's scope references `usePortfolioStore` or `mock-portfolio`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-schema-persistence-hydration/02-05-SUMMARY.md`
</output>
