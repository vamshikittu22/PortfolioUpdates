---
phase: 04-csv-import
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - src/components/import/ImportDropzone.tsx
  - src/components/import/ImportSummary.tsx
  - src/components/import/PreviewTable.tsx
  - src/components/import/SymbolMappingSection.tsx
autonomous: true
requirements: [IMPT-03, IMPT-04]

must_haves:
  truths:
    - "The preview renders compact rows (symbol, type, quantity, status badge) that expand on click to show every parsed field and any validation message — the locked compact-with-expandable-detail decision."
    - "Skip/override is bulk-only: category-level toggles (import valid, skip/import duplicates, skip unsupported) with a visible count per category — NOT per-row checkboxes (the locked bulk-only decision, IMPT-03)."
    - "Each unique unmatched broker symbol appears once in a Resolve-symbols section with an instrument search (reusing searchInstrumentsAction) and a create-new form; resolving one entry fixes all its rows and nothing is silently dropped (IMPT-04)."
    - "The result summary shows explicit imported / skipped / duplicate counts with a link to Holdings (no auto-redirect) and fires refreshPricesNow so imported holdings show live P&L when the user follows the link (success criterion 4)."
    - "The create-new-instrument form never fabricates an ISIN: it is prefilled for Groww (ISIN from the file) and requires the user to enter the ISIN for Robinhood (no ISIN in that file)."
  artifacts:
    - path: "src/components/import/ImportDropzone.tsx"
      provides: "file input + drag-drop with a client-side size pre-check"
      contains: "'use client'"
    - path: "src/components/import/PreviewTable.tsx"
      provides: "compact expandable rows + category chips + bulk toggles driven by ImportPreview"
      contains: "'use client'"
    - path: "src/components/import/SymbolMappingSection.tsx"
      provides: "one entry per unique unmatched symbol; search + create-new instrument form"
      contains: "'use client'"
    - path: "src/components/import/ImportSummary.tsx"
      provides: "result counts + Holdings link + post-commit refreshPricesNow fire"
      contains: "refreshPricesNow"
  key_links:
    - from: "src/components/import/SymbolMappingSection.tsx"
      to: "searchInstrumentsAction (existing, src/server-actions/portfolio.ts)"
      via: "instrument picker reuse — never a new free-text picker"
      pattern: "searchInstrumentsAction"
    - from: "src/components/import/ImportSummary.tsx"
      to: "refreshPricesNow (existing, src/server-actions/prices.ts)"
      via: "fire-and-forget after commit success so imported holdings get priced"
      pattern: "refreshPricesNow"
    - from: "src/components/import/PreviewTable.tsx"
      to: "ImportPreview / PreviewRow / RowStatus types (src/lib/import/types.ts)"
      via: "typed props"
      pattern: "PreviewRow"
---

<objective>
Build the four presentational client components the import page composes: the file dropzone, the preview table (compact rows with expandable detail + category chips + bulk-only toggles), the resolve-symbols section (search + create-new instrument), and the result summary. These are the leaf components — each takes typed props/callbacks and holds only local view state; the page-level state machine that wires them to previewImport/commitImport is plan 04-06. Building the leaves first lets 04-06's container import them.

Purpose: this is where the locked UX decisions become concrete pixels — single progressive page, compact+expandable rows, bulk-only category controls, a result summary that is an audit trail (not a toast), and a mapping UI that never silently drops an unmatched symbol. All follow the existing dashboard component idioms (glass-card Tailwind, lucide icons, `cn()`), reuse `searchInstrumentsAction` and `refreshPricesNow` rather than building anything new.
Output: `src/components/import/{ImportDropzone,PreviewTable,SymbolMappingSection,ImportSummary}.tsx`. Verified by tsc + build; visual/functional verification is 04-07.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-csv-import/04-CONTEXT.md
@.planning/phases/04-csv-import/04-RESEARCH.md
@.planning/phases/04-csv-import/04-02-PLAN.md
@.planning/codebase/CONVENTIONS.md
@src/components/dashboard/RefreshPricesButton.tsx
@src/components/dashboard/HoldingFormDialog.tsx
@src/components/dashboard/StalenessBadge.tsx
@src/server-actions/portfolio.ts
@src/lib/import/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImportDropzone + ImportSummary</name>
  <files>src/components/import/ImportDropzone.tsx, src/components/import/ImportSummary.tsx</files>
  <action>
Both `'use client'`, styled with existing glass-card/Tailwind idioms and lucide icons (`Upload`, `FileText`, `CheckCircle2`, `ArrowRight`).

**ImportDropzone.tsx** — props `{ onFile: (file: File) => void; disabled?: boolean; }`. A drop area + hidden `<input type="file" accept=".xlsx,.csv">`; supports both drag-drop and click-to-choose. On a chosen/dropped file, do a client-side size pre-check (`file.size` over ~4MB → show an honest inline error matching the server's 4mb limit, do not call `onFile`) then call `onFile(file)`. Show the selected file name once chosen. Pure view — no Server Action calls here (the container owns previewImport).

**ImportSummary.tsx** — props `{ result: { imported: number; skipped: number; duplicates: number }; onReset: () => void; }`. Render the three counts explicitly as an audit trail (distinct labeled stats, not a single toast), a "View Holdings" link to `/holdings` (Next `<Link>`), and an "Import another file" button calling `onReset`. On mount (`useEffect`, once), fire the existing `refreshPricesNow()` from `@/server-actions/prices` as fire-and-forget inside a `startTransition` (imported instruments get priced so Holdings shows live P&L when the user follows the link — 04-RESEARCH Pattern 5). Swallow/ignore its result here (its own revalidatePath refreshes Holdings); never block the summary render on it. Do NOT auto-redirect (locked decision — the summary is the audit trail).
  </action>
  <verify>
1. `grep -l "'use client'" src/components/import/ImportDropzone.tsx src/components/import/ImportSummary.tsx` → both.
2. `grep "refreshPricesNow" src/components/import/ImportSummary.tsx` → matches; `grep -c "router.push\|redirect(" src/components/import/ImportSummary.tsx` → 0 (no auto-redirect).
3. `grep "accept=\".xlsx,.csv\"\|accept='.xlsx,.csv'" src/components/import/ImportDropzone.tsx` → matches (accepts both broker file types).
4. `npx tsc --noEmit` passes.
  </verify>
  <done>ImportDropzone accepts .xlsx/.csv via drag-drop or click with a client-side size guard and reports the file up via onFile; ImportSummary shows explicit imported/skipped/duplicate counts + a Holdings link, fires refreshPricesNow once, and never auto-redirects.</done>
</task>

<task type="auto">
  <name>Task 2: PreviewTable — compact expandable rows + category chips + bulk toggles</name>
  <files>src/components/import/PreviewTable.tsx</files>
  <action>
`'use client'`. Props `{ preview: Extract<ImportPreview, { ok: true }>; choices: CommitChoices; onChoicesChange: (c: CommitChoices) => void; }` (types from `@/lib/import/types`).

- **Category chips row:** one chip per RowStatus present, each showing the category label + count from `preview.categories` (valid, duplicate, unsupported, invalid, unmatched). Color by meaning (reuse StalenessBadge's palette conventions: success/warning/danger/muted).
- **Bulk-only toggles (IMPT-03, locked):** category-level switches ONLY — e.g. "Skip duplicates / Import duplicates anyway" bound to `choices.importDuplicates`, and (if you surface it) unsupported handling — calling `onChoicesChange`. NO per-row checkbox anywhere. Show a one-line explanation of what each category does on commit (valid→import, duplicate→skip by default, unsupported→skip, invalid/unmatched→not importable here).
- **Compact rows (locked):** a table/list where each row shows symbol, type, quantity, and a status badge. Clicking a row toggles an expanded panel showing every `rawFields` entry, the normalized parsed values, the resolved instrument (or "unmatched"), and any `statusReason`/validation message. Use local `useState<Set<number>>` for which rows are expanded (view state only — NOT an import selection; selection is category-level).
- Rows are display-only regarding import selection — the only import controls are the bulk category toggles. Unmatched rows visibly indicate they become importable once resolved in the mapping section.
  </action>
  <verify>
1. `grep "'use client'" src/components/import/PreviewTable.tsx` → matches.
2. `grep -ci "checkbox\|per-row" src/components/import/PreviewTable.tsx` → 0 (bulk-only, no per-row selection).
3. `grep "importDuplicates" src/components/import/PreviewTable.tsx` → matches (bulk toggle wired to choices).
4. `grep "categories" src/components/import/PreviewTable.tsx` → matches (per-category counts rendered).
5. `npx tsc --noEmit` passes; `npm run build` compiles this component.
  </verify>
  <done>PreviewTable renders per-category counts, bulk-only category toggles wired to CommitChoices (no per-row checkboxes), and compact rows that expand on click to full parsed detail + validation messages.</done>
</task>

<task type="auto">
  <name>Task 3: SymbolMappingSection — resolve unmatched symbols (search + create)</name>
  <files>src/components/import/SymbolMappingSection.tsx</files>
  <action>
`'use client'`. Props `{ broker: ImportBroker; unmatchedSymbols: string[]; growwIsinBySymbol?: Record<string,string>; value: CommitChoices['mappings']; onChange: (m: CommitChoices['mappings']) => void; }`.

- Render ONE entry per unique unmatched broker symbol (a dedicated "Resolve symbols" section, 04-RESEARCH resolution) — resolving one entry fixes all rows for that symbol.
- Each entry offers TWO resolution paths:
  1. **Map to existing:** a search box that calls the existing `searchInstrumentsAction(query)` from `@/server-actions/portfolio` (debounced, same pattern as HoldingFormDialog's instrument search — reuse, do not build a new picker) and lets the user pick a real instrument; records `{ brokerSymbol, instrumentId }` into `value` via `onChange`.
  2. **Create new instrument:** a small form (ticker, ISIN, exchange select NSE/BSE/NASDAQ/NYSE/OTHER, display name, currency INR/USD) recording `{ brokerSymbol, create: {...} }`. **ISIN handling (never fabricate):** for Groww, prefill ISIN from `growwIsinBySymbol[symbol]` (the file has it). For Robinhood, the ISIN field is REQUIRED and empty (the file has none — the user must supply it; show helper text "Robinhood exports carry no ISIN — enter the instrument's ISIN"). Validate ISIN shape client-side (`^[A-Z]{2}[A-Z0-9]{9}[0-9]$`) before allowing submit; the server RPC validates again.
- Show which symbols are still unresolved so the user sees nothing is being dropped. The container (04-06) disables commit while any unmatched symbol that the user chose to import remains unresolved.
  </action>
  <verify>
1. `grep "searchInstrumentsAction" src/components/import/SymbolMappingSection.tsx` → matches (reuses existing action).
2. `grep "A-Z]{2}\|[A-Z0-9]{9}" src/components/import/SymbolMappingSection.tsx` → matches (client-side ISIN validation, no fabricated placeholder).
3. `grep -i "robinhood" src/components/import/SymbolMappingSection.tsx` → matches (ISIN-required helper path for the no-ISIN broker).
4. `npx tsc --noEmit` passes; `npm run build` compiles this component.
  </verify>
  <done>SymbolMappingSection lists each unique unmatched symbol once, resolves via the reused instrument search or a create-new form that prefills the Groww ISIN and requires (never fabricates) the Robinhood ISIN, and records resolutions into CommitChoices.mappings.</done>
</task>

</tasks>

<verification>
**Static (run now):** grep checks above; `npx tsc --noEmit` clean; `npm run build` compiles all four components (they are pure client components with typed props — no DB needed to build).

**DEFERRED (visual/functional — needs the running app + live DB):** actually seeing the compact rows expand, the bulk toggles change what commits, the mapping search return real instruments, and the summary fire the price refresh — 04-07's human-verify checkpoint.
</verification>

<success_criteria>
Four client components exist under `src/components/import/` implementing the locked UX: dropzone (both file types, size guard), compact+expandable preview with bulk-only category toggles and no per-row checkboxes, a resolve-symbols section reusing searchInstrumentsAction with an honest ISIN policy (prefilled Groww / required Robinhood, never fabricated), and an audit-trail summary that fires refreshPricesNow and links to Holdings without auto-redirecting. tsc and build are clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-csv-import/04-05-SUMMARY.md`
</output>
