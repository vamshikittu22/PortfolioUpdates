---
phase: 04-csv-import
plan: 07
type: execute
wave: 6
depends_on: ["04-03", "04-06"]
files_modified: []
autonomous: false
requirements: [IMPT-01, IMPT-02, IMPT-03, IMPT-04, IMPT-05]

must_haves:
  truths:
    - "Both parsers are validated against the USER's REAL Groww XLSX and REAL Robinhood CSV (not just synthetic fixtures) — the LOW-confidence Groww layout is confirmed or the loud ImportParseError points at exactly what differs (04-RESEARCH Open Question 1)."
    - "A human confirms, once the 04-01 migration is applied to the live DB with consent: importing a Groww export and a Robinhood export parses rows into a preview with per-row validation, duplicate detection, and bulk skip/override before anything commits (IMPT-01/02/03)."
    - "A human confirms an unmatched symbol can be mapped/created rather than silently dropped, and that imported holdings show live P&L after following the summary's Holdings link (IMPT-04 + success criterion 4)."
    - "A human confirms re-importing the SAME file adds zero duplicate transactions (idempotent via import batch id + row hash), and the two-user RLS test passes green against the live DB for import_batches/symbol_mappings (IMPT-05 + AUTH-04 discipline)."
  artifacts: []
  key_links: []
---

<objective>
Close the phase's live-verification debt. Every prior Phase 4 plan (04-01 through 04-06) was written and statically/unit verified (`npx tsc --noEmit`, grep checks, `npm run test:import-primitives`, `npm run test:import-parse` against SYNTHETIC fixtures, `npm run build`) but nothing has run against a real broker file, the live database, or through the browser — this environment has no local Docker and the 04-01 migration is deliberately unapplied until the user consents to push it (matching how Phase 1/2/3 each closed with a live checkpoint). Two debts are specific to this phase: the Groww XLSX layout is LOW-confidence until validated against a real export, and idempotency/RLS are DB behaviors that static review cannot prove.

Purpose: this is the phase's honesty gate. Phase 4's success criteria are about observed behavior — real files parse, the preview gates the commit, unmatched symbols are resolvable not dropped, re-import is idempotent, imported holdings show live P&L. None of that is claimable from static checks alone.
Output: either a completed live verification, or an explicit DEFERRED record in the plan SUMMARY (with reason: no consent to push the migration / no live run available) — never a fabricated pass.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-csv-import/04-RESEARCH.md
@.planning/phases/04-csv-import/04-01-PLAN.md
@.planning/phases/04-csv-import/04-03-PLAN.md
@.planning/phases/04-csv-import/04-06-PLAN.md
@.planning/phases/03-price-pipeline-pnl-scheduling/03-06-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Real-file fixtures + live end-to-end import verification</name>
  <files>none (verification-only; any real broker files the user drops into scripts/fixtures/ are gitignored if personal — a sanitized copy may be committed, but this task writes no source)</files>
  <what-built>
The complete Phase 4 import feature: schema + find_or_create_instrument RPC (04-01), pure primitives (04-02), broker parsers + matching + duplicate detection proven against synthetic fixtures (04-03), previewImport/commitImport Server Actions with body-size config (04-04), and the progressive /import UI with a Holdings entry point (04-05, 04-06).

This environment has no local Docker and the 04-01 migration is intentionally unapplied — everything above was written and statically/unit verified but never run against a real file, the live DB, or the browser. This checkpoint is where that gap closes.
  </what-built>
  <action>
See how-to-verify. This is verification-only — no source is written by this task. If a step fails, describe observed vs. expected so it can be fixed in a follow-up plan before Phase 4 is marked complete; do not silently patch code from within this checkpoint. Applying the 04-01 migration requires EXPLICIT user consent (it is a live-DB schema change) — if consent is not given, record the whole checkpoint DEFERRED.
  </action>
  <how-to-verify>
Prerequisites: the user provides a real Groww holdings-statement `.xlsx` and a real Robinhood activity-report `.csv`; `.env.local` has real Supabase credentials; and the user consents to apply this phase's migration.

1. **Real-file parser check (no DB needed):** Drop the real Groww `.xlsx` and Robinhood `.csv` into `scripts/fixtures/` and point `scripts/import-parse-test.ts` at them (or a one-off tsx invocation). Confirm the Groww parser finds the header row and produces one synthetic BUY per equity holding with the sheet's quantity/avg-price (feed through `deriveHoldings` → avgCost matches the statement), and the Robinhood parser classifies Buy/Sell/SPL and reports CDIV/etc. as unsupported. If the Groww layout differs, confirm the parser throws the loud ImportParseError echoing the real first rows, then adjust the header-scan fuzzy matchers to the real spellings (that adjustment, if needed, is a follow-up fix, not part of this checkpoint).
2. **Apply the migration (with consent):** `npx supabase db push` to apply `<timestamp>_csv_import.sql`. Confirm `import_batches`, `symbol_mappings`, the `transactions.import_batch_id`/`import_row_hash` columns, the partial unique index, and `find_or_create_instrument` exist (Supabase Studio or `psql \d`).
3. **RLS test green:** `npm run test:rls` → PASS, now covering import_batches + symbol_mappings two-user isolation (04-01 Task 3).
4. **Preview gates commit (IMPT-03):** `npm run dev`, sign in, go to Holdings → click Import → drop the real Robinhood CSV. Confirm the preview shows per-category counts, compact rows that expand to full detail, and bulk toggles — and that NOTHING is written yet (check the DB).
5. **Mapping, not dropping (IMPT-04):** Confirm any unmatched symbol appears in the Resolve-symbols section; map one to an existing instrument and create one new instrument (enter a real ISIN for the Robinhood one — confirm the RPC creates it and it is NOT a fabricated placeholder). Confirm you cannot commit while an intended-import symbol is unresolved.
6. **Commit + live P&L (IMPT-01/02, success criterion 4):** Commit; confirm the result summary shows explicit imported/skipped/duplicate counts and does NOT auto-redirect. Follow the Holdings link; confirm the imported holdings appear and show live prices/P&L (the summary's refreshPricesNow fired). Repeat with the real Groww XLSX.
7. **Idempotency (IMPT-05):** Re-import the exact same file. Confirm the prior-batch banner appears, every row is flagged duplicate and defaulted to skip, and committing adds ZERO new transactions (holdings quantities unchanged). Confirm the Groww "already held" rule flags a second Groww snapshot of an existing position.
8. **Honest failure:** Feed a malformed file (e.g. a random .xlsx with no ISIN header) and confirm a loud "unrecognized format" error naming what it saw — never a partial silent import.
  </how-to-verify>
  <verify>All 8 steps pass against real broker files + a live Supabase, or the checkpoint is recorded DEFERRED with the specific reason.</verify>
  <done>Real Groww/Robinhood exports parse correctly; the live import flow previews, maps, commits, shows live P&L, and is idempotent on re-import; the RLS test passes for the new tables — OR the checkpoint is explicitly and honestly recorded as DEFERRED (no migration consent / no live run), matching Phase 1/2/3 deferral precedent. Phase 4 is not claimed verified on static checks alone.</done>
  <resume-signal>Type "approved" once all 8 steps pass, or describe which step failed (observed vs. expected) so it can be fixed before Phase 4 is marked complete.</resume-signal>
</task>

</tasks>

<verification>
**DEFERRED / human checkpoint (Task 1):** everything requiring the user's real broker files, the applied migration (consent-gated), a live database, and the running app. If those are unavailable when this plan executes, record the checkpoint DEFERRED in the SUMMARY rather than fabricating a pass — this is the plan's entire purpose and its only task.
</verification>

<success_criteria>
The 8-step live verification either passes in full against real broker exports + a live Supabase, or is explicitly and honestly recorded as deferred pending that infrastructure and the user's migration consent. Phase 4 is not marked verified on the basis of static/unit checks against synthetic fixtures alone.
</success_criteria>

<output>
After completion, create `.planning/phases/04-csv-import/04-07-SUMMARY.md`
</output>
