---
phase: 03-price-pipeline-pnl-scheduling
plan: 06
type: execute
wave: 5
depends_on: ["03-04", "03-05"]
files_modified: []
autonomous: false
requirements: [PRICE-02, PRICE-03, PRICE-04]

must_haves:
  truths:
    - "A human, once a live Supabase + deployed domain exist, can confirm: the pg_cron job actually fires and updates price_cache on its own schedule."
    - "A human can confirm: clicking 'Refresh now' fetches a real current price and the UI updates without a manual reload."
    - "A human can confirm: a wrong/missing refresh secret is rejected with 401 before touching the database."
    - "A human can confirm: a deliberately-broken fetch (e.g. an invalid price_source_symbol) shows a stale-with-warning badge, never a fabricated price."
  artifacts: []
  key_links: []
---

<objective>
Close the phase's live-verification debt. Every prior Phase 3 plan (03-01 through 03-05) was written and statically verified (`npx tsc --noEmit`, grep checks, unit tests for the pure logic layer) but NEVER executed against a real database, a real deployed domain, or a real pg_cron scheduler — this environment has no Docker and no live Supabase (see .planning/STATE.md), matching the exact same gap Phase 1 (01-04) and Phase 2 (02-06) each closed with a checkpoint of this shape.

Purpose: this is the phase's honesty gate. The roadmap's Phase 3 success criteria are about *observed* behavior (prices actually refresh on schedule, a failed fetch actually shows a warning, not fabricated data) — none of that can be claimed true from static review alone.
Output: either a completed 8-step live verification (if a live Supabase + deployed domain now exist), or an explicit DEFERRED record in the plan SUMMARY — never a fabricated pass.
</objective>

<execution_context>
@C:/Users/kittu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-price-pipeline-pnl-scheduling/03-01-SUMMARY.md
@.planning/phases/03-price-pipeline-pnl-scheduling/03-04-SUMMARY.md
@.planning/phases/03-price-pipeline-pnl-scheduling/03-05-SUMMARY.md
@.planning/phases/02-schema-persistence-hydration/02-06-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live verification of scheduled + on-demand price refresh and honest staleness</name>
  <files>none (verification-only task, no files modified)</files>
  <what-built>
The complete Phase 3 price pipeline: schema (03-01: fx_cache, re-keyed price_cache, pg_cron/pg_net migration), pure logic proven by unit tests (03-02: parsing, corporate-action detection, dedup, auth guard, FX conversion, P&L math), network wrappers (03-03), the orchestration/route/Server Action (03-04), and the UI (03-05: real prices, staleness badges, FX-visible totals, corporate-action flags, refresh button).

This environment has no Docker and no live Supabase — everything above was written and statically/unit-test verified but never executed end-to-end. This checkpoint is where that gap closes, once a live Supabase project and a deployed (or `npm run dev` reachable) domain exist.
  </what-built>
  <action>
See how-to-verify below. This is a verification-only checkpoint; no code is written by this task. If any step fails, describe the observed vs. expected behavior so it can be fixed in a follow-up plan before Phase 3 is marked complete — do not silently patch code from within this checkpoint task.
  </action>
  <how-to-verify>
Once a live Supabase exists (local `npx supabase start` with Docker, or a hosted project) and `.env.local` has real `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` / `PRICE_REFRESH_SECRET`:

1. Apply all migrations through 03-01: `npx supabase db push` (or `db reset` locally). Confirm `price_cache` now has an `instrument_id` primary key and `fx_cache` exists (`\d price_cache`, `\d fx_cache` in `psql`, or the Supabase Studio table view).
2. Start the app (`npm run dev`) and sign in as a user with at least one held instrument and one watched instrument from Phase 2's testing (ideally including the NSE/NYSE INFY dual listing from the seed data, to prove the two rows price independently).
3. Manually trigger the refresh route with the real secret: `curl -i -X POST http://localhost:3000/api/prices/refresh -H "Authorization: Bearer <PRICE_REFRESH_SECRET from .env.local>"` → expect `200` with a JSON summary (`succeeded`, `failed`, counts). Then repeat with a wrong/missing header → expect `401`, and confirm via Supabase logs/Studio that no query was attempted for the unauthorized request.
4. Refresh the Holdings page in the browser → confirm real prices, day-change, and total-change now render for priced instruments, replacing Phase 2's em-dash placeholders. Confirm the NSE and NYSE INFY rows (if both are held/watched) show different prices/currencies, not the same value.
5. Click "Refresh now" on the Holdings page → confirm a new fetch happens (network tab / server logs) and the page updates without a manual browser reload (Server Action + revalidatePath). Click it again within 60 seconds → confirm the dedup guard causes it to report "skipped" for already-fresh instruments rather than re-fetching everyone.
6. Simulate a failure: manually set an instrument's `price_source_symbol` to something invalid (e.g. `'NOTAREALTICKER'`) directly in the DB, trigger a refresh, and confirm that instrument's row shows `fetch_error` set and the UI renders a stale/error badge — NOT a fabricated price, and NOT a crash. Revert the symbol afterward.
7. Simulate a corporate action: manually set a `price_cache` row's `corporate_action_flag = true` (or engineer a >40% change via two manual price writes) and confirm the UI shows the warning pill next to that holding WITHOUT hiding the price or blocking the row.
8. Register the pg_cron job for real: run the two `ALTER DATABASE ... SET app.settings...` statements from 03-01's cron migration comment with real values (deployed domain + secret), confirm `SELECT * FROM cron.job WHERE jobname = 'refresh-price-cache-every-3h';` shows the scheduled job, and — if practical within the verification window — observe one real scheduled run in `cron.job_run_details` (if a 3-hour wait isn't practical, temporarily reschedule to `'*/2 * * * *'` for a 2-minute test run, confirm one fires, then restore the 3-hour schedule).
  </how-to-verify>
  <verify>All 8 steps above pass against a live Supabase instance and (for step 8) a real or locally-served deployment.</verify>
  <done>Scheduled refresh, on-demand refresh, dedup, honest failure handling, and corporate-action flagging are confirmed live, or explicitly recorded as DEFERRED (with reason: no live Supabase/deployment available) in the plan SUMMARY — matching Phase 1 (01-04) and Phase 2 (02-06)'s honest-deferral precedent.</done>
  <resume-signal>Type "approved" once all 8 steps pass, or describe which step failed (observed vs. expected) so it can be fixed before Phase 3 is marked complete.</resume-signal>
</task>

</tasks>

<verification>
**DEFERRED / human checkpoint (Task 1):** everything requiring a live database, a real Yahoo Finance/FX network call, and (for step 8) a reachable deployed URL for pg_net to call. If no live Supabase is available when this plan executes, record the checkpoint as DEFERRED in the SUMMARY rather than fabricating a pass — this is the plan's entire purpose and its only task.
</verification>

<success_criteria>
The 8-step live verification either passes in full against a real Supabase + deployment, or is explicitly and honestly recorded as deferred pending that infrastructure. Phase 3 is not claimed "verified" on the basis of static checks alone.
</success_criteria>

<output>
After completion, create `.planning/phases/03-price-pipeline-pnl-scheduling/03-06-SUMMARY.md`
</output>
